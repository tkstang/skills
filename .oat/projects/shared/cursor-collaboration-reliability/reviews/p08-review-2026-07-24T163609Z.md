---
oat_generated: true
oat_generated_at: 2026-07-24T16:36:09Z
oat_review_scope: p08
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: p08

**Reviewed:** 2026-07-24T16:36:09Z
**Scope:** Narrow Phase 8 re-review of `p08-t32` at clean HEAD `2586303`
**Files reviewed:** 7
**Commits:** `33a97c4cc72de8aeb7c2b47050b0021a7fdf91de..258630316cdf9da54be8f5f762e8feecf494b3f2` (3 commits)

## Summary

The sole finding from the archived `2026-07-24T16:31:13Z` review is corrected
at its requested surfaces: wildcard `arm()` is scalar-only,
`compareAndSwapTrigger()` accepts a scalar or clock callback consistently with
runtime/specific declarations, and the ambient consumer proves CAS callback
acceptance plus `arm()` callback rejection. The remediation does not yet pass,
however, because the same commit changed wildcard `run()` to accept the clock
union even though its runtime and specific declaration remain scalar-only.
Live OAT status correctly reports Phase 8 at 32/32 and the project at 72/72.

Findings: 0 critical, 0 important, 1 medium, 0 minor

## Findings

### Critical

None.

### Important

None.

### Medium

- **The ambient clock union was also applied to scalar-only `run()`** (`skills/session-observer-collab/scripts/mjs-modules.d.ts:113`)
  - Issue: `p08-t32` correctly restores `arm()` and updates
    `compareAndSwapTrigger()`, but it also changes the wildcard `run()` third
    parameter from `now?: number` to `clock?: number | (() => number)`.
    `collab-control.mjs:388` and `collab-control.d.ts:90` still require a scalar
    timestamp, and the runtime passes that value into `pruneLeases()`,
    `arm()`, `disarm()`, and `status()`. The wildcard contract therefore
    compiles an unsupported callback that can fail in date/arithmetic paths.
    `ambient-types.ts` tests `arm()` and trigger CAS but does not exercise
    `run()`, so both targeted and aggregate type-checks remain green.
  - Fix: Restore the wildcard `run()` third parameter to `now?: number` and
    extend `tests/session-observer-collab/ambient-types.ts` with a compile-time
    rejection for a `run()` callback, alongside the existing positive scalar
    contract. Re-run the ambient/aggregate type-check, validation, and changed
    skill-version check.
  - Requirement: NFR3, NFR6; `p08-t32`

### Minor

None.

## Requirements/Design Alignment

**Evidence sources used:** archived
`reviews/archived/p08-review-2026-07-24T163113Z.md`, `plan.md`,
`implementation.md`, `state.md`, commits `6197aa7` and `2586303`, current
runtime/specific/wildcard declarations, `ambient-types.ts`, and live OAT
status.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| NFR3 | partial | `arm()` and trigger CAS now align, but wildcard `run()` advertises a callback unsupported by runtime and its specific declaration. |
| NFR6 | partial | Ambient coverage proves the two requested contracts but does not catch the adjacent `run()` declaration drift introduced by the same fix. |

### Extra Work (not in declared requirements)

The `run()` clock-union change is outside the requested fix and is the Medium
finding above.

## Verification Commands

```bash
pnpm exec tsc -p tests/session-observer-collab/tsconfig.types.json
pnpm run type-check
pnpm run validate
pnpm run validate:skill-versions --base-ref 33a97c4cc72de8aeb7c2b47050b0021a7fdf91de
oat project status --project-path .oat/projects/shared/cursor-collaboration-reliability --json
```

Observed results: ambient and aggregate type-checks, repository validation, and
the one changed-skill version check passed. Live OAT status reports Phase 8 at
32/32, the project at 72/72, a null current task, and no blocker. Per the
review scope, no runtime or full-suite tests were rerun.

## Recommended Next Step

Run `oat-project-review-receive` to convert the residual Medium ambient
`run()` declaration finding into one bounded remediation task before Phase 8
can pass.
