---
oat_generated: true
oat_generated_at: 2026-07-24T13:52:10Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: auto
oat_project: /Users/tstang/Code/cursor-collaboration-reliability/.oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: final

**Reviewed:** 2026-07-24T13:52:10Z
**Scope:** Entire implemented delta from `origin/main` through clean HEAD `b9dea1e`
**Files reviewed:** 133 changed files in the authoritative range; load-bearing source, test, documentation, and lifecycle artifacts inspected directly
**Commits:** `63759a07f3e627f40736e4b5287a641f4ddee829..b9dea1efb2bdca040b91d084d2cf8784e83f5522`

## Summary

The two prior Important implementation findings are substantively resolved:
exact provider-artifact cleanup now survives cross-root preservation-budget
exhaustion under separate finite discovery/removal budgets, and live OAT status
recognizes Phase 8 at 18/18 and the project at 58/58. Final closeout remains
blocked by stale lifecycle artifact fields that still claim 16/56 and an active
`p08-t17`, plus a narrower aggregate-boundedness gap where provider directory
entries are materialized by `readdir()` before the new budgets account for
them.

Findings: 0 critical, 1 important, 1 medium, 0 minor

## Review Orchestration

| Wave | Task class | Classification rationale | Selected target | Acceptance / outcome | Floor satisfaction | Fallback |
| ---- | ---------- | ------------------------ | --------------- | -------------------- | ------------------ | -------- |
| 1 | `consequential` | Exact cleanup, state ownership, and final release readiness have silent-residue and misrouting consequences. | `default` / `gpt-5.6-sol` high | First payload was rejected before start because full-history forks cannot override model/effort; corrected no-history launch was rejected before start by the agent-thread limit. No worker report was produced. | Selected target satisfied the consequential floor; no child started. | `caller-inline`; below-floor fallback disallowed |

**Primary reconciliation:** The primary reviewer covered both intended lanes
inline, reopened the authoritative source and tests, ran the focused and full
verification matrices, reproduced the current OAT status by live read-back, and
accepted no unverified worker claims.

## Findings

### Critical

None

### Important

- **Closeout artifacts still disagree with the canonical 58-task lifecycle state** (`.oat/projects/shared/cursor-collaboration-reliability/plan.md:1313`)
  - Issue: The implemented task sections and live
    `oat project status --project-path ... --json` now correctly report Phase 8
    at 18/18 and the project at 58/58, resolving the prior parser-recognition
    defect. However, the plan's closeout section still says Phase 8 has 16
    tasks, total completion is 56, and readiness depends on all 56 tasks
    (`plan.md:1313-1317`). The state frontmatter also still declares
    `oat_current_task: p08-t17` (`state.md:2`) despite the body and live parser
    reporting no current task, and the canonical `p08-t18` section retains
    `this commit` instead of the durable `40b2022` SHA
    (`implementation.md:1825`). These conflicting lifecycle sources can
    mislead closeout, resume, and human review even though the parser now
    derives the correct totals.
  - Fix: Update the plan's Implementation Complete summary to Phase 8 18 tasks
    and total 58, change its readiness sentence to 58, clear the stale current
    task in `state.md` while recording the current bookkeeping commit, and
    replace the `p08-t18` placeholder with `40b2022`. Then read back both the
    artifacts and `oat project status` to require one consistent 58/58,
    18/18, no-current-task state.

### Medium

- **Provider-state entry budgets begin only after whole directory listings are materialized** (`scripts/probe-cursor-wake-surfaces.mjs:622`)
  - Issue: The new shared entry, byte, and elapsed-time counters bound
    fingerprinting and artifact inspection after entries reach the loop, but
    `snapshotProviderState()` first awaits an unbounded `readdir(root.path)`.
    The same eager allocation occurs inside fingerprint/inspection trees
    (`scripts/probe-cursor-wake-surfaces.mjs:566`,
    `scripts/probe-cursor-wake-surfaces.mjs:694`) and while finding post-run
    names (`scripts/probe-cursor-wake-surfaces.mjs:870`). A provider root or
    nested artifact with far more than 20,000 names can therefore allocate and
    sort the entire listing, and spend unaccounted enumeration time, before the
    aggregate entry/time budget fails visibly. The cleanup result remains
    fail-closed, but NFR5's deterministic aggregate resource bound is not
    complete.
  - Fix: Iterate provider roots and nested directories with `opendir()` (or an
    equivalently capped iterator), consume the shared entry/time budget before
    retaining or queueing each name, and keep queued-name memory under the same
    bound. Add oversized root and nested-directory tests that prove the scan
    stops at the configured aggregate limit without first materializing the
    complete listing.
  - Requirement: NFR5

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `spec.md`, `design.md`, `plan.md`,
`implementation.md`, `state.md`, the archived prior final review, the
authoritative `origin/main..b9dea1e` commit range, canonical and generated
runtime source, collaboration runtime modules, focused/full tests, Cursor
runtime evidence, user/engineering documentation, and live OAT project status.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| FR1 | implemented | Exact Cursor identity is canonical-path-derived, body-free, aggregate-index-bounded, and fail-visible on duplicate-index exhaustion. |
| FR2 | implemented | Prefix-stable content, structural filtering, source ordering, keyed delivery, and lifecycle-pending labeling are covered. |
| FR3 | implemented | Lifecycle reconciliation remains independent and only terminal success is completion-eligible. |
| FR4 | implemented | Physical frames, prefix/device/inode continuity, repair, replacement, and explicit recovery remain fail-visible. |
| FR5 | implemented | Engagement, activity, content, lifecycle, buffering, delivery, and health remain independent. |
| FR6 | implemented | Reservation, callback-confirmed output, checkpoint CAS, uncertain replay keys, and cursor separation pass the full suite. |
| FR7 | implemented | Foreground ownership, baseline, stability, deadline, heartbeat, control, and cleanup paths remain bounded. |
| FR8 | implemented | Collaboration completion remains exact-pin, terminal-only, private-cursor, capped, CAS-protected, and fail-closed. |
| FR9 | implemented | Support claims remain evidence-gated; exact new artifacts discovered before preservation-budget exhaustion are removed and cleanup uncertainty cannot promote a result. |
| FR10 | implemented | Buffered-manual remains the strongest evidence-backed Cursor tier; unavailable callback surfaces are not promoted. |
| NFR1 | implemented | Canonical containment, exact ownership, synthetic provenance, and local authority remain fail-closed. |
| NFR2 | implemented | Durable state/evidence is structural and redacted; owner-only state permissions and privacy scans pass. |
| NFR3 | implemented | Claude Code, Codex, Export Session Transcript, digest/envelope compatibility, and provider-owner routes remain green. |
| NFR4 | implemented | Runtime remains dependency-free, generated outputs are synchronized, and all three changed skill versions are valid. |
| NFR5 | partial | Runtime/watch/identity and cleanup operations have finite counters, but eager directory listings occur before provider-state aggregate budgets consume entries. |
| NFR6 | partial | Code, probes, evidence, and docs are reproducible, but plan/state closeout fields still conflict with the canonical 58/58 read-back. |

### Design Alignment

The implementation follows the design's separate observation/completion
projections, exact identity and continuity guard, isolated Cursor state,
atomic delivery, private completion cursor, sanitized evidence matrix, and
honest buffered-manual fallback. The Important finding is artifact alignment
drift rather than a defect in the implemented 58-task behavior. The Medium
finding is a residual implementation gap against the design's aggregate
resource-limit contract.

### Extra Work (not in declared requirements)

None.

## Verification Commands

```bash
pnpm exec vitest run tests/tooling/cursor-wake-probe.test.ts tests/session-observer/locate.test.ts tests/transcript-core/runtimes.test.ts
pnpm run test
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run smoke
node scripts/validate-cursor-evidence.mjs --base-ref origin/main
pnpm run validate:internal-flags
pnpm run validate:skill-versions --base-ref origin/main
pnpm --dir documentation run docs:format:check
pnpm --dir documentation run build
oat project status --project-path .oat/projects/shared/cursor-collaboration-reliability --json
git diff --check origin/main..HEAD
```

Observed results: 141/141 focused tests passed; 1,526 full-suite tests passed
with 1 skipped; type-check, generated-output parity, repository validation,
smoke, 34-file Cursor evidence validation, 66 internal-flag checks, all 3
changed-skill version checks, docs formatting, the 34-page production docs
build, live OAT 58/58 read-back, and diff checks passed.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert the Important
artifact-alignment finding and Medium bounded-enumeration finding into stable
fix tasks.
