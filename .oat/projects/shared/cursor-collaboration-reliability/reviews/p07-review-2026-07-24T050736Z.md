---
oat_generated: true
oat_generated_at: 2026-07-24T05:07:36Z
oat_review_scope: p07
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: p07

**Reviewed:** 2026-07-24T05:07:36Z
**Scope:** Whole Phase 7 from
`8d24d552a0d642c3aee103870e6270137a889dcc` through
`1dfce5d596612b565e3496363eacff5790138dcb`
**Files reviewed:** 26 changed paths
**Commits:** 9 commits
**Verdict:** PASSED
**Reconnaissance:** not-attempted

## Summary

Phase 7 resolves all four findings from the configured final review. The
selected Cursor success prefix is now bound through the lease CAS for both
owners, bounded stability confirmation makes progress during later growth,
completion selection can deliver a complete successful prefix before a valid
pending suffix, and the evidence validator permits only narrowly typed OAT gate
bookkeeping while retaining its privacy and credential protections.

Focused adversarial coverage, the full test suite, generated-output parity,
repository and documentation gates, privacy validation, dogfood parity, and
provider-residue checks all passed. No actionable finding remains in the Phase 7
change set.

Findings: 0 critical, 0 important, 0 medium, 0 minor

## Prior Finding Disposition

| Prior configured-gate finding | Disposition |
| --- | --- |
| Critical: a completion wake could be authorized from a stale success scan after the selected range became non-successful. | Resolved by `6454740`. Both Cursor-owner and Codex-owner paths now carry the selected boundary, hash, device, and inode from observation through a bounded reread immediately before CAS. Rewrites from `success` to `aborted`, `error`, `cancelled`, or `unknown`, and inode replacement, fail closed without wake, budget spend, cursor advance, or checkpoint mutation. Unchanged success still wakes. |
| Important: later appends restarted the stateful stability candidate instead of confirming its bounded prefix. | Resolved by `a1a8999`. Confirmation is bound to the original candidate boundary and entry keys, the verified prefix can commit while later safe bytes remain, and later content is retained as the next candidate. One-shot A/B and continuous-growth tests prove exact-once ordered delivery through `maxPendingSec` without starvation, replay, or loss. |
| Important: a completed Cursor turn was missed when a later turn was already pending. | Resolved by `31c49e6`. The selector accepts only a fully accounted, unsliced terminal-success prefix followed by a structurally valid `stability-wait` suffix. Both owners wake exactly once, checkpoint only the completed prefix, and leave the pending suffix unread. Malformed, sliced, discontinuous, unaccounted, and unbound shapes remain rejected. |
| Important: exit-gate bookkeeping caused the committed Cursor evidence gate to fail. | Resolved by `0e6cd1c`, with normalized path bookkeeping in `6e16e94`. Narrowly typed accepted/result/receive gate fields, repository SHAs, structural fingerprints, and system-temporary markers are accepted only in the exact shared OAT bookkeeping files. Raw identities, credentials, transcript prose, personal paths, arbitrary hashes, and the same typed-looking values outside that boundary remain rejected. |

## Findings

### Critical

None.

### Important

None.

### Medium

None.

### Minor

None.

## Requirements/Design Alignment

**Evidence sources used:** `spec.md`, `design.md`, `plan.md`,
`implementation.md`, `state.md`, the prior configured final-review artifact,
the authoritative Phase 7 diff, canonical TypeScript and shipped runtime,
focused and full tests, evidence tooling, documentation, user-level dogfood
copies, OAT sync state, and active Cursor provider roots.

| Requirement | Status | Phase 7 evidence |
| --- | --- | --- |
| FR2 | implemented | A bounded stability candidate is confirmed by its original boundary and entry keys even when later safe content has arrived. |
| FR3 | implemented | Both completion owners bind the selected successful range through CAS and can deliver a completed prefix before a pending turn. |
| FR4 | implemented | The same verified selected-prefix snapshot supplies the proposed lease continuity; mutation or replacement fails closed. |
| FR7 | implemented | One-shot and foreground-watch growth tests prove progress through the maximum pending interval without starvation or replay. |
| FR8 | implemented | Wake authorization accepts only the exact verified terminal-success prefix and structurally valid pending-suffix form. |
| FR9 | implemented | Gate evidence remains structured and machine-verifiable without admitting raw identity or personal-path data. |
| NFR1 | implemented | Path identity, prefix identity, ownership, and CAS boundaries remain fail-closed for both owners. |
| NFR2 | implemented | Tracked bookkeeping uses normalized, typed evidence; personal paths and credentials remain rejected. |
| NFR6 | implemented | The four configured-gate interleavings now have direct adversarial regressions and pass the complete repository suite. |

### Extra Work

None. The Phase 7 changes are limited to the four received configured-gate
findings, their generated runtime and documentation consequences, tests, and
lifecycle bookkeeping.

## Cross-Fix Verification

### Selected-success binding

- Cursor-owner and Codex-owner tests cover `success` rewritten to `aborted`,
  `error`, `cancelled`, and `unknown` between selection and CAS.
- Both owners also cover replacement by a different inode in that window.
- Every mutation case leaves the peer cursor at zero, leaves the checkpoint
  unchanged, spends no continuation budget, schedules no loop, and emits no
  wake.
- Unchanged success paths continue to advance and wake.
- Existing path containment, continuity, owner-mismatch, and CAS-conflict
  regressions remain green.

### Bounded-prefix progress during growth

- The one-shot A/B regression appends B during A's confirmation interval,
  delivers A first, retains B, and later delivers B exactly once.
- The foreground-watch regression writes continuously through `maxPendingSec`
  and observes every message exactly once and in source order, with the final
  cursor equal to the source count and no candidate left behind.
- Replacement, shrink, partial-tail, stdout, ownership, reservation, commit,
  and uncertain-delivery regressions remain green.

### Completed prefix before pending suffix

- Both owners select frames 0 through 2 as the complete successful turn, wake
  once, checkpoint through frame 3, and leave pending frames 3 and 4 unread.
- A later invocation does not emit a duplicate wake.
- Malformed, partial, tail-sliced, discontinuous, unaccounted, non-completion,
  and unbound suffix shapes remain rejected.
- The selected-success rewrite and inode-replacement cases remain green after
  the selector relaxation.

### Typed gate bookkeeping

- Exact shared-project `implementation.md`, `plan.md`, `project-log.md`, and
  `state.md` bookkeeping accepts typed gate status, result, receipt, run,
  repository SHA, structural fingerprint, normalized base-ref, and
  system-temporary marker fields.
- The validator still rejects raw session, lease, event, and launch identities;
  credentials; transcript prose; user-specific absolute paths; arbitrary
  hashes or fingerprints; and typed-looking repository values outside the
  approved OAT bookkeeping boundary.
- End-to-end accepted/result/receive bookkeeping and normalized path fixtures
  pass.

## Verification

The following checks passed:

- Focused adversarial Vitest suite:
  `tests/session-observer-collab/cursor-hook.test.ts`,
  `codex-hook.test.ts`, `completion.test.ts`, `codex-install.test.ts`,
  `tests/session-observer/observe.test.ts`, `watch.test.ts`, and
  `tests/tooling/cursor-evidence-validation.test.ts`: 7 files and 168 tests
  passed.
- Full Vitest suite: 120 files passed, 1 skipped; 1,490 tests passed, 1
  skipped; 0 failed.
- `pnpm run build`, `pnpm run type-check`, and `pnpm run build:check`.
- `pnpm run validate` and `pnpm run smoke`.
- `node scripts/validate-cursor-evidence.mjs --base-ref 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773`.
- `pnpm run validate:skill-versions --base-ref 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773`: three changed skills verified.
- `pnpm run validate:internal-flags`: 66 internal tooling skills verified.
- Documentation format check: 31 files passed.
- Documentation production build: 34 pages passed.
- Changed-file oxlint and oxfmt checks.
- Conventional Commit validation for
  `8d24d552a0d642c3aee103870e6270137a889dcc..1dfce5d`.
- `git diff --check
  8d24d552a0d642c3aee103870e6270137a889dcc..1dfce5d`.
- Repository-to-user dogfood parity for `session-observer` and
  `session-observer-collab`; Claude entries resolve to the canonical
  user-level copies and no Cursor user-skill entry is claimed.
- `oat sync --scope all --dry-run --json`: 322 operations, all `skip`/already
  in sync.
- Both active Cursor provider roots are real, non-symlink directories and have
  zero direct children matching `*cursor-wake-probe*`.
- An independent validator probe accepted typed bookkeeping and rejected raw
  session and lease identities, credentials, transcript prose, personal paths,
  and a repository SHA outside OAT bookkeeping.

The build regenerated committed runtime outputs deterministically; the
subsequent build-check and clean-tree check found no drift.

## Configured-Gate State

This artifact is the required whole-Phase 7 review. It does not replace the
configured final lifecycle review or regenerate the configured exit-gate
receipt. The existing configured-gate basis predates the Phase 7 fixes and
remains stale. After this review is received, the normal sequence is:

1. run the mandatory final lifecycle review;
2. rerun the configured exit gate under the persisted block policy; and
3. receive the regenerated gate result.

## Recommendation

Accept Phase 7 with zero findings through the normal
`oat-project-review-receive` flow, then continue to the mandatory final review
and configured exit-gate regeneration. This review created only this artifact;
it did not mutate lifecycle bookkeeping, code, documentation, backlog state,
user-level installations, or provider state.

## Blockers

None.
