---
oat_generated: true
oat_generated_at: 2026-07-24T16:31:13Z
oat_review_scope: p08
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: p08

**Reviewed:** 2026-07-24T16:31:13Z
**Scope:** Phase 8 tasks `p08-t01` through `p08-t31` at clean HEAD `33a97c4`
**Files reviewed:** 55
**Commits:** `3b4ea8049dd86abc8b6feebd6e8687c3d3c8f101..33a97c4cc72de8aeb7c2b47050b0021a7fdf91de` (60 commits)

## Summary

The three findings from the archived `2026-07-24T16:11:17Z` Phase 8 review
are substantively remediated: both owner hooks now evaluate a live clock under
the lease lock after final validation, generic unpinned Cursor discovery is
aggregate-bounded and fail-visible before excess body classification, and the
handoff consistently reports Phase 8 at 31/31, the project at 71/71, and the
1,543-pass/1-skip matrix. Phase 8 retains one Medium declaration-contract
regression: the wildcard ambient edit applied the new clock union to `arm()`
instead of `compareAndSwapTrigger()`, and the ambient consumer test does not
exercise either changed parameter.

Findings: 0 critical, 0 important, 1 medium, 0 minor

## Findings

### Critical

None.

### Important

None.

### Medium

- **The wildcard ambient declaration applies the trigger clock type to the wrong function** (`skills/session-observer-collab/scripts/mjs-modules.d.ts:99`)
  - Issue: `ddd9794` changed the ambient `arm()` third parameter from
    `now?: number` to `clock?: number | (() => number)`, although the runtime
    and paired declaration still require a scalar timestamp
    (`skills/session-observer-collab/scripts/collab-control.mjs:136`,
    `skills/session-observer-collab/scripts/collab-control.d.ts:54`). At the
    same time, the ambient `compareAndSwapTrigger()` declaration still exposes
    `now?: number` at line 212 even though the runtime and paired declaration
    now accept `number | (() => number)`. A consumer compiled against the
    wildcard surface can therefore pass a callback to `arm()` that fails when
    the runtime constructs dates and cannot type the new callback form of the
    trigger CAS. The existing ambient consumer covers the update payload but
    does not exercise either function's time parameter, so aggregate
    type-checking remains green while the shipped declarations drift.
  - Fix: Restore `arm()` to `now?: number`; change the ambient
    `compareAndSwapTrigger()` fifth parameter to
    `clock?: number | (() => number)`; and extend
    `tests/session-observer-collab/ambient-types.ts` to prove the CAS callback
    is accepted while an `arm()` callback is rejected. Run the ambient
    type-check and collaboration/version gates.
  - Requirement: NFR3, NFR6; `p08-t29`

### Minor

None.

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `spec.md`, `design.md`, `plan.md`,
`implementation.md`, `state.md`, archived
`reviews/archived/p08-review-2026-07-24T161117Z.md`, the authoritative Phase 8
commit range, commits `ddd9794`, `ff74098`, and `33a97c4`, canonical/generated
Session Observer source, authored collaboration runtime/declarations, focused
tests, and current git/OAT status.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| FR1 | implemented | Generic public unpinned discovery now shares entry, elapsed-time, byte, and retained-candidate budgets and fails visibly before partial discovery can be treated as exact. |
| FR2 | implemented | Callback-confirmed delivery, same-turn progress, unsupported-shape rejection, and structural-turn slicing remain covered. |
| FR3 | implemented | Completion remains terminal-success-only and selected-prefix-bound. |
| FR4 | implemented | Cursor continuity retains exact safe-prefix checkpoints and fail-visible replacement/mismatch handling. |
| FR5 | implemented | Activity, content, lifecycle, buffering, and watcher health remain independent projections. |
| FR6 | implemented | Native delivery completion and reservation finalization remain covered. |
| FR7 | implemented | Remaining runtime budgets and deterministic virtual-clock watch coverage remain intact. |
| FR8 | implemented | Both owner routes pass a live clock through final validation; trigger CAS evaluates expiry/deadline under the lease lock before mutation, and contention regressions preserve cursor and budgets. |
| FR9 | implemented | Provider-version, preservation, bounded-scan, exact-cleanup, and safe-unavailable evidence remains recorded. |
| FR10 | implemented | Buffered-manual remains the evidence-backed wake tier. |
| NFR1 | implemented | Exact identity and under-lock wake authorization fail closed without widening authority. |
| NFR2 | implemented | Durable evidence remains structural/redacted and validation is recorded green. |
| NFR3 | partial | Runtime behavior remains green, but the wildcard ambient clock declarations contradict the runtime and paired declarations. |
| NFR4 | implemented | Generated outputs are synchronized, shipped runtimes remain dependency-free, and changed skill versions validate. |
| NFR5 | implemented | Wake authorization, generic discovery, provider scans, cleanup, and watcher waits are finite and fail-visible. |
| NFR6 | partial | The handoff and recorded matrix are current, but ambient consumer coverage misses the declaration regression. |

### Extra Work (not in declared requirements)

None.

## Verification Commands

```bash
pnpm exec vitest run tests/session-observer-collab/cursor-hook.test.ts tests/session-observer-collab/codex-hook.test.ts tests/session-observer/locate.test.ts
pnpm exec tsc -p tests/session-observer-collab/tsconfig.types.json
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run validate:skill-versions --base-ref 3b4ea8049dd86abc8b6feebd6e8687c3d3c8f101
```

Observed results: 119 focused tests passed (68 owner-hook and 51 locate tests);
the ambient and aggregate type-checks, generated-output parity, repository
validation, and three changed-skill version checks passed. The recorded
post-fix release matrix of 1,543 passing tests with 1 skipped, live acceptance
19/19, and the remaining build/smoke/evidence/privacy/provider/residue gates
was inspected rather than rerun. Live OAT status reports Phase 8 at 31/31 and
the project at 71/71 with no current task or blocker.

## Recommended Next Step

Run `oat-project-review-receive` to convert the Medium ambient-declaration
finding into one bounded remediation task before Phase 8 can pass.
