---
oat_generated: true
oat_generated_at: 2026-07-24T05:17:04Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: final

**Reviewed:** 2026-07-24T05:17:04Z
**Scope:** Whole project from
`8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773` through
`779f1cedffb4ae12ad2250f19167dd0afe4e4252`, including Configured Exit-Gate
Remediation Phase 7 and all earlier implementation
**Files reviewed:** 127 changed paths
**Commits:** 142 commits
**Verdict:** PASSED

## Summary

The complete project now implements the discovery, specification, design, and
40-task plan without an unresolved correctness, authority, privacy,
compatibility, or evidence gap. All four configured-gate findings are resolved
in the current implementation basis, and their interactions preserve the
selected-success CAS boundary, bounded stability progress, strict
completion-prefix accounting, and evidence-safe lifecycle bookkeeping.

The two earlier final-review watch-state findings also remain resolved after
Phase 7. Focused cross-fix tests, the complete suite, build and generated-output
checks, documentation, evidence/privacy, version and internal-flag checks,
provider synchronization, dogfood parity, and residue checks all passed.

Findings: 0 critical, 0 important, 0 medium, 0 minor

## Review Orchestration

| Wave | Task class     | Classification rationale                                                                                                                                                         | Selected target                                            | Acceptance / outcome                                                                                                  | Floor satisfaction                                      | Fallback                                        |
| ---- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ----------------------------------------------- |
| 1    | hard-reasoning | The completion-selector relaxation and selected-prefix CAS binding interact across two owner hooks, and a silent stale-success miss would have consequential correctness impact. | Native Codex `explorer`, configured `gpt-5.6-sol` / medium | Rejected before child start because the agent thread limit was reached; no worker output was produced or relied upon. | Target met the declared floor; launch was not accepted. | `caller-inline`; below-floor dispatch forbidden |

**Primary reconciliation:** The primary reviewer covered both intended lanes
inline. It reopened the authoritative artifacts and range, inspected canonical
and shipped runtime, inspected both owner hooks and all negative test matrices,
ran the focused and full suites, independently exercised evidence-safe positive
and negative classifications, and verified provider, dogfood, privacy, and
residue state. No delegated claim was accepted because no child started.

## Prior Finding Disposition

| Prior finding                                                                                                                                 | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Final review round 1 — repaired Cursor frames retained stale buffered state and reported one record behind.                                   | Resolved by `15cc61f` and still resolved. Both watch-state copy sites preserve the digest's explicit `null` clear (`src/transcript/session-observer/lib/watch.ts:792-831`), and the repaired-frame regression again passed with frame 4, cleared persisted/in-memory buffering, zero records behind, and at least 30 agreeing later heartbeats (`tests/session-observer/watch.test.ts:1025-1152`).                                                                                              |
| Final review round 2 — durable watch targets retained `recordCount: 3` after repair while in-memory status reported frame 4.                  | Resolved by `6afb9bf` and still resolved. Result-bearing transitions persist the exact total, result-less transitions preserve it, and invalid/regressing values fail without mutation (`src/transcript/session-observer/lib/watch-state.ts:791-818`). The repaired-frame regression again proved durable/status count 4 and zero lag.                                                                                                                                                          |
| Configured gate Critical — a selected Cursor success could be rewritten to a non-success outcome before lease CAS and still authorize a wake. | Resolved by `6454740`. Observation carries a selected boundary, hash, device, and inode (`skills/session-observer-collab/scripts/lib/selected-prefix.mjs:101-198`); both hooks re-read and verify that exact prefix immediately after the injected interleaving and before trigger CAS (`cursor-stop.mjs:257-276`, `codex-stop.mjs:272-297`). Success-to-aborted/error/cancelled/unknown rewrites and inode replacement remain no-wake, no-budget, no-cursor-mutation outcomes for both owners. |
| Configured gate Important — later appends restarted a stateful stability candidate, allowing starvation under continuous growth.              | Resolved by `a1a8999`. Confirmation retains the original boundary and entry keys, verifies its bounded prefix, reserves only through the digest's verified cursor, and retains later safe bytes as the next candidate (`src/transcript/session-observer/lib/observe.ts:954-1049`, `1159-1245`). One-shot A/B and continuous `maxPendingSec` growth tests passed with ordered exact-once progress and no replay or loss.                                                                         |
| Configured gate Important — a valid completed turn was missed when followed by a later pending turn.                                          | Resolved by `31c49e6`. The selector accepts a complete accounted terminal-success prefix only when the entire suffix is a healthy `stability-wait` range (`skills/session-observer-collab/scripts/lib/completion-selection.mjs:113-260`), checkpoints only the selected prefix, and leaves the pending suffix unread. Both owners wake once; malformed, partial, tail-sliced, discontinuous, unaccounted, unbound, and duplicate forms remain rejected.                                         |
| Configured gate Important — accepted gate bookkeeping made the durable Cursor evidence validator fail and retained a personal temporary path. | Resolved by `0e6cd1c` plus terminal path normalization `6e16e94`. Safe exceptions are limited to exact typed fields in the four shared OAT bookkeeping files (`scripts/validate-cursor-evidence.mjs:29-40`, `134-169`). The live 34-file scan passed; independent checks accepted typed gate IDs/SHAs/fingerprints and rejected raw session/lease IDs, credentials, transcript prose, personal paths, arbitrary values, and typed-looking values outside the bookkeeping boundary.              |
| Earlier phase-review findings from p01-p06                                                                                                    | Remain resolved. Their latest phase reviews passed, the full 1,490-test suite is green, generated outputs remain synchronized, and no Phase 7 change weakened exact identity, continuity, ownership, authority, no-op suppression, bounded leases, evidence labels, or conservative wake-tier non-claims.                                                                                                                                                                                       |

## Findings

### Critical

None.

### Important

None.

### Medium

None.

### Minor

None.

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `spec.md`, `design.md`, `plan.md`,
`implementation.md`, `state.md`, prior phase/final/configured-gate review
artifacts, the authoritative merge-base-to-HEAD range, canonical and generated
observer runtime, collaboration runtime and installed bundle, focused and full
tests, evidence tooling and artifacts, documentation, provider views,
user-level dogfood copies, and active Cursor provider roots.

### Requirements Coverage

| Requirement | Status      | Notes                                                                                                                                                                          |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| FR1         | implemented | Exact runtime/session/cwd/transcript identity, canonical containment, diagnostic-only weak candidates, ambiguity rejection, and pin retention remain verified.                 |
| FR2         | implemented | Prefix-stable open content is confirmed at its original bounded byte/entry-key boundary and progresses during later and continuous growth without replay, starvation, or loss. |
| FR3         | implemented | Content availability remains separate from lifecycle completion; non-success outcomes remain non-triggering, and selected success is reverified through CAS.                   |
| FR4         | implemented | Physical frame accounting, prefix/device/inode continuity, fail-visible mutation handling, repair recovery, and selected-range revalidation remain intact.                     |
| FR5         | implemented | Engagement, content, lifecycle, health, buffering, frame totals, records behind, heartbeat, and separate-process status remain independent and truthful.                       |
| FR6         | implemented | Owner-only reservations, expected-checkpoint CAS, output/commit recovery, keyed uncertain replay, and separate observation/completion cursors remain atomic.                   |
| FR7         | implemented | Baseline, catch-up-then-watch, debounce, maximum-pending progress, pin retention, controls, ownership, heartbeat, and cleanup behavior pass.                                   |
| FR8         | implemented | Only an exact, contiguous, verified terminal-success range under a valid private lease can spend bounded continuation; valid pending suffixes remain unread.                   |
| FR9         | implemented | Support claims remain fixture-, validator-, and sanitized-live-evidence gated; durable artifacts contain structural/redacted evidence only.                                    |
| FR10        | implemented | The stronger-wake evaluation remains evidence-backed `buffered-manual`; unavailable surfaces are not promoted and no privileged adapter was added.                             |
| NFR1        | implemented | Local authority, supported-root containment, exact leases, synthetic provenance, owner checks, and no direct peer/transcript mutation remain fail-closed.                      |
| NFR2        | implemented | State and evidence remain structural, owner-local, and prose-free; typed gate bookkeeping exceptions do not exempt personal paths or raw identities.                           |
| NFR3        | implemented | Claude Code, Codex, non-Cursor observer, Export, collaboration, state/schema migration, and documentation regressions pass.                                                    |
| NFR4        | implemented | Shipped runtime remains dependency-free; canonical/generated parity passes and all three changed skill versions are valid and dogfooded.                                       |
| NFR5        | implemented | Scanning, bounded rereads, unchanged polling, stability waits, watch runtime, continuation counts, loops, and expiry remain deterministic and finite.                          |
| NFR6        | implemented | Every changed safety boundary has direct automated coverage, the live evidence scan passes, capability labels remain honest, and commands/results are reproducible.            |

### Extra Work

None. Phase 7 is confined to the four received configured-gate findings,
required generated runtime, installed-bundle and documentation alignment,
adversarial tests, and lifecycle bookkeeping. The overall project remains
inside the discovery non-goals.

## Cross-Fix Verification

### Selected success through CAS

- `observeCursorCompletion()` scans and analyzes the exact pinned transcript,
  captures a selected checkpoint at the completion cursor, and binds its byte
  prefix plus device/inode to the digest.
- Both owner hooks call the same `verifySelectedPrefix()` immediately after
  the testable pre-update interleaving and before `claimAdapterTrigger()`.
- All eight success-to-non-success owner/status combinations and both inode
  replacement cases passed without wake, cursor/checkpoint advance,
  continuation spend, or loop increment. Unchanged success and existing
  ownership/stale-CAS paths remain green.

### Bounded stability progress

- Candidate equality is checked at the original turn, frame boundary, prefix
  bytes, and ordered entry-key set.
- The confirmatory scan may contain later safe bytes, but the intended
  continuity checkpoint is captured at `digest.range.nextIndex`.
- After committing the confirmed prefix, later content is installed as the
  next candidate. One-shot A/B delivery and continuous foreground growth
  delivered every written message once, in order, and ended at the source
  frame count with no candidate stranded.

### Completed prefix before pending suffix

- Frame accounting requires `fromIndex <= nextIndex <= totalFrames`, exact raw
  counts, exact rendered bounds, healthy status, no blocking frame, and zero
  unstable-content filtering.
- A suffix is accepted only when it starts exactly at `nextIndex`, accounts for
  every remaining frame, is labeled `stability-wait`, and agrees with
  `bufferedFromFrame` plus pending lifecycle.
- The selected prefix remains bound to the snapshot used by both owner hooks;
  selector relaxation therefore does not reopen the stale-success race.

### Evidence-safe terminal bookkeeping

- Typed SHA, fingerprint, gate-run, receipt, and normalized marker forms are
  redacted only for identity classification and only in
  `implementation.md`, `plan.md`, `project-log.md`, or `state.md`.
- Personal-path, credential, and transcript-prose checks still inspect the
  original line, so a typed gate field cannot hide those classes.
- Raw UUID/session/lease identities, arbitrary hashes, and the same SHA-shaped
  values outside approved bookkeeping remain rejected.

### Prior watch buffer and count fixes

- The repaired partial frame advances the durable and in-memory cursor/count
  to 4, clears `bufferedFromFrame`, and reports
  `transcriptRecords: 4` / `recordsBehind: 0`.
- At least 30 later heartbeats and a separate status read retain those exact
  values.
- Invalid negative, fractional, unsafe, below-cursor, and regressing durable
  counts remain mutation-free; result-less transitions preserve the prior
  count, and owner/stale CAS checks still precede mutation.

## Deferred Findings

- Deferred Medium count: 0
- Deferred Minor count: 0
- Ledger: None. No accepted deferred item remains to promote, retain, or reject
  at final scope.

## Verification

The following checks passed on
`779f1cedffb4ae12ad2250f19167dd0afe4e4252`:

- Focused cross-fix Vitest suite:
  `cursor-hook.test.ts`, `codex-hook.test.ts`, `completion.test.ts`,
  `codex-install.test.ts`, `observe.test.ts`, `watch.test.ts`,
  `watch-state.test.ts`, and `cursor-evidence-validation.test.ts`: 8 files,
  190 tests passed.
- Full Vitest suite: 238 suites passed; 1,490 tests passed, 1 skipped, 0
  failed.
- `pnpm run build`, `pnpm run type-check`, and `pnpm run build:check`.
- `pnpm run validate` and `pnpm run smoke`.
- `node scripts/validate-cursor-evidence.mjs --base-ref 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773`:
  34 files passed.
- `pnpm run validate:skill-versions --base-ref 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773`:
  three changed skills passed. An initial invocation containing an extra
  argument separator was rejected by the CLI parser; the repository-prescribed
  command above passed.
- `pnpm run validate:internal-flags`: 66 internal tooling skills passed.
- Documentation formatting: 31 files passed; production build: 34 pages
  passed.
- Changed-file checks: 12 authored JS/MJS files passed oxlint; the changed
  formattable set passed oxfmt.
- Conventional Commit validation passed for all 142 commits.
- `oat status --scope all`: 188 entries in sync.
- `oat status --scope user`: 114 entries in sync.
- `oat sync --scope all --dry-run --json`: zero planned or failed operations;
  all 274 plan/extension actions were skips/already synchronized.
- Repository-to-user dogfood directories for `session-observer` 1.0.12,
  `session-observer-collab` 1.0.11, and `export-session-transcript` 1.0.5 are
  byte-identical. Claude provider entries resolve to the canonical user copies;
  absent Cursor user-skill entries are not claimed.
- Independent evidence classification accepted typed gate bookkeeping and
  rejected raw session/lease UUIDs, a personal absolute path, a credential
  shape, transcript prose, and a SHA-shaped value outside OAT bookkeeping.
- `~/.cursor/projects` and `~/.cursor/chats` are real non-symlink directories
  with zero direct `*cursor-wake-probe*` residue.
- `git diff --check` reports only the intentional blank line at EOF in
  `tests/session-observer/fixtures/cursor/framed-blank-lines.jsonl`, which is
  the framing fixture under test.
- Build, documentation generation, and all checks left the pre-artifact
  worktree clean.

## Configured Exit-Gate State

The configured exit gate remains deliberately stale and was not regenerated or
mutated by this review. `state.md` still records:

- `status: stale`
- `attempts_completed: 1`
- `reviewed_head: 5d02b9144cc263660590a7c982024bf29f7351c4`

This artifact establishes a fresh passing implementation basis only. The later
project workflow must receive this review before regenerating the configured
exit gate under its persisted block policy.

## Recommended Next Step

Run `oat-project-review-receive` for this fresh final review, then regenerate
and receive the configured exit gate. Do not treat this review itself as a
fresh gate receipt.

## Blockers

None.
