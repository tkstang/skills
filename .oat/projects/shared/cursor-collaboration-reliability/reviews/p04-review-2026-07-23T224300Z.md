---
oat_generated: true
oat_generated_at: 2026-07-23T22:43:00Z
oat_review_scope: p04
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: p04

**Reviewed:** 2026-07-23T22:43:00Z
**Scope:** Whole Phase 4, tasks p04-t01 through p04-t06, plus the authorized append-only integration fixture fix
**Files reviewed:** 18 authored runtime, canonical TypeScript, generated-runtime, declaration, and focused test files
**Commits:** `22a7510f5c45004597f2573ead037b4279dc6bbe`, `4ceb7eeaaaa662c4c102cfcabd1c55fcb753ca27`, `8e64ff411612d9849f32dc48f94db369e5adb963`, `dff5f92ee27abe5e060325bc02d54613a2fff491`, `ce7f1f674f3fa671c542d1ae3f65777e39e4505e`, `e6e4c5decdc41d21d35a96de9d949637c4b93919`, `f84dfd82e1ba7295152c629e9a7f481676e3c155`

## Summary

Phase 4 implements the intended Cursor-owner completion path, lease-v6 migration, private continuity, confirmed-completion selection, versioned wake provenance, suppression, and bounded safety matrix. However, the supported Codex-owner/Cursor-peer route still consumes the Cursor transcript through the legacy record-index digest path and can advance a frame-index lease without a continuity checkpoint, violating the P0 collaboration authority boundary.

Findings: 1 critical, 0 important, 0 medium, 0 minor

**Verdict:** BLOCKED

## Findings

### Critical

- **Codex-owner Cursor completion bypasses frame-index projection and private continuity CAS** (`skills/session-observer-collab/scripts/hooks/codex-stop.mjs:111`)
  - Issue: `defaultObserve()` always calls the legacy `buildDigest(runtime, path, { fromIndex })` path, and `validSelection()` never compares `selection.indexBase` with the lease's `peerIndexBase`. The trigger update then supplies only `peerCursor` (`skills/session-observer-collab/scripts/hooks/codex-stop.mjs:211`), while `validateCursorUpdate()` explicitly permits a Cursor update without `peerContinuity` whenever the current lease checkpoint is null (`skills/session-observer-collab/scripts/lib/runtime-adapter.mjs:166`). A direct production-path reproduction with a schema-v6 Codex-owner/Cursor-peer lease emitted a schema-v2 wake carrying `index_base="zero-based-jsonl-record-index"` even though the lease declared `zero-based-jsonl-frame-index`, advanced `peerCursor` from 0 to 3, spent one continuation, and left `peerContinuity: null`. This reinterprets parsed-record offsets as frame offsets and lets a Cursor continuation bypass confirmed-completion framing and append-only prefix authority.
  - Fix: Give `codex-stop.mjs` the same Cursor-peer observation and checkpoint path used by `cursor-stop.mjs`: scan physical frames, build the `confirmed-completion` schema-v2 projection, validate digest/index base against the lease, poll pending completion safely, and pass a verified frame checkpoint in both trigger and suppressed-range CAS updates. Tighten `validateCursorUpdate()` so any Cursor cursor advancement requires a matching non-null proposed checkpoint, including the first advance. Add an integrated `runCodexStopHook()` regression for a Codex owner with a Cursor peer that proves frame-index wake emission, checkpoint persistence, pending/failure/no-op suppression, and fail-closed continuity.
  - Requirement: FR3, FR4, FR8, NFR1, NFR2

### Important

None

### Medium

None

### Minor

None

## Requirements/Design Alignment

**Artifacts available:** `discovery.md`, `spec.md`, `design.md`, `plan.md`, `implementation.md`, and root `AGENTS.md`.

**Evidence sources used:** `spec.md`, `design.md`, `plan.md`, `implementation.md`, root `AGENTS.md`, all seven authoritative Phase 4 commits, current authored collaboration runtime, canonical `src/transcript/core/runtimes.ts`, committed generated runtime copies, focused tests, full repository tests, and a direct Codex-owner/Cursor-peer production-path reproduction. `discovery.md` was available but was not required as the primary requirements source for this spec-driven review. The pre-existing `project-log.md` working-tree modification was excluded and preserved.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| FR3 | partial | Cursor-owner completion uses confirmed terminal-success frames, but the Codex-owner/Cursor-peer route still accepts the legacy Cursor record projection. |
| FR4 | partial | Cursor-owner completion validates private append-only continuity; the Codex-owner route can perform its first Cursor advance without creating a continuity checkpoint. |
| FR8 | partial | Lease, cap, expiry, duplicate, disarm, no-op, synthetic, metadata, and Cursor-owner completion cases are covered, but the Codex-owner/Cursor-peer path violates schema/index-base and CAS authority invariants. |
| NFR1 | partial | Canonical containment and later-checkpoint verification fail closed, but the first Codex-owner Cursor advancement can bypass the frame/continuity boundary. |
| NFR2 | partial | Lease files are owner-only and retain structural data, but the missing private checkpoint on the affected route fails the required minimal continuity-state contract. |
| NFR3 | implemented for Phase 4 boundary | Legacy v1 wake parsing, v5 non-Cursor lease migration, Claude/Codex peer paths, Export generated parity, and non-Cursor suites remain compatible. |
| NFR5 | implemented | Waits, lease expiry, continuation/loop caps, polling, and no-op advancement remain finite and bounded. |

### Extra Work (not in declared requirements)

The append-only integration fixture correction in `f84dfd8` is a justified bounded adaptation: it makes three intended-valid v2 automatic-control fixtures include the now-required frame index base and changes no production behavior.

## Verification Commands

```bash
pnpm exec vitest run tests/session-observer-collab tests/transcript-core/runtimes.test.ts tests/transcript-core/cursor-analysis.test.ts
pnpm run test
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run smoke
git diff --check
```

Observed results:

- Focused Phase 4 and adjacent runtime suite: 11 files passed, 199 tests passed.
- Full suite: 117 files passed, 1 skipped; 1,402 tests passed, 1 skipped.
- Type-check, generated-output synchronization, repository validation, and smoke passed.
- A direct temporary-store reproduction of `arm(runtime=codex, peerRuntime=cursor)` followed by `runCodexStopHook()` produced the blocking mismatched record-index wake and frame-lease advancement described above.
- No source, test, OAT bookkeeping, project log, or pre-existing working-tree content was modified by this review.

## Recommended Next Step

Run `oat-project-review-receive` and convert the Critical finding into a Phase 4 fix task. Re-review Phase 4 after both owner runtimes use the same Cursor confirmed-completion/frame-continuity authority path.

**Reconnaissance:** not-attempted
