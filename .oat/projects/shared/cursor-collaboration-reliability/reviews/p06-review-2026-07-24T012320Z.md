---
oat_generated: true
oat_generated_at: 2026-07-24T01:23:20Z
oat_review_scope: p06
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: p06

**Reviewed:** 2026-07-24T01:23:20Z
**Scope:** Whole Phase 6, tasks p06-t01 through p06-t04; implementation commits
`0ef8e5d..3dac3a0` inclusive (diff base `7307adb`), with implementation
closeout/bookkeeping inspected through `7a05334`
**Files reviewed:** 11 changed files plus governing project, provider-contract,
user-install, and PJM artifacts
**Commits:** `0ef8e5d..3dac3a0` inclusive
**Verdict:** BLOCKED

## Summary

Phase 6 makes the correct narrow product decision: no stronger Cursor wake
adapter is promoted, `buffered-manual` remains selected, provider/version
claims are scoped to the measured Agent path, no prohibited infrastructure or
authority was added, and the full regression/release matrix passes. The
stronger-wake backlog may legitimately close without shipping an adapter
because FR10 and the item itself define completion as an evidence-backed
investigation.

The review is nevertheless blocked because the two load-bearing negative live
probes are not independently repeatable from the durable repository evidence.
Their command records omit the sanitized setup and prompts that caused the
tested hook/tool paths to execute, as well as the mechanism that enforced the
claimed process caps. The privacy validator can confirm that the prose is safe
to retain, but it cannot reproduce or validate those provider outcomes.

Findings: 0 critical, 1 important, 0 medium, 0 minor

## Findings

### Critical

None

### Important

- **The Stop and managed-subagent negative probes are not independently
  reproducible from the committed evidence**
  (`skills/session-observer-collab/references/runtime-cursor.md:257`)
  - Issue: The same-parent record says an isolated project hook used a
    ten-second hook timeout and the Agent process had a 90-second cap, but the
    only retained command ends with `"<sanitized-probe-prompt>"` and contains
    no process-timeout mechanism
    (`skills/session-observer-collab/references/runtime-cursor.md:259`). The
    managed record likewise ends with
    `"<sanitized-managed-probe-prompt>"` and omits the exact sanitized
    `hooks.json`, structural logger/follow-up hook, child definition, fixed
    markers, and 120-second cap mechanism
    (`skills/session-observer-collab/references/runtime-cursor.md:291`).
    Repository search confirms that these placeholder inputs exist nowhere
    else. A reviewer can inspect the stated outcome but cannot repeat the
    provider path or distinguish a hook-loading/configuration mistake from the
    claimed unavailable surface. The evidence validator passes because it
    checks privacy and the measured capability matrix, not the completeness or
    executability of these Phase 6 recipes.
  - Impact: The selected `buffered-manual` tier remains safe and honestly
    scoped, but FR9/FR10 and NFR6 require recorded, independently reviewable
    commands/outcomes and a repeatable sanitized probe. The stronger-wake
    backlog's capability-inventory and exact-probe-evidence criteria therefore
    are not yet fully evidenced, so its otherwise atomic closure cannot be
    accepted as complete.
  - Fix: Commit either a bounded sanitized probe fixture/script or complete
    executable recipes that create the temporary hook configuration, logger,
    fixed prompt/child definition, expected markers, and external process
    timeout; emit structural expected-versus-actual JSON without identities or
    prose. Add validation/tests that require both Stop and managed recipes,
    provider versions, bounds, and outcome rows, then rerun the measured
    provider path and evidence validator before re-closing the review.
  - Requirement: FR9, FR10, NFR5, NFR6;
    `BL-260713-stronger-cursor-collaboration` acceptance criteria 1 and 4

### Medium

None

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `spec.md`, `design.md`, `plan.md`,
`implementation.md`, `state.md`, the Phase 6 implementation diff, installed
Cursor/Agent command and hook-contract help, changed skill/reference/user docs
and tests, the archived stronger-wake backlog item and all PJM lifecycle
surfaces, canonical user/provider copies, generated-output status, and fresh
read-only structural verification.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| FR9 | partial | Evidence labels and non-claims are accurate, but the two Phase 6 provider probes cannot be independently repeated from their committed command records. |
| FR10 | partial | The tier decision and no-ship outcome are valid, ordered, bounded in intent, and non-blocking by design; the load-bearing negative live evidence needs an executable sanitized recipe. |
| NFR1 | implemented | Phase 6 added no adapter, daemon, scheduler, background LLM, credential, service, executor, or authority. The credentialed long-running private worker is correctly excluded. |
| NFR2 | implemented | The evidence scan passes and retained records contain structural/version/outcome data without raw identities, credentials, paths, or transcript prose. |
| NFR3 | implemented | Shipped guidance remains compatible with existing Cursor/Codex/Claude behavior; the full regression suite passes. |
| NFR4 | implemented | No runtime dependency or generated-runtime change was introduced. `session-observer-collab` is versioned at 1.0.7, generated outputs are clean, and user/provider dogfood is synchronized. |
| NFR5 | partial | The documented hook and process bounds are finite in intent, but the retained commands do not show the mechanism that enforced the 90/120-second process caps. Existing runtime waits and leases remain bounded. |
| NFR6 | partial | Provider/version, sanitized outcomes, tier labels, and automated fixtures exist; the stronger-wake live probe recipes are incomplete and therefore not reproducible. |

### Extra Work (not in declared requirements)

The two stale docs-contract assertions in
`tests/repo/docs-presence.test.ts` and `tests/repo/readme-scope.test.ts` were
bounded corrections required by the changed support claim. They strengthen
coverage for Cursor unavailability and `buffered-manual` selection. The
additional one-line regex formatting change is behavior-neutral.

## Backlog Disposition Assessment

- Closing `BL-260713-stronger-cursor-collaboration` without shipping a stronger
  adapter is semantically allowed. Its description requests an investigation,
  its implementation criterion is conditional on selecting a surface, and
  FR10 explicitly permits an evidence-backed non-claim with conservative
  fallback.
- The lifecycle mechanics are correct and atomic in `3dac3a0`: status/date were
  updated, the newest-first completion entry was added, the item was moved to
  `backlog/archived/`, the generated index was refreshed, and
  `current-state.md` was reconciled.
- The automated collaboration suite covers success, non-success, late output,
  duplicate/no-op suppression, restart/re-arm boundaries, disarm, cleanup, and
  the retained fallback contract.
- Acceptance is still blocked by the Important evidence finding above. Once
  the exact sanitized probe recipes and results are reproducible, the existing
  terminal backlog lifecycle can remain; no stronger adapter is required.

## Additional Scope Dispositions

- The measured provider claims match the current local tools: Cursor 3.11.13
  arm64 and Agent `2026.07.23-e383d2b`. Installed hook guidance confirms
  `followup_message` for `subagentStop`, not top-level `stop`.
- Current Agent help exposes no scheduler command. `agent worker --help`
  confirms an operator-managed token and a long-running worker/service
  boundary. No Cursor/Session Observer user cron entry or LaunchAgent was
  found. The inventory order and `unsupported` worker classification are
  defensible.
- Runtime reference, shipped skill, and user guide agree that Cursor
  top-level Stop and managed-subagent paths were unavailable on the measured
  Agent version, no existing scheduled callback was selected, and
  `buffered-manual` is the honest current tier.
- `session-observer-collab` 1.0.7 is synchronized between top-level and metadata
  versions and matches the canonical user install byte-for-byte. Claude and
  Copilot links resolve to that install; Cursor skill entries are absent and
  are not claimed. OAT reports 188/188 provider surfaces in sync.
- Phase 6 changes no canonical TypeScript, generated runtime, shipped runtime
  script, dependency manifest, or lockfile. `build:check` and the full suite
  pass.
- The pre-existing dirty `project-log.md` dispatch entry was preserved. This
  review created only this artifact and did not mutate lifecycle bookkeeping,
  code, docs, tests, backlog, user installs, or provider state.

## Verification Commands

```bash
cursor --version
agent --version
agent --help
agent worker --help
node scripts/validate-cursor-evidence.mjs --base-ref 7307adb
node scripts/probe-cursor-acceptance.mjs --runtime cursor --cwd "$PWD" --json
pnpm exec vitest run tests/session-observer-collab
pnpm exec vitest run tests/session-observer-collab tests/repo/docs-presence.test.ts tests/repo/readme-scope.test.ts
pnpm exec vitest run
pnpm run type-check
pnpm run build:check
pnpm run validate:skill-versions --base-ref 7307adb
pnpm run validate
pnpm run smoke
pnpm --dir documentation run docs:format:check
pnpm --dir documentation run build
pnpm exec commitlint --from 7307adb --to 3dac3a0 --verbose
oat status --scope all --json
```

### Verification Results

- Collaboration suite: 115/115 passed.
- Focused collaboration plus docs-contract tests: 158/158 passed.
- Full Vitest suite: 1,448 passed, 1 skipped, 0 failed.
- Fresh read-only Cursor acceptance: 19/19 passed; 4 live-validated and 15
  automated-only.
- Explicit Phase 6 evidence scan: 20 files passed.
- Current provider versions match the durable record.
- Changed-skill version validation, generated-output parity, type-check,
  repository validation, smoke, exact Phase 6 formatting, commit lint, docs
  formatting, the 34-page production docs build, user dogfood parity, and
  188/188 OAT provider synchronization all passed.
- No stronger provider wake was re-run because this review was explicitly
  prohibited from mutating provider state; the missing repeatable sanitized
  recipe is the reported finding.

## Recommended Next Step

Run `oat-project-review-receive` to add one bounded Phase 6 evidence-fix task.
After committing an executable sanitized probe recipe and its structural
results, rerun a whole-Phase 6 review; do not ship a stronger adapter unless a
future run actually proves the full bounded same-parent contract.
