---
oat_generated: true
oat_generated_at: 2026-07-24T03:11:30Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: final

**Reviewed:** 2026-07-24T03:11:30Z
**Scope:** Final lifecycle review round 2 of all project changes from
`8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773` through
`10e8ac433c3f8348c10ddf19037fd0e064c857f8`
**Files reviewed:** 120 changed paths
**Commits:** 116 commits
**Verdict:** BLOCKED

## Summary

The round-1 Important finding is resolved: independent shipped-runtime
reproduction proved that repairing a buffered partial Cursor frame advances the
observer cursor, clears persisted and in-memory `bufferedFromFrame`, and keeps
all later heartbeat and status `recordsBehind` values at zero. One adjacent
status-contract defect remains: the durable watch target retains its baseline
frame count, so a separate `watch-ctl status` process reports a stale
`transcriptRecords` value after successful Cursor advancement. All other final
verification, authority, privacy, evidence, generated-runtime, version,
dogfood, synchronization, and cleanup gates passed.

Findings: 0 critical, 0 important, 1 medium, 0 minor

## Prior Finding Disposition

- **Round-1 Important — cleared Cursor buffering remains persisted as
  pending:** Resolved by `15cc61f`. Both canonical state-copy sites call
  `cursorBufferedFromFrame`, which distinguishes an absent result value from an
  explicit `null`. The generated runtime is in sync, the new regression passes,
  and the independent reproduction observed 30 consecutive post-repair
  heartbeats with `bufferedFromFrame: null` and `recordsBehind: 0`.

## Findings

### Critical

None.

### Important

None.

### Medium

- **Cursor status retains the baseline transcript frame count**
  (`src/transcript/session-observer/lib/watch.ts:828`)
  - Issue: `persistCursorTarget` advances the durable Cursor
    `observationCursor`, buffer, continuity, and status fields but does not pass
    the result's `digest.range.totalFrames` into the watch-target CAS.
    `compareAndSetCursorWatchTarget` consequently reuses
    `current.recordCount` at `watch-state.ts:797`, even though the in-memory
    target is updated to the new total at `watch.ts:813`. `watch-ctl status`
    maps that durable count directly to `transcriptRecords` at
    `session-observer.ts:1715`. In the independent repaired-frame reproduction,
    the in-memory heartbeat correctly reported frame count 4 while the
    persisted target and a separate status process reported
    `observationCursor: 4` with `transcriptRecords: 3`. This contradicts the
    documented live transcript-drift enrichment and can mislead operators
    diagnosing an active Cursor watcher, although delivery, buffering, and
    records-behind accounting remain correct.
  - Fix: Add a validated current frame-count field to
    `CursorWatchTargetTransition`, pass
    `result.digest.range.totalFrames` whenever an observation result is
    persisted, and preserve the existing count only for result-less
    health/continuity transitions. Extend the repaired-frame regression to
    assert durable `recordCount: 4`, live status `transcriptRecords: 4`, and
    continued agreement with post-repair heartbeats. Rebuild the generated
    runtime and apply the required session-observer version/dogfood update.
  - Requirement: FR5

### Minor

None.

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `spec.md`, `design.md`, `plan.md`,
`implementation.md`, `state.md`, the round-1 review artifact, the authoritative
commit range, changed canonical and generated runtime code, tests, evidence
artifacts, archived backlog items, user-level skill installations, provider
mirrors, active Cursor roots, and the recorded probe quarantine.

### Requirements Coverage

| Requirement | Status      | Notes                                                                                                                                    |
| ----------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| FR1         | implemented | Direct corroborated identity, canonical containment, exact pins, and ambiguous-candidate failure were verified.                          |
| FR2         | implemented | Prefix-stable content observation, source keys, filtering, recoverability, and pending-vs-complete labeling are covered.                 |
| FR3         | implemented | Terminal reconciliation and successful-completion-only continuation remain separate from content visibility.                             |
| FR4         | implemented | Physical frame accounting, prefix continuity, fail-visible replacement/shrink handling, and repaired-frame resumption were verified.     |
| FR5         | partial     | Buffering and records-behind now clear correctly, but `watch-ctl status` exposes a stale durable `transcriptRecords` count after growth. |
| FR6         | implemented | Owner-only state, atomic contender publication, CAS reservation/commit, restart, and uncertain-delivery behavior were verified.          |
| FR7         | implemented | Baseline, debounce, repair, pin retention, heartbeat, controls, and cleanup behavior are covered; the round-1 buffer defect is resolved. |
| FR8         | implemented | Completion-sensitive private cursors, exact leases, finite budgets, authority boundaries, and fail-closed adapters were verified.        |
| FR9         | implemented | Supported coverage remains fixture-, validator-, and sanitized-evidence-gated, with explicit non-claims for unsupported surfaces.        |
| FR10        | implemented | Bounded probes rejected unavailable stronger surfaces and retained `buffered-manual` without adding a privileged adapter.                |
| NFR1        | implemented | State remains owner-only and observation adds no daemon, scheduler, credential, or widened provider authority.                           |
| NFR2        | implemented | Durable state/evidence is structural and metadata-only; evidence validation rejects prose and unsafe paths.                              |
| NFR3        | implemented | Existing runtime, Export, collaboration, documentation, and provider regression gates passed.                                            |
| NFR4        | implemented | Shipped runtime remains dependency-free, generated output is synchronized, and skill versions pass enforcement.                          |
| NFR5        | implemented | Scanning and timing remain bounded; performance and deterministic watch coverage passed.                                                 |
| NFR6        | implemented | Claims remain tied to named evidence and cleanup, and live unavailability is not promoted into support.                                  |

### Extra Work (not in declared requirements)

None. The bounded round-1 fix, its version bump, generated output, regression,
dogfood propagation, and lifecycle bookkeeping directly address final review
and release-readiness requirements.

## Integrated Risk Assessment

- **Observation and continuity:** Exact stateful identity, canonical
  containment, physical frame indexes, prefix checkpoints, and explicit
  recovery continue to prevent silent inheritance, skips, or replay across
  weak aliases and transcript mutation.
- **Concurrency and delivery:** Owner-only state, atomic contender
  publication, generation-aware stale reclaim, pending-delivery reservation,
  and CAS commit/recovery cover the principal duplicate, loss, and crash-window
  risks.
- **Completion authority:** Collaboration remains terminal-success-only with
  exact leases, finite budgets, no-op/control suppression, and
  provider-supported synthetic envelopes. No stronger wake path was promoted
  without evidence.
- **Privacy and provider state:** Evidence validation passed for all 34 records;
  active Cursor `projects` and `chats` roots contain no probe residuals; the
  nine quarantined directories remain outside active roots with no symlinks.
- **Generated/runtime and dogfood:** Canonical TypeScript and shipped runtime
  are in sync. Session Observer is version 1.0.10 in both version fields and is
  byte-identical to its user-level installation; the other changed skills are
  also byte-identical and correctly mirrored.
- **Synchronization:** All 188 project/user provider views and all 114
  user-scoped views report in sync with zero drift, missing entries, or strays.
- **Residual blocker:** A separate status process reads the stale durable
  Cursor frame count even after the live watcher has advanced, so final
  zero-finding acceptance is not yet met.

## Deferred Findings

None. Earlier phase findings and the round-1 final Important finding are
resolved. No accepted Medium-severity ledger exists, and the two unrelated
future collaboration backlog items remain outside this project's declared
scope.

## Verification Commands

The following commands were run from the repository root:

```bash
pnpm run build
pnpm run type-check
pnpm run build:check
pnpm run test
pnpm exec vitest run tests/session-observer/watch.test.ts
pnpm run validate
pnpm run smoke
node scripts/validate-cursor-evidence.mjs --base-ref 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773
pnpm exec vitest run tests/tooling/cursor-evidence-validation.test.ts tests/tooling/cursor-acceptance-probe.test.ts tests/tooling/cursor-wake-probe.test.ts
pnpm --dir documentation run docs:format:check
pnpm --dir documentation run build
node scripts/validate-skill-versions.mjs --base-ref 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773
pnpm run validate:internal-flags
oat status --scope all --json
oat status --scope user --json
git diff --check 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773..HEAD
```

Results: build, type checking, generated parity, repository validation, smoke,
38/38 focused watch tests, 57/57 evidence/probe tests, the 34-file evidence
validator, documentation formatting, the 34-page static build, skill-version
enforcement, internal flags, and changed-file lint/format all passed. The
machine-readable full run passed 238/238 suites and 1,465/1,466 tests, with one
intentional skip and zero failures. `git diff --check` reported only the
intentional blank-line-at-EOF Cursor framing fixture documented by the
implementation contract.

The repaired-frame reproduction ran against the shipped generated runtime in a
temporary Cursor store/state root and removed it afterward. It confirmed the
round-1 fix and independently exposed the durable frame-count mismatch described
above without modifying repository or provider state.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert the Medium finding into a
bounded final-review fix task. After persisting the updated frame count, add the
status-count assertion, rebuild/bump/dogfood Session Observer, rerun the focused
watch and final verification matrices, and perform final lifecycle review round 3.
