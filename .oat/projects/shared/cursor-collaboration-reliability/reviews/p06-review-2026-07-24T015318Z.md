---
oat_generated: true
oat_generated_at: 2026-07-24T01:53:18Z
oat_review_scope: p06
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: p06 Round 2

**Reviewed:** 2026-07-24T01:53:18Z
**Scope:** Whole Phase 6, tasks p06-t01 through p06-t04; original implementation
and closeout from diff base `7307adb`, review fix `2cddafe`, and lifecycle
bookkeeping inspected through `98200f4`
**Files reviewed:** 18 changed files plus governing project, provider-contract,
user-install, runtime-state, and PJM artifacts
**Commits:** `0ef8e5d..2cddafe` implementation/fix; bookkeeping through `98200f4`
**Verdict:** BLOCKED
**Reconnaissance:** not-attempted

## Summary

The round-1 reproducibility finding is substantively resolved. The repository
now contains an executable sanitized dual-mode recipe with exact fixed prompts,
hooks configuration, structural hook script, managed child definition, markers,
independent 90/120-second process timers, termination and kill fallback,
expected-versus-actual JSON, privacy scanning, timeout/cleanup tests, and
validator enforcement. The recorded live results remain honest: both the
top-level Stop and managed-subagent callback surfaces are unavailable, no
stronger adapter is shipped, and `buffered-manual` remains the strongest
evidence-backed tier.

The review remains blocked because the live recipe's safety and cleanup claims
are false for the actual Cursor Agent process. The fix's own live runs created
persistent provider-owned project directories, transcripts, logs, and auth
metadata under `~/.cursor/projects/` outside the disposable workspace. The
recipe removes only the `/tmp` workspace and nevertheless reports
`temporaryWorkspaceOnly: true`; tests and the evidence validator assert that
literal without exercising or detecting the provider's external writes.

Findings: 0 critical, 1 important, 0 medium, 0 minor

## Prior Finding Disposition

| Prior finding | Disposition | Evidence |
| --- | --- | --- |
| Stop and managed-subagent negative probes were not independently reproducible | Resolved | `--describe --json` exposes both exact recipes with no placeholders; the implementation supplies fixed hooks/config/prompts/child/markers, real external 90/120-second caps, process-group TERM/KILL fallback, structural results, privacy checks, and cleanup reporting. Probe/validator tests pass 31/31 and the validator passes 22 files. |

The residual finding below is distinct: the recipe is now executable, but its
declared isolation boundary and cleanup result do not cover state written by
the real provider.

## Findings

### Critical

None

### Important

- **The live probe leaves provider transcripts and metadata outside the
  declared disposable boundary**
  (`scripts/probe-cursor-wake-surfaces.mjs:201`)
  - Issue: The recipe declares `temporaryWorkspaceOnly: true` and
    `cleanup: "remove-exact-created-workspace"`
    (`scripts/probe-cursor-wake-surfaces.mjs:211`), invokes the real authenticated
    Cursor Agent with the ordinary process environment
    (`scripts/probe-cursor-wake-surfaces.mjs:467`), and cleans only the direct
    temporary workspace it created
    (`scripts/probe-cursor-wake-surfaces.mjs:248`,
    `scripts/probe-cursor-wake-surfaces.mjs:605`). Read-only inspection found
    four persistent directories from the fix's two live runs:
    `~/.cursor/projects/{private-,}var-folders-...-cursor-wake-probe-top-level-stop-ri8Oa8`
    and
    `~/.cursor/projects/{private-,}var-folders-...-cursor-wake-probe-managed-subagent-dY3yL2`.
    Together they contain three agent-transcript JSONL files, two `worker.log`
    files, two `mcp-auth.json` files, and repository metadata. Their creation
    times (20:37:13–20:37:31 local) precede the 20:44:42 fix commit and match
    the recorded fix-run workspaces. The retained top-level evidence says no
    live transcript was mutated
    (`skills/session-observer-collab/references/runtime-cursor.md:350`), while
    its cleanup JSON reports success solely because the `/tmp` workspace is
    gone. The tests use a fake provider that writes only inside the supplied
    temporary parent (`tests/tooling/cursor-wake-probe.test.ts:218`), and the
    validator checks the safety declaration as a literal
    (`scripts/validate-cursor-evidence.mjs:342`) rather than verifying the
    execution boundary.
  - Impact: A reviewer following the committed live commands creates durable
    provider state outside the advertised sandbox. That violates the review's
    non-mutation condition, makes the cleanup result incomplete, and leaves
    retained transcript/log/auth metadata unreported. The negative provider
    outcomes themselves remain conservatively classified, but the public
    safety/evidence claim is not independently honest under NFR1, NFR2, and
    NFR6.
  - Fix: Either run Cursor Agent with a proven disposable provider-state root
    and remove every exact probe-created local artifact, or change the recipe
    and documentation to disclose the provider-side writes and require
    explicit operator authorization. Add a real-boundary regression that
    models provider writes outside `--workspace`, fail cleanup unless every
    declared disposable artifact is absent, and make the evidence validator
    enforce the implemented isolation/cleanup contract rather than a
    self-reported boolean. Do not delete pre-existing provider state by broad
    pattern; resolve and prove ownership of each exact artifact before any
    cleanup.
  - Requirement: NFR1, NFR2, NFR6; FR9 repeatable structural live-probe
    evidence; `BL-260713-stronger-cursor-collaboration` exact evidence and
    cleanup claims

### Medium

None

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `spec.md`, `design.md`, `plan.md`,
`implementation.md`, `state.md`, the complete Phase 6 diff and review fix,
executable probe and validator source/tests, changed skill/runtime/user docs,
the archived stronger-wake backlog item and PJM lifecycle surfaces, canonical
user/provider copies, current provider-created probe paths, and fresh
non-mutating verification.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| FR9 | partial | The exact dual-mode commands and structural outcomes are now reproducible, but the real provider path writes durable state outside its declared isolated workspace. |
| FR10 | implemented | The evidence-backed no-ship decision is correct: top-level Stop and managed callbacks remain unavailable, no scheduled surface exists, the private worker exceeds authority, and buffered-manual is retained. |
| NFR1 | partial | Phase 6 ships no adapter, daemon, scheduler, credential, service, or widened runtime authority, but the live probe understates its provider-state mutation boundary. |
| NFR2 | partial | Durable repository evidence remains sanitized, but live runs retain unreported provider transcripts/log/auth metadata outside the disposable workspace. |
| NFR3 | implemented | Existing Cursor/Codex/Claude behavior remains compatible and the full regression suite passes. |
| NFR4 | implemented | No runtime dependency was added. `session-observer-collab` top-level and metadata versions are 1.0.8, skill-version validation passes, and canonical user dogfood matches byte-for-byte. |
| NFR5 | implemented | The provider process has real 90/120-second external caps, hooks have ten-second/one-loop bounds, timeout tests exercise termination, and existing waits/leases remain bounded. |
| NFR6 | partial | Exact recipes, versions, structural expected/actual rows, support labels, and validator rules exist; the live isolation/cleanup claim is not reproducible or true. |

## Original Phase 6 Re-review

- The non-ship decision remains correct. No callback was promoted from
  configuration presence, no unavailable surface was selected, and no stronger
  Cursor wake adapter was implemented.
- `buffered-manual` is consistently selected across the shipped skill, Cursor
  runtime reference, and user guide. No document overstates autonomous wake.
- Authority boundaries remain intact for product behavior: no new credential,
  daemon, scheduler, recurring LLM, external service, worker, or background
  executor was added. The private cloud worker remains correctly
  `unsupported`.
- Recorded current live results are structurally explicit. Top-level Stop:
  Agent `2026.07.23-e383d2b` exited normally within 90 seconds, the initial
  marker appeared, zero Stop hooks/follow-ups appeared, and the outcome is
  `callback-unavailable`. Managed-subagent: the Agent exited normally within
  120 seconds, the parent marker appeared, the child/follow-up markers and both
  lifecycle hooks did not, and the outcome is `callback-unavailable`.
- The managed result no longer claims native child execution merely because
  the parent marker appeared. Identity, one-consumer delivery, interruption,
  recurrence, restart, late-output, disarm, and suppression remain unpromoted
  where the callback prerequisite was unavailable.
- Versioning and dogfood are reconciled at 1.0.8. The repository skill and
  canonical `~/.agents` copy match, the Claude link resolves to the canonical
  copy, no Cursor user-skill entry is present or claimed, and dry-run sync
  reports 114/114 user plus 74/74 project operations already synchronized.
- The changed docs/skill evidence and support labels are aligned. Generated
  output parity, repository validation, type-check, smoke, exact changed-file
  formatting/lint, docs formatting/build, and the full regression suite pass.

## Backlog Disposition Assessment

- Closing `BL-260713-stronger-cursor-collaboration` without an adapter remains
  semantically valid: its implementation criterion is conditional on selecting
  a surface, and FR10 explicitly permits an evidence-backed non-claim with
  conservative fallback.
- Lifecycle mutation in `3dac3a0` is mechanically complete and atomic: the
  item was marked closed, moved to `backlog/archived/`, added to
  `completed.md`, removed from the open index, and reconciled in
  `current-state.md`.
- The automated adapter suite covers success, non-success/late output,
  duplicate/no-op suppression, restart/re-arm boundaries, disarm, cleanup, and
  buffered-manual fallback.
- Acceptance cannot yet be endorsed because the exact live-evidence recipe
  misstates its isolation and cleanup boundary. The terminal lifecycle may
  remain structurally closed during a bounded fix, but the safety/evidence
  claim must be corrected before Phase 6 passes review.

## Verification Commands

```bash
node scripts/probe-cursor-wake-surfaces.mjs --describe --json
node scripts/validate-cursor-evidence.mjs --base-ref 7307adb
pnpm exec vitest run tests/tooling/cursor-wake-probe.test.ts tests/tooling/cursor-evidence-validation.test.ts tests/session-observer-collab
pnpm run test
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run smoke
pnpm run validate:skill-versions --base-ref 7307adb
pnpm run validate:internal-flags
pnpm --dir documentation run docs:format:check
pnpm --dir documentation run build
pnpm exec oxfmt --check <changed Phase 6 source/docs/tests>
pnpm exec oxlint <changed Phase 6 source/tests>
oat sync --scope user --dry-run --json
oat sync --scope project --dry-run --json
find ~/.cursor/projects -maxdepth 1 -type d -iname '*cursor-wake-probe*'
```

### Verification Results

- `--describe`: both exact modes present with fixed prompts, hooks/config,
  child/markers, 90/120-second process caps, ten-second hook timeouts,
  one-loop caps, and no placeholder prompts.
- Probe/validator plus collaboration suites: 146/146 passed (31 probe/validator,
  115 collaboration).
- Explicit Phase 6 evidence validator: 22 files passed.
- Full Vitest suite: 1,457 passed, 1 skipped, 0 failed.
- Type-check, generated-output parity, repository validation, smoke, internal
  flags, one changed-skill version check against `7307adb`, exact changed-file
  formatting/lint, docs formatting, and the 34-page production docs build
  passed.
- Dogfood/provider dry run: repository and canonical user skill match at 1.0.8;
  114/114 user and 74/74 project sync operations are already synchronized.
- Real live modes were not rerun. Read-only preflight proved that prior
  invocations wrote provider state outside the disposable workspace, so a
  rerun would violate this review's non-mutation condition. The durable
  structural results from the fix were inspected and are reported above.
- The pre-existing dirty `project-log.md` entry was preserved. This review
  created only this artifact and did not alter lifecycle bookkeeping, code,
  docs, tests, backlog, user installs, provider state, or the residual probe
  artifacts.

## Recommended Next Step

Run `oat-project-review-receive` for one bounded Phase 6 fix that makes the live
probe's provider-state boundary truthful and validator-backed. Re-run whole
Phase 6 only after the recipe can either prove disposable isolation/complete
exact cleanup or explicitly disclose and require authority for every external
provider write.
