---
oat_generated: true
oat_generated_at: 2026-07-24T02:51:00Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/cursor-collaboration-reliability
---

# Code Review: final

**Reviewed:** 2026-07-24T02:51:00Z
**Scope:** Final lifecycle review of all project changes from
`8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773` through
`c21bbec1f3890f87a11350b1c1d2ac06e6cf0afd`
**Files reviewed:** 119 changed paths
**Commits:** 113 commits
**Verdict:** BLOCKED

## Summary

The implementation substantially satisfies the exact-identity, continuity,
delivery-accounting, completion-authority, evidence, privacy, generated-runtime,
versioning, dogfood, and provider-cleanup contracts. One reproducible watch-state
defect remains: after a previously buffered Cursor frame is repaired and
successfully consumed, the watcher discards the digest's explicit `null` clear
and continues reporting that frame as buffered and behind. This is an actionable
P0 watch/status truthfulness regression, so the final lifecycle review is
blocked.

Findings: 0 critical, 1 important, 0 medium, 0 minor

## Review Orchestration

| Wave | Task class        | Classification rationale                                                                                                     | Selected target                   | Acceptance / outcome                                                                                                                                                                   | Floor satisfaction                                                        | Fallback        |
| ---- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | --------------- |
| R1   | intelligent-recon | Observer/state/runtime and collaboration/provider evidence required semantic cross-file checks where misses could be silent. | `explorer` (`gpt-5.6-luna`, high) | Initial full-history route was rejected before launch; corrected launch then failed because the agent thread limit was reached. No worker was accepted and no worker claims were used. | Requested target met the declared class floor; execution was unavailable. | `caller-inline` |

**Primary reconciliation:** The primary reviewer directly covered both intended
lanes, reopened the authoritative sources, inspected the load-bearing observer,
state, watch, lease, hook, evidence, and provider-boundary code, and independently
ran the full verification matrix. There were no worker observations to accept or
reject.

## Findings

### Critical

None.

### Important

- **Cleared Cursor buffering remains persisted as pending**
  (`src/transcript/session-observer/lib/watch.ts:800`)
  - Issue: `applyCursorTargetState` uses nullish fallback when copying
    `result.digest.cursorEvidence.bufferedFromFrame`, and
    `persistCursorTarget` repeats the same operation at line 823. The digest
    intentionally uses `null` to mean that buffering has cleared, but `??`
    treats that explicit value as absent and retains the target's prior numeric
    frame. An exact runtime reproduction started a watch at frame 3, appended an
    unterminated frame so `bufferedFromFrame` became 3, then repaired the frame.
    Observation advanced and committed through frame 4 with digest
    `bufferedFromFrame: null`, while the persisted target remained at 3 and
    subsequent heartbeats repeatedly reported `recordsBehind: 1`. Future
    heartbeats can therefore present successfully consumed content as still
    buffered/behind.
  - Fix: Preserve the result's exact `bufferedFromFrame` whenever a result is
    present, including explicit `null`, in both the CAS payload and in-memory
    target update. Add a Cursor watch integration test that establishes a
    partial or stability buffer, repairs/confirms the frame, and asserts the
    persisted watch target and heartbeat/status output clear
    `bufferedFromFrame` and return `recordsBehind` to zero.
  - Requirement: FR5, FR7

### Medium

None.

### Minor

None.

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `spec.md`, `design.md`, `plan.md`,
`implementation.md`, the authoritative commit range, the changed source and
generated runtime, the changed tests and evidence artifacts, the archived
backlog items, the user-level skill installation, provider mirrors, active
Cursor roots, and the recorded probe quarantine.

### Requirements Coverage

| Requirement | Status      | Notes                                                                                                                                    |
| ----------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| FR1         | implemented | Direct corroborated identity, canonical path containment, exact pins, and ambiguous-candidate failure were verified.                     |
| FR2         | implemented | Prefix-stable content observation, source keys, filtering, recoverability, and pending-vs-complete labeling are implemented and covered. |
| FR3         | implemented | Terminal reconciliation and successful-completion-only continuation remain separate from content visibility.                             |
| FR4         | implemented | Physical frame accounting, prefix continuity, fail-visible replacement/shrink handling, and repaired-frame resumption were verified.     |
| FR5         | partial     | Independent health facets exist, but the finding above makes buffering and records-behind untruthful after a buffered frame clears.      |
| FR6         | implemented | Owner-only state, hard-link contender publication, CAS reservation/commit, restart, and uncertain-delivery behavior were verified.       |
| FR7         | partial     | Foreground watch behavior is broadly implemented, but repaired buffered frames leave stale watch status as described above.              |
| FR8         | implemented | Completion-sensitive private cursors, exact leases, finite budgets, owner/peer authority, and fail-closed adapters were verified.        |
| FR9         | implemented | Supported coverage is fixture-, validator-, and sanitized-evidence-gated; unsupported surfaces remain explicit non-claims.               |
| FR10        | implemented | Bounded probes rejected unavailable stronger surfaces and retained `buffered-manual` without shipping a new privileged adapter.          |
| NFR1        | implemented | State remains owner-only and observation does not widen provider authority or introduce a daemon/scheduler.                              |
| NFR2        | implemented | Durable state/evidence is structural and metadata-only; evidence validation rejects transcript prose and unsafe paths.                   |
| NFR3        | implemented | Full regression, validation, smoke, docs, and focused provider-boundary suites passed across existing runtimes.                          |
| NFR4        | implemented | Shipped runtime remains dependency-free and generated output is synchronized from canonical source.                                      |
| NFR5        | implemented | Scanning is incremental/bounded, tests include performance and deterministic timing coverage, and the full suite passed.                 |
| NFR6        | implemented | Claims are tied to named evidence and cleanup; live unavailability is not promoted into support.                                         |

### Extra Work (not in declared requirements)

None. The generated-runtime plumbing, evidence validator, dogfood propagation,
and backlog closure changes are direct implementation or closeout work for the
declared requirements. The stronger-wake investigation correctly ended in an
evidence-backed non-selection rather than expanding scope.

## Integrated Risk Assessment

- **Identity and continuity:** Exact stateful identity, canonical containment,
  prefix checkpoints, physical frame indexes, and explicit recovery prevent
  silent state inheritance across weak aliases, path changes, transcript
  mutation, and legacy unverified state.
- **Concurrency and delivery:** Owner-only state, atomic contender publication,
  expected-generation stale reclaim, pending-delivery reservation, and CAS
  commit/recovery cover the principal duplicate, loss, and crash-window risks.
- **Authority and privacy:** Automatic continuation remains terminal-success
  only; Cursor and Codex owners use finite leases, no-op suppression, bounded
  loops, and provider-supported synthetic envelopes. No credentials, prose,
  daemon, background LLM poller, or unsupported provider control surface was
  added.
- **Provider and release state:** The three changed skills have synchronized
  top-level and metadata versions, byte-identical user dogfood copies, correct
  Claude symlinks, and no falsely claimed Cursor mirror. All 188 OAT provider
  views report in sync.
- **Cleanup:** The active Cursor `projects` and `chats` roots contain no
  `cursor-wake-probe` residuals. The nine recorded residual directories are
  quarantined outside active roots with no symlinks back into them.
- **Residual blocker:** The stale buffering clear affects the public foreground
  watch/status contract and must be fixed and regression-tested before final
  lifecycle acceptance.

## Deferred Findings

None. Earlier phase-review findings were resolved in the reviewed range, and no
accepted Medium-severity ledger remains. The two unrelated, independently
tracked future collaboration backlog items do not block this project's declared
scope.

## Verification Commands

The following commands were run from the repository root:

```bash
pnpm run build
pnpm run type-check
pnpm run build:check
pnpm run test
pnpm run validate
pnpm run smoke
node scripts/validate-cursor-evidence.mjs --base-ref 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773
pnpm exec vitest run tests/tooling/cursor-evidence-validation.test.ts tests/tooling/cursor-acceptance-probe.test.ts tests/tooling/cursor-wake-probe.test.ts
pnpm --dir documentation run docs:format:check
pnpm --dir documentation run build
node scripts/validate-skill-versions.mjs --base-ref 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773
pnpm run validate:internal-flags
git diff --check 8c006a5abd8e2e5e97cfa1f26d81cc82ac15b773..c21bbec1f3890f87a11350b1c1d2ac06e6cf0afd
```

Results: generated output, type checking, repository validation, smoke,
documentation, skill versions, internal flags, changed-file lint/format checks,
and focused evidence/probe tests passed. The full Vitest run passed 120 files
(one skipped), with 1,464 tests passed and one skipped. `git diff --check`
reported only the intentional blank-line-at-EOF Cursor framing fixture documented
by the implementation contract.

The watch finding was independently reproduced against the shipped generated
runtime with a temporary Cursor store and state root. The reproduction left the
repository unchanged and was removed afterward.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert the Important finding into
a plan task. After fixing both null-preservation sites and adding the repaired
buffer watch regression, rerun the focused watch suite, generated-output check,
full test/validation matrix, and final lifecycle review.
