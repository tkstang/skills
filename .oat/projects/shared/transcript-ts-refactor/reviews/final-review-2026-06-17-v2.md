---
oat_generated: true
oat_generated_at: 2026-06-17
oat_review_scope: final
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/transcript-ts-refactor
---

# Code Review: final

**Reviewed:** 2026-06-17
**Scope:** final (`109571812005d5851f9a26c2f7ec40ff2e880253..HEAD`, all 8 tasks p01-t01 → p03-t02)
**Files reviewed:** 48 changed files (focused scrutiny on canonical TS source, generated `.mjs`, build script, tests, docs)
**Commits:** 25

## Summary

This independent final review verifies the transcript-core and
export-session-transcript migration to canonical TypeScript source under
`src/transcript/` with committed, banner-stamped generated `.mjs` runtime output
produced by `scripts/build-generated.mjs`. Every discovery success criterion is
met, the #1 import-rewrite risk is correctly handled (generated CLI imports
shipped local `./lib/*.mjs`, never `src/` specifiers) and asserted by tests, and
all six required verification commands pass (build:check, type-check, test,
validate, smoke, plus the sync-wrapper `--check`). The one deferred Minor from
the prior archived final review — `structure.md` wording that credited the sync
script — is now FIXED. No Critical, Important, or Medium findings; one new Minor
(observational) is recorded below.

## Findings

### Critical

None

### Important

None

### Medium

None

### Minor

- **`runtime: both` watcher fix lightly stretches the documented Out-of-Scope boundary** (`skills/session-observer/scripts/lib/watch.mjs:520-880`)
  - Issue: Discovery Out-of-Scope says "Do not migrate session-observer tests
    except for changes required to keep the generated transcript-core copy
    green." The shipped commit `ea2495a fix(session-observer): preserve pending
    runtime both updates` goes a step beyond a migration-compat change: it adds
    real runtime behavior (`enqueueRecordsAppendedDuringBaseline`,
    `flushPendingBeforeMaxRuntime`) to fix a pending-update race surfaced by the
    full `pnpm test` gate. This is defensible — it was needed to land a green
    final verification, is bounded to the watcher loop, and ships with 115 lines
    of new regression coverage in `tests/session-observer/watch.test.mjs`
    (baseline-append enqueue + max-runtime flush, plus the existing
    max-pending/debounce-starvation cases). But it is not literally a
    "transcript-core copy green" change, so the implementation's own Out-of-Scope
    framing is slightly stale relative to what shipped.
  - Fix: Artifact-alignment only (no code change). Add one line to
    `implementation.md` → "Deviations from Plan / Design" (currently empty)
    recording that a small session-observer runtime fix was accepted under final
    verification to unblock the full test gate, with `watch.mjs` as source of
    truth and the new watcher tests as the guard. The shipped code is correct
    and well-tested; only the deviation ledger is silent on it.

## Deferred Findings Disposition

- **Prior final review Minor — "Generated knowledge still credits the sync
  script as the maintainer" (`.oat/repo/knowledge/structure.md:347`)**:
  **FIXED.** Line 347 now reads "Single source of truth maintained by
  `scripts/build-generated.mjs` and drift guards in tests." The surrounding
  Transcript-Core Module block (lines 334-348) correctly lists
  `src/transcript/core/runtimes.ts` as canonical and frames
  `scripts/sync-transcript-core.mjs` as a compatibility wrapper that delegates
  to `build-generated.mjs`. No residual wording credits the sync script as the
  maintenance path. No action required.
- **Archived phase reviews (p01/p02/p03) carry-forward debt**: None. p01 and
  p02 archived reviews recorded 0/0/0/0. p03 recorded only the
  structure.md wording Minor (now resolved above). The p02 Important finding
  about generated-output formatting drift was already fixed in-range
  (`fix(p02): protect export generated outputs from formatting drift`,
  `--list-outputs` guard + static ignores + CI/hook filters + regression tests).

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `plan.md`, `implementation.md`,
`summary.md`, archived phase/final reviews. (Quick mode — `spec.md`/`design.md`
not present and not required.)

### Requirements Coverage (discovery Success Criteria)

| Success Criterion | Status | Notes |
| --- | --- | --- |
| Transcript-core canonical TS under `src/transcript/core/` | implemented | `src/transcript/core/runtimes.ts` with explicit public types; type-check clean |
| Export-session canonical TS under `src/transcript/export-session/` | implemented | `export-session-transcript.ts` + `sanitize.ts` present and typed |
| Generated `.mjs` committed with build banner | implemented | All 4 outputs carry `// GENERATED by scripts/build-generated.mjs...` + `// Source:` line |
| `build:check` catches drift for transcript-core and export | implemented | `--check` reports all 6 mappings "in sync"; mutation test proves stale detection + restore |
| Export CLI behavior and exit codes unchanged | implemented | cli.test.ts covers selection precedence, default paths, not-a-git fallback, `--all`, sanitization, `--help`, exit 0/1/2/3 |
| Session-observer works with generated transcript-core copy | implemented | `skills/session-observer/scripts/lib/runtimes.mjs` regenerated, banner present; watch.test.mjs (24 tests) green |
| No second source of truth at `shared/transcript-core/runtimes.mjs` | implemented | File deleted; `shared/transcript-core/README.md` is now a pointer to `src/transcript/core/runtimes.ts` |
| Docs/AGENTS/reference no longer describe old sync contract as canonical | implemented | README/AGENTS have no stale canonical ref; structure.md, shared README, DR-014 (superseded), DR-020/021 updated |
| Import rewrites: generated CLI imports `./lib/runtimes.mjs` + `./lib/sanitize.mjs` | implemented | Generated CLI imports verified; test asserts presence AND absence of `../core/runtimes.js` / `./sanitize.js` |

### Out-of-Scope Respected

| Constraint | Status | Notes |
| --- | --- | --- |
| Do not migrate session-observer impl modules to TS | respected | `watch.mjs` remains hand-maintained `.mjs` (not migrated to TS); runtimes.mjs is the only generated copy there |
| Do not migrate session-observer test suites to Vitest | respected | `tests/session-observer/watch.test.mjs` stays on the Node test runner |
| Retain Node test runner | respected | 204 Node tests still run alongside 339 Vitest tests |
| No runtime deps added to shipped skills | respected | Shipped `.mjs` import only `node:` stdlib + local `./lib/*` files; esbuild/tsc/vitest are dev-only |
| Do not implement consensus-evaluate | respected | No consensus-evaluate code in range |

### Extra Work (not in declared requirements)

- `skills/session-observer/scripts/lib/watch.mjs` runtime fix
  (`enqueueRecordsAppendedDuringBaseline` + `flushPendingBeforeMaxRuntime`).
  Defensible and well-tested; the only nuance is that it slightly exceeds the
  literal "keep the generated copy green" Out-of-Scope wording. Captured as the
  single Minor above (artifact-alignment, not a defect).

## Verification Commands

All run during this review (read-only; no files modified other than this
artifact). Results recorded:

```bash
pnpm run build:check        # PASS — all 6 mappings "in sync", no pending
pnpm run type-check         # PASS — tsc --noEmit, no errors
node scripts/sync-transcript-core.mjs --check  # PASS — wrapper delegates, all in sync
pnpm run test               # PASS — 204 Node tests + 339 Vitest tests (26 files), 0 fail
pnpm run validate           # PASS — "validation passed"
pnpm run smoke              # PASS — "smoke passed"
```

Additional targeted checks confirmed:

- Generated CLI head shows shebang → `// GENERATED` banner → `// Source:` line,
  then `import ... from './lib/runtimes.mjs'` and `from './lib/sanitize.mjs'`.
- No shipped `.mjs` imports a non-`node:` package or a `src/` specifier.
- `shared/transcript-core/runtimes.mjs` is absent; `tests/transcript-core/sync.test.mjs` deleted on disk.
- Git working tree clean after the full verification run (no generated drift).

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks.
The single Minor is an artifact-alignment item (record the session-observer
watcher fix in `implementation.md`'s Deviations ledger); it is non-blocking and
the project is otherwise ready for closeout.
