---
oat_generated: true
oat_generated_at: 2026-05-04
oat_review_scope: p02
oat_review_type: code
oat_project: .oat/projects/shared/consensus-plugin
---

# Code Review: p02

**Reviewed:** 2026-05-04
**Scope:** Phase p02, commits `66fa5ee..c53c70c` (`p02-t01` through `p02-t10`, including final fix-loop commits `79a8127..c53c70c`)
**Files reviewed:** 15
**Commits:** 18 (`66fa5ee..c53c70c`)

## Summary

The prior findings C1/C2/C3/I1/I2 from the first p02 review and I3/I4/I5 from the second p02 review are fixed in the updated code: schema/status contracts, prompt framing, path confinement, peer-id validation, missing-Paseo exit mapping, agency behavior, and partial record preservation now align with the main p02 contracts. One Important issue remains in the `--fail-on-section-error` control flow: the flag neither preserves section aggregation on hard errors nor converts completed impasse sections to exit 74 as the design requires.

Artifacts available and read: `spec.md`, `design.md`, `plan.md`, `implementation.md`, `discovery.md`, and prior reviews `reviews/p02-review-2026-05-04.md` / `reviews/p02-review-2026-05-04-v2.md`. The authoritative code review surface was the provided commit range `66fa5ee..c53c70c`.

## Findings

### Critical

None

### Important

- **`--fail-on-section-error` bypasses the designed aggregation semantics** (`plugins/consensus/skills/consensus-refine/scripts/consensus-refine.mjs:799`)
  - Issue: With `failOnSectionError` set, `runSequential` throws immediately inside the per-section catch block, before later sections run and before the partial artifact is rendered. That conflicts with the design's per-section error handling, which says section-level errors continue other sections and are surfaced in the artifact. The same flag is also not applied to normally completed `impasse` sections: after `runSequential` renders a `partial` result, `runWrapperCli` returns 0 because there is no post-aggregation check for `status: "impasse"`, even though the design says `--fail-on-section-error` exits 74 when any section ended in error or impasse.
  - Fix: Always collect section results and write the artifact first. After aggregation, if `failOnSectionError` is set and any section status is `error` or `impasse`, return or throw a `SECTION_ERROR` with exit 74 while preserving the output path/run directory in the structured event. Add regressions for both cases: hard error still processes later sections and writes the artifact, and explicit IMPASSE with the flag exits 74.
  - Requirement: FR4, FR6, p02-t10

### Minor

- **Rendered artifact still does not use the designed canonical-state container** (`plugins/consensus/skills/consensus-refine/scripts/consensus-refine.mjs:693`)
  - Issue: `renderDeliberationArtifact` starts with a visible H1 and emits visible fenced JSON blocks for resolution, section states, section status, and verdicts. Design section 4.5 calls for structured frontmatter plus HTML-commented canonical JSON blocks so resume can read machine state while humans primarily see sanitized prose. Heading containment for prose logs is also still deferred.
  - Suggestion: Before resume work depends on this artifact shape, update the renderer to emit frontmatter and commented canonical blocks, or explicitly revise the design if visible JSON blocks are intentional for v0.1.

- **Missing-Paseo remediation points at the wrong install-assist path** (`plugins/consensus/skills/consensus-refine/scripts/consensus-refine.mjs:22`)
  - Issue: The preflight remediation advertises `plugins/consensus/skills/consensus-refine/scripts/install-paseo.mjs`, but the spec and plan point to the repo-level `scripts/install-paseo.mjs` that is scheduled for p04. The current test only checks that some `install-paseo.mjs` path appears, so it would not catch the drift.
  - Suggestion: Change the remediation constant and test to the repo-level `scripts/install-paseo.mjs` path when the install-assist script is added.

## Requirements/Design Alignment

**Evidence sources used:** `spec.md`, `design.md`, `plan.md`, `implementation.md`, `discovery.md`, prior p02 review artifacts, code diff `66fa5ee..c53c70c`, and p02 scoped tests.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| FR2 | implemented | Alternating turns, verdict validation, REVISE updates, default/configurable peers, and prompt framing are implemented for p02. |
| FR3 | implemented | Hash normalization, hash-match, double ACCEPT, max-rounds, oscillation, and agency-specific behavior are implemented. |
| FR4 | partial | Sequential section parsing/orchestration works, but `--fail-on-section-error` can abort later sections instead of preserving aggregation. Parallel remains correctly deferred to p03. |
| FR5 | partial | Records, statuses, and artifacts are written, and partial hard-error records are preserved. Artifact canonical-state shape still diverges from the design. |
| FR6 | partial | Explicit impasse terminal state exists, but `--fail-on-section-error` does not convert completed impasse sections to exit 74. User intervention/resume remains later-scope. |
| FR9 | implemented | Host-aware defaults, provider inventory preflight, provider-id grammar validation, and missing-Paseo exit behavior are implemented. |
| NFR6 | implemented for p02 | Paseo availability/preflight and environment error mapping are fixed for wrapper and direct loop paths. Parallel host-dispatch authorization remains p03 scope. |

### Extra Work (not in declared requirements)

None significant. Parsing `--prepare-parallel` and `--fan-in` remains p02 CLI-surface preparation for p03 behavior.

## Verification Commands

Run these to verify the implementation:

```bash
node --test tests/loop-convergence.test.mjs tests/verdict-validation.test.mjs tests/loop-records.test.mjs tests/paseo-invocation.test.mjs tests/consensus-loop-cli.test.mjs tests/wrapper-options.test.mjs tests/section-parser.test.mjs tests/path-safety.test.mjs tests/sequential-wrapper.test.mjs tests/error-handling.test.mjs
node scripts/validate.mjs
git diff --check 66fa5ee..c53c70c
```

Reviewer executed the commands above; all passed. The remaining Important finding is a design/behavior gap not covered by the current tests.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks.
