---
oat_generated: true
oat_generated_at: 2026-05-04
oat_review_scope: p01
oat_review_type: code
oat_project: .oat/projects/shared/consensus-plugin
---

# Code Review: p01

**Reviewed:** 2026-05-04
**Scope:** Phase 1 repository scaffolding and distribution metadata in `e5f7f6f..8c5b80a`
**Files reviewed:** 34
**Commits:** 7 commits (`e5f7f6f..8c5b80a`)

## Summary

Phase 1 establishes the expected repo layout, provider manifests, marketplace entries, baseline docs, tests, workflows, and structural validator. The local test suite, validator, and diff whitespace checks pass, but the validator misses a key FR8 path: standalone `skills/*/SKILL.md` entries can be invalid while CI still reports success.

## Findings

### Critical

None

### Important

- **Validator ignores standalone skill directories** (`scripts/validate.mjs:287`)
  - Issue: FR8 requires CI to validate each `skills/*/SKILL.md`, and the design says `scripts/validate.mjs` should parse skills under both `skills/` and `plugins/*/skills/`. `validateRepository` only validates fixed directory layout plus skills referenced by the hardcoded consensus provider manifests, so a new standalone skill can bypass required frontmatter and folder/name checks. I verified this against an archived copy of the reviewed tree by adding `skills/bad-skill/SKILL.md` with missing required fields; `validateRepository` returned `{"ok":true,"errors":[]}`.
  - Fix: Discover skill directories from the filesystem (`skills/*/SKILL.md` and `plugins/*/skills/*/SKILL.md`) and validate all of them with `validateSkillFrontmatter`. Keep manifest skill-reference validation as an additional check. Add a regression test that creates an invalid standalone skill in a temp repo and asserts `validateRepository` fails.
  - Requirement: FR8

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `spec.md`, `design.md`, `plan.md`, `implementation.md`, `discovery.md`

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| FR1 | implemented for p01 scaffold | Top-level `skills/`, self-contained `plugins/consensus/`, provider manifests, repo marketplace entries, and skill frontmatter are present. Full `npx skills add` smoke remains planned for later release validation. |
| FR7 | partial / planned | README includes release-candidate install matrix and provider docs. Fresh provider install smoke is explicitly deferred to later release validation tasks. |
| FR8 | partial | CI workflows, tests, JSON parsing, manifest path checks, version consistency, and README matrix validation exist. Standalone skill discovery is missing; see Important finding. |
| NFR3 | implemented for p01 docs/frontmatter | Canonical skill includes baseline frontmatter plus additive `allowed-tools`; `CONTRIBUTING.md` documents additive vs. provider-specific frontmatter. |
| NFR4 | implemented for p01 scaffold | Plugin manifests do not reference `.oat/` or project-local infrastructure. |
| NFR5 | implemented for p01 docs | README labels v0.1 pre-release scope, provider asymmetry, and deferred features honestly. |

### Extra Work (not in declared requirements)

None significant. The release workflow is scaffolded as planned by p01-t07.

## Verification Commands

Run these to verify the implementation:

```bash
node --test
node scripts/validate.mjs
git diff --check e5f7f6f..8c5b80a
```

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks.
