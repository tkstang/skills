---
oat_generated: true
oat_review_scope: bounded
oat_review_type: code
oat_review_run_id: "c79c4062-1531-457e-9b7e-79049cd55942"
---

# Consensus Review

**Verdict:** pass
**Worktree:** `/Users/tstang/orca/workspaces/skills/session-fidelity`
**Scope token:** `351d1c990d50daacf7261e2254e5237c1a48b41f9a471526ff12f802b03a177a`
**Reviewer:** claude (model unobserved, effort unobserved)
**Findings:** 0 critical, 0 high, 0 medium, 5 low

## Request

Verify the bounded p02 follow\-up fixes against prior Opus review af48661b\-3642\-4447\-8bec\-cd81881f0bea, base6d7ec63c9d42feb6a9423bc301e64127428969a2\. Review changed behavior and regression risk, not unchanged whole phase\. No edits or providers\. Read surrounding immutable source as needed\.<br>H1 response usage checks native thread\_id against source\.nativeSessionId; originating session\_id does not falsely reject own usage\. Native fixture includes distinct root/thread plus genuinewrong\-thread case\.<br>H2 bounded watch/catch\-up preserves delivered calls/results despite hundreds of optional whole\-source usage/skill records\. Optional metadata trims first with honest counts and captured\-source scope\. No accidental starvation of existing coverage/diagnostic signals; bounded report \<=limit when irreduciblefloorpermits\. Full futurecompletecapture must still be able to retain metadata withouttotalcap\.<br>M1 cumulative/last\-turn usage preserves Codex root/child/inherited/unknown ownership and reset segmentation does not falsely bridge ownership boundaries\. Missing/contradictory boundary staysunknown, model joins avoidwrongownership\. H1 response handling and ownership should coexist\.<br>M2 full native Skill caller arguments restored underpreviewcap; only separate attachment instructionbodiesexcluded\. No unrecordednarrowing\.<br>L1 source\-name coverage explicitly distinguishes its native carrier from event\-levelskill evidence; validemptylisting versus absent, consumer/watchguards coherent\. L2 repeatedavailable names dedup with documentedrepresentativelocator, invokedoccurrencesretained; budget/locators/counts remainhonest\.<br>Check relevant regression tests and changed docs/versions/changelog/generated parity\. Report unresolved originalissues or concrete newdefects\. Response uses externaldocument anchor with exactpacketSHA256 and repo path/lines in evidence\. ZeroCritical/High means pass \(Medium/Low permitted\); changes\_requested requiresCritical/High\. State unrunchecks\.<br>

## Summary

All six accepted p02 findings \(H1, H2, M1, M2, L1, L2\) are implemented in commit b58fd28f and are behaviorally correct as far as static review plus independent corpus evidence can establish\. No Critical or High defect survived verification, so this passes\.<br><br>H1 — \`codexUsage\` now guards on \`payload\.thread\_id\` against \`source\.nativeSessionId\` \(src/shared/transcript/activity/usage\.ts:208\-216\) and no longer consults \`session\_id\`\. I independently confirmed the premise rather than trusting implementation\.md: across 39 local Codex rollouts / 1,931 \`token\_usage\_record\` entries, \`thread\_id\` was present on 100% and equalled the \`session\_meta\.id\` on 100%, while \`session\_id\` differed from the header id on 1,617 of them — i\.e\. the old guard really did falsely reject own\-session usage at scale, and the new guard rejects none of it\. The regression fixture carries a distinct root/thread pair plus a genuine foreign\-thread case \(usage\.test\.ts:159\-200\)\.<br><br>H2 — \`projectActivityWithLimits\` inserts a dedicated optional\-source\-metadata reduction pass before any delivered evidence is touched \(project\.ts:686\-732\)\. The design is sound and, importantly, the subsequent wholesale drop of optional metadata in the group\-removal path is \*justified\* rather than coarse: the first binary search does evaluate \`retainedCount = 0\`, so by the time step 3 runs it is already proven that zero optional metadata plus all groups does not fit\. Coverage and diagnostics are passed through untouched by \`retainOptionalSourceMetadata\` \(project\.ts:411\-413\), omission counts reconcile to captured\-source totals, \`scope\` stays \`captured\-source\`, and both searches are monotone over nested prefix sets so the bounded result is \<= limit whenever a floor exists\. Export mode's 64 MiB budget means a complete capture still retains metadata uncapped\.<br><br>M1 — Ownership is now a required field on every sample, \`counterStates\` is keyed per ownership class so a reset cannot bridge inherited→owned \(usage\.ts:148\-185\), and the model join is keyed \`$\{ownership\}:$\{turnId\}\` \(usage\.ts:144, 239\)\. Missing/contradictory lineage stays \`unknown\` via the unchanged \`headerOwnershipEvidence\` rules\. I checked the over\-strictness risk empirically: in 13 local bounded\-child rollouts, \`turn\_context\` records are predominantly inherited \(50 vs 24\), so an ownership\-keyed join could plausibly have destroyed model attribution — but all 518 usage records joined same\-ownership, zero cross\-ownership\. Ordinals are present on 100% of \`turn\_context\`, \`token\_usage\_record\` and \`token\_count\` records, so the segmentation is actionable rather than degrading to \`unknown\`\. H1 and ownership coexist cleanly: the thread guard runs before labelling, and no inherited usage record exists below the boundary in the corpus, so it produces no \`USAGE\_SESSION\_MISMATCH\` noise\.<br><br>M2 — \`claudeToolArguments\` is deleted outright; \`arguments: input\` is the sole assignment in claude\-code\.ts:329, so there is no residual narrowing\. Attachment extraction stays names\-only \(claudeSourceSkills, claude\-code\.ts:48\-93\) and previews remain capped at \`previewBytes\` in every mode\.<br><br>L1/L2 — The coverage class is renamed to \`source\-skill\-names\` with no stale \`'skills'\` literal anywhere in src, tests, or docs; status derives from a new \`sourceSkillNamesRecorded\` flag whose predicate exactly matches the one producing \`available\` entries, so empty\-listing \(\`available\`/0\) and absent\-carrier \(\`not\-recorded\`/0\) are coherent\. The watch guard was renamed in lockstep \(watch\.ts:281\)\. Dedup keeps the latest \`available\` occurrence per name by identity while preserving every \`invoked\` occurrence, matching the documented "latest recorded locator" contract\.<br><br>Versions, changelog and generated parity are consistent: four canonical bumps, a matching Unreleased entry naming all four, and generated payloads under skills/ and plugins/ carry the new symbols and identical versions\.<br><br>The five reported items are all Low: one residual \(pre\-existing\) priority inversion in the last\-resort metadata path, one assertion that the M2 fixture edit rendered vacuous, one untested branch, one API footgun, and one coverage\-label gap for the invoked\-only carrier\. None blocks the phase\.

## Scope and provenance

- Selector: `document=/tmp/evidence-p02-fix-review-packet.md`
- Requested paths: none
- External documents: `/private/tmp/evidence-p02-fix-review-packet.md`
- Captured evidence: 99573 bytes
- Reviewer claim: \{"provider":"anthropic","model":"claude\-opus\-5","effort":"high"\}
- Observed reviewer evidence: The provider envelope identifies the provider only; model and effort were not independently observed\.
- Diversity: unknown — Provider selection alone does not establish a different model family\.
- Drift comparison: stable within stated coverage
- Detection limit: Content changes outside the selected set may go undetected when Git status is unchanged; ignored, unselected, external, and transient write\-then\-revert activity are not fully monitored\.

### Authorship evidence

- unknown — unknown, unknown coverage; No bounded author evidence was supplied\.

### Reviewer-reported inspected context

- Immutable phase review packet for p02 follow\-up fixes \(request, scope adaptation, changed\-file manifest, authored before/after diff\) (`43f1202ac8aa64c11369f05256a04c318e82a345da73cd5010ac016d205a389c`)
- Worktree /Users/tstang/orca/workspaces/skills/session\-fidelity at reviewed HEAD 9a74ed1d8b6ca5bc7354e6bdc45bdae052ac0487 \(base 6d7ec63c9d42feb6a9423bc301e64127428969a2\), read\-only (`9a74ed1d8b6ca5bc7354e6bdc45bdae052ac0487`)
- Local Codex rollout corpus under ~/\.codex/sessions, read\-only structural survey of record/field names and ordinals \(no transcript content inspected\) (`local-corpus-2026-09-20-40-to-60-most-recent-rollouts`)

## Findings

### Critical

None

### High

None

### Medium

None

### Low

- **L1: Last\-resort metadata search still lets optional source metadata starve source\-wide coverage entries** (`anchor: evidence-p02-fix-review-packet.md@sha256:43f1202ac8aa64c11369f05256a04c318e82a345da73cd5010ac016d205a389c#authored-before-after-diff/src/shared/transcript/activity/project.ts`)
  - Claim: The H2 fix protects delivered events and the first two reduction passes from optional metadata, but the final fallback pass at src/shared/transcript/activity/project\.ts:767\-807 still ranks candidates with \`compareMetadataPriority\` \(project\.ts:270\-281\), which sorts by descending \`locator\.recordIndex\` with \`?? \-1\` for absent locators\. The five source\-wide coverage entries produced by \`baseCoverage\` and the \`source\-skill\-names\` entry \(src/shared/transcript/activity/extract\.ts:60\-66, 164\-174\) carry no locator, so they sort dead last and are evicted before any usage sample\. In that path the honesty summary the H2 finding set out to protect can still be starved by optional whole\-source metadata\.
  - Evidence: src/shared/transcript/activity/project\.ts:777 calls \`retainMetadata\(metadata, retainedCount\)\` on the \*full\* metadata set including \`sourceSkills\`, \`usage\.samples\` and \`usage\.diagnostics\`, unlike step 3 which uses \`boundedMetadata\`\. \`compareMetadataPriority\` \(project\.ts:275\) evaluates \`\(right\.locator?\.recordIndex ?? \-1\) \- \(left\.locator?\.recordIndex ?? \-1\)\`, and the coverage entries built at extract\.ts:61\-65 are \`\{dataClass, status, captured\}\` with no \`locator\` field\. The path is demonstrably reachable: the pre\-existing test 'labels captured\-source skill metadata\.\.\.' \(project\.test\.ts:612\-676\) and the 1,000\-diagnostic/1,005\-coverage fixture asserting \`first\.coverage\.length \+ first\.omitted\.coverageEntries === 1\_005\` \(project\.test\.ts:600\-601\) both exercise heavy metadata reduction\. Reaching step 4 additionally requires that zero events plus zero optional metadata plus full coverage/diagnostics still exceeds maxBytes, so exposure is narrow; counts stay honest via \`omitted\.coverageEntries\` \(rendered at render\.ts:133\)\.
  - Suggestion: Either reuse the two\-tier ordering in the fallback — exhaust \`sourceSkills\`/\`usageSamples\`/\`usageDiagnostics\` before considering \`coverage\`/\`diagnostics\` candidates — or give \`compareMetadataPriority\` an explicit kind\-class tier so locator\-less coverage entries are not implicitly ranked below every located optional sample\.
  - Confidence: 0.6

- **L2: M2 fixture edit left the event\-level body\-leak assertion matching nothing** (`anchor: evidence-p02-fix-review-packet.md@sha256:43f1202ac8aa64c11369f05256a04c318e82a345da73cd5010ac016d205a389c#authored-before-after-diff/src/shared/transcript/activity/extract.test.ts`)
  - Claim: src/shared/transcript/activity/extract\.test\.ts:171 still asserts \`expect\(JSON\.stringify\(extracted\)\)\.not\.toContain\('private sentinel'\)\`, but the M2 change replaced the fixture field that produced that string \(\`body: 'private sentinel'\` became \`args: 'caller invocation input'\` at extract\.test\.ts:58\-61\)\. The exact substring 'private sentinel' now occurs nowhere in the fixture, so the assertion passes unconditionally and no longer constrains anything\.
  - Evidence: grep over src/shared/transcript/activity/extract\.test\.ts finds 'sentinel' at only three lines: 75 \(\`content: 'private listing sentinel'\`\), 87 \(\`content: 'private invoked sentinel'\`\) and 171 \(the assertion itself\)\. Neither fixture string contains 'private sentinel' as a substring, so the \`not\.toContain\` check cannot fail regardless of extractor behaviour\. The adjacent checks at extract\.test\.ts:169\-170 still carry real weight, but they scope to \`JSON\.stringify\(extracted\.sourceMetadata\)\` only — after this edit nothing asserts that whole\-report serialization excludes attachment instruction bodies, which is precisely the boundary M2 promised to keep \('only separate attachment instruction bodies excluded'\)\.
  - Suggestion: Point the assertion at a string the fixture actually contains — e\.g\. assert \`JSON\.stringify\(extracted\)\` excludes 'private listing sentinel' and 'private invoked sentinel' — so the test again proves attachment bodies stay out of the event stream while the restored \`Skill\` caller args stay in\.
  - Confidence: 0.95

- **L3: New bounded group\-removal branch has no regression test** (`anchor: evidence-p02-fix-review-packet.md@sha256:43f1202ac8aa64c11369f05256a04c318e82a345da73cd5010ac016d205a389c#authored-before-after-diff/src/shared/transcript/activity/project.ts`)
  - Claim: The H2 change adds \`boundedMetadata\`/\`boundedReasons\` at src/shared/transcript/activity/project\.ts:726\-732 and threads them through the group\-removal search at project\.ts:746\-757, but both new and updated tests only reach the optional\-metadata search at project\.ts:686\-724\. The branch that actually trades delivered event groups away — and the one that must report \`sourceSkills\`/\`usageSamples\`/\`usageDiagnostics\` omissions at their full captured\-source totals — is unexercised\.
  - Evidence: The new test 'trims oversized optional source metadata before delivered event evidence' \(src/shared/transcript/activity/project\.test\.ts:678\-753\) explicitly asserts \`report\.omitted\.byteLimitGroups\` is 0 and that all four delivered events survive, which is by construction the step\-2 path\. The pre\-existing test at project\.test\.ts:612\-676 uses \`activity\(\[\]\)\` with an empty delivery range, so it also now resolves inside step 2 rather than reaching group removal\. A mistake such as passing \`metadata\` instead of \`boundedMetadata\` at project\.ts:752, or spreading \`initialReasons\` instead of \`boundedReasons\` at project\.ts:754, would silently under\-report omitted optional metadata with no test failure\.
  - Suggestion: Add one case with event evidence large enough that step 2 cannot fit the budget \(forcing \`omitted\.byteLimitGroups \> 0\`\), asserting that coverage and diagnostics survive intact while \`sourceMetadata\.skills\.length \+ omitted\.sourceSkills\` and the usage equivalents still reconcile to the captured\-source totals\.
  - Confidence: 0.85

- **L4: extractUsageMetadata's defaulted events parameter silently degrades Codex ownership to unknown** (`anchor: evidence-p02-fix-review-packet.md@sha256:43f1202ac8aa64c11369f05256a04c318e82a345da73cd5010ac016d205a389c#authored-before-after-diff/src/shared/transcript/activity/usage.ts`)
  - Claim: src/shared/transcript/activity/usage\.ts:262 declares \`events: readonly ExtractedActivityEvent\[\] = \[\]\`\. Ownership is now a required field on every sample and the whole M1 contract depends on that argument, but a caller that omits it gets no type error: \`activityOwnershipContext\` finds no \`session\_meta\` header, returns \`\{kind: 'unknown'\}\` \(correlate\.ts:88\), and every Codex sample is labelled \`ownership: 'unknown'\` with reset segmentation and model joins collapsed into one bucket\.
  - Evidence: \`activityOwnershipContext\` \(src/shared/transcript/activity/correlate\.ts:77\-88\) filters \`events\` for \`session\_meta\` metadata events and returns \`\{kind: 'unknown'\}\` when \`headers\.length === 0\`; \`ownershipForLocator\` \(correlate\.ts:112\-120\) then returns 'unknown' for every locator\. Today the single call site passes real events \(src/shared/transcript/activity/extract\.ts:145\) so no live path is affected, and the degradation is honest rather than wrong — but the default converts a required input into a silent accuracy loss instead of a compile\-time error, which is a poor fit for a field the M1 finding made load\-bearing\.
  - Suggestion: Drop the default and make \`events\` a required parameter; the one existing caller already supplies it, so this costs nothing and turns any future omission into a type error\.
  - Confidence: 0.7

- **L5: source\-skill\-names coverage reports not\-recorded when only the invoked\_skills carrier is present** (`anchor: evidence-p02-fix-review-packet.md@sha256:43f1202ac8aa64c11369f05256a04c318e82a345da73cd5010ac016d205a389c#authored-before-after-diff/src/shared/transcript/activity/claude-code.ts`)
  - Claim: \`sourceSkillNamesRecorded\` is set only for the \`skill\_listing\` carrier \(src/shared/transcript/activity/claude\-code\.ts:282\-286\), and \`captured\` counts only \`evidence === 'available'\` entries \(src/shared/transcript/activity/extract\.ts:170\-172\)\. A Claude transcript that records \`attachment\.type == "invoked\_skills"\` but no \`skill\_listing\` therefore reports \`source\-skill\-names: not\-recorded, captured 0\` while \`sourceMetadata\.skills\` is non\-empty with invoked source\-level names — a coverage label that contradicts the metadata beside it\.
  - Evidence: \`claudeSourceSkills\` \(claude\-code\.ts:48\-93\) treats both \`skill\_listing\` \(available\) and \`invoked\_skills\` \(invoked\) as captured\-source carriers, but the \`sourceSkillNamesRecorded\` predicate at claude\-code\.ts:282\-286 matches \`skill\_listing\` only\. The L1 finding asked coverage to distinguish its native carrier from event\-level skill evidence; \`invoked\_skills\` is a second \*source\-level\* carrier, not event\-level evidence, and the docs \(documentation/docs/engineering/architecture/session\-schemas/index\.md, 'absent carriers are \`not\-recorded\`'\) do not record that this class tracks only the availability listing\. Impact is contained because consumers exclude this class from signalling \(src/skills/session\-observer/src/lib/watch\.ts:281\) and the skills themselves are still emitted, so no data is lost — only the summary label under\-describes one carrier\.
  - Suggestion: Either set \`sourceSkillNamesRecorded\` for the \`invoked\_skills\` carrier too \(keeping \`captured\` as the available\-name count\), or state explicitly in the schema docs and the dataClass description that \`source\-skill\-names\` tracks the availability listing carrier alone, so a not\-recorded status beside non\-empty invoked names is legible rather than contradictory\.
  - Confidence: 0.7

## Questions

- H1's guard is skipped entirely when \`payload\.thread\_id\` is absent \(usage\.ts:210\-216\), since only a present\-and\-differing thread id rejects\. My survey of 1,931 usage records across 39 local rollouts found zero missing \`thread\_id\`, so this is unobservable today — but was dropping the \`session\_id\` check outright \(rather than falling back to it when \`thread\_id\` is absent\) a deliberate call for older rollout formats, or simply untested territory?
- Once optional metadata cannot save the budget, step 3 discards 100% of source skills and usage samples before removing a single event group\. My reading is that this is exactly the requested events\-first priority and is provably necessary \(step 2 already evaluates retainedCount=0\), so an over\-budget Codex watch digest will always show zero usage samples\. Is that acceptable steady\-state behaviour for watch, or should a small floor of most\-recent usage samples survive alongside trimmed events?
- \`ACTIVITY\_SCHEMA\_VERSION\` stays at 1 \(types\.ts:8\) although this change renames a coverage \`dataClass\` and adds a required \`ownership\` field to every usage sample — both schema\-visible breaks\. Given the feature is still under \`\#\# \[Unreleased\]\` and the repo prefers clean breaks over compatibility shims, I read this as intentional; confirm the version is meant to stay pinned until the activity schema first ships\.

## Limitations

- No test, build, type\-check, lint, or format command was run — the review brief prohibits them\. The implementation\.md claims of 'activity 83/83, relevant consumers 869/869, types, build/freshness, validate, scoped lint/format, diff check and docs production 58 pages passed' are therefore unverified by me; my assessment of the new and amended tests is from reading them, not executing them\.
- Generated\-payload parity was spot\-checked by symbol presence and version strings only \(\`source\-skill\-names\`, \`recordedThreadId\`, \`retainOptionalSourceMetadata\` found in skills/session\-observer/scripts/session\-observer\.mjs, plugins/consensus/skills/observer/scripts/lib/digest\.mjs and skills/session\-export\-transcript/scripts/session\-export\-transcript\.mjs; all six generated SKILL\.md versions matching canonical\)\. \`pnpm run build:check\` was not run, so byte\-exact regeneration parity is unconfirmed\.
- Byte\-budget monotonicity for both binary searches was reasoned about analytically \(each retainedCount forms a nested prefix of one priority\-sorted list, and per\-entry serialized length dominates the at\-most\-one\-character shrink in the rendered omission counters\) rather than measured\. A pathological fixture that violates monotonicity would yield a suboptimal — though still within\-limit — report\.
- The Codex corpus evidence comes from the 40\-60 most recent local rollouts under ~/\.codex/sessions, which is a single machine's sample from one CLI version range\. It strongly supports the \`thread\_id\` premise and the ownership\-join behaviour but does not establish them for older rollout formats or other Codex builds\.
- Review was bounded to the base\.\.HEAD authored delta per the packet; unchanged p02 surfaces \(codex\.ts record extraction, digest\.ts, the collaboration hooks\) were read only as context where the changed code depends on them\.
- Content changes outside the selected set may go undetected when Git status is unchanged; ignored, unselected, external, and transient write\-then\-revert activity are not fully monitored\.
- Provider read\-only controls are not universal filesystem or network isolation\.
- Retention is operator\-managed; the external run directory has no automatic cleanup or replay policy\.

## Checks reported

- Evidence packet SHA256 matches manifest — passed: shasum \-a 256 of /private/tmp/evidence\-p02\-fix\-review\-packet\.md returned 43f1202ac8aa64c11369f05256a04c318e82a345da73cd5010ac016d205a389c, identical to the manifest entry\.
- Reviewed HEAD and base match the packet; tree clean — passed: git rev\-parse HEAD = 9a74ed1d8b6ca5bc7354e6bdc45bdae052ac0487; log shows b58fd28f \(the single fix commit\) on top of base 6d7ec63c; git status reports no modifications\.
- H1 — response usage keys on native thread\_id, not originating session\_id — passed: usage\.ts:208\-216 reads payload\.thread\_id and compares to source\.nativeSessionId; session\_id is no longer consulted anywhere in codexUsage\.
- H1 — native fixture covers distinct root/thread plus a genuine wrong\-thread rejection — passed: usage\.test\.ts:159\-200 gives own\-session records thread\_id 'native\-session' with differing session\_id 'root\-session', adds a session\_meta header, and includes a foreign record with thread\_id 'other\-thread' that must be rejected\.
- H1 — premise verified against real rollouts rather than trusted from implementation\.md — passed: 39 rollouts / 1,931 token\_usage\_record entries: thread\_id present and equal to session\_meta\.id in 100%; session\_id differed from the header id in 1,617, confirming the pre\-fix guard false\-rejected own usage at scale\.
- H2 — optional source metadata is reduced before any delivered group is removed — passed: project\.ts:686\-724 runs a dedicated optional\-metadata binary search on the fully retained group set and returns early on the first fitting candidate; group removal at 734\-764 is reached only afterwards\.
- H2 — coverage and diagnostics are not starved by the new pass — passed: retainOptionalSourceMetadata passes metadata\.coverage and metadata\.diagnostics through by reference \(project\.ts:411\-413\) and never enrolls them as trim candidates \(376\-398\)\. A residual inversion in the unrelated last\-resort pass is reported as a Low finding\.
- H2 — omission counts stay honest and captured\-source scope is preserved — passed: Counts are computed as full\-minus\-retained \(project\.ts:707\-714, 727\-732\) and rendered at render\.ts:134\-135; sourceMetadata\.scope remains 'captured\-source' \(project\.ts:639\-643\)\. Test assertions reconcile retained\+omitted to 400/400\.
- H2 — bounded report stays \<= limit when a floor exists — passed: Every candidate is accepted only under \`renderedBytes \<= limits\.maxBytes\`; both searches range over nested prefix sets of one priority\-sorted list, and step 2 evaluates retainedCount=0, which justifies step 3's wholesale metadata drop rather than making it coarse\.
- H2 — complete capture can still retain metadata without a total cap — passed: export mode keeps maxBytes at 64 MiB and maxInvocations null \(project\.ts:48\-53\); no reduction pass runs unless the initial report exceeds the budget \(project\.ts:684\)\.
- M1 — cumulative/last\-turn usage preserves root/child/inherited/unknown ownership — passed: ownershipForLocator is applied per record \(usage\.ts:167, 238\) and every sample now carries a required ownership field \(types\.ts:119\); non\-codex sources resolve to 'root' → 'owned' \(correlate\.ts:81\)\.
- M1 — reset segmentation cannot bridge an ownership boundary — passed: counterStates is a Map keyed by ownership holding independent previousSnapshot/previousTotal/segment \(usage\.ts:148\-185\); the new test asserts the inherited 100→120 run does not emit a reset against the owned 20→10 run, yielding exactly one USAGE\_COUNTER\_RESET\.
- M1 — missing or contradictory lineage stays unknown — passed: headerOwnershipEvidence returns unknown on dangling markers, conflicting direct/nested parents, or a non\-integer boundary \(correlate\.ts:41\-73\); activityOwnershipContext returns unknown on no header or disagreeing headers \(correlate\.ts:88\-101\); locators without a safe\-integer ordinal resolve to unknown \(correlate\.ts:113\-118\)\.
- M1 — model joins avoid wrong ownership — passed: turnModels is keyed \`$\{ownership\}:$\{turnId\}\` on both write \(usage\.ts:144\) and read \(usage\.ts:239\); the new test asserts an owned usage record whose turn\_context is inherited receives no model\.
- M1 — ownership keying does not destroy model attribution on real data — passed: 13 bounded\-child rollouts: 518/518 usage records joined same\-ownership, 0 cross\-ownership, 0 unjoined, despite turn\_context records being 50 inherited vs 24 owned — so the strict join loses nothing observable\.
- M1 — H1 thread handling and ownership labelling coexist — passed: The thread guard precedes ownership labelling \(usage\.ts:210\-216 then 238\); in the corpus no token\_usage\_record falls below a subagent boundary, so the guard generates no inherited\-history mismatch noise\. No import cycle: usage\.ts imports correlate\.js, which does not import usage\.js\.
- M2 — full native Skill caller arguments restored under the preview cap — passed: claudeToolArguments is deleted; claude\-code\.ts:329 assigns \`arguments: input\` unconditionally, and projectEvent caps it via preview\(event\.arguments, limits\.previewBytes\) at project\.ts:461\-463\.
- M2 — only separate attachment instruction bodies remain excluded; no unrecorded narrowing — passed: grep for 'arguments' in claude\-code\.ts returns exactly one line \(329\)\. claudeSourceSkills \(48\-93\) copies only names and locators from skill\_listing/invoked\_skills, never content or path\. Documented in claude\-code\.md and both user\-guide pages\.
- L1 — coverage distinguishes the native carrier from event\-level skill evidence — passed: dataClass renamed to 'source\-skill\-names' \(types\.ts:216\) with status driven by sourceSkillNamesRecorded; event\-level skillEvidence remains a separate per\-event field \(project\.ts:491\-493\)\. A gap for the invoked\-only carrier is reported as a Low finding\.
- L1 — valid empty listing distinguished from an absent carrier — passed: A skill\_listing with names: \[\] yields status 'available', captured 0 \(extract\.ts:167\-173\), asserted by the new test at extract\.test\.ts:180\-202; an absent carrier yields 'not\-recorded', asserted at extract\.test\.ts:217\-222 and for Codex at 594\-599\.
- L1 — consumer and watch guards renamed coherently — passed: watch\.ts:281 excludes 'source\-skill\-names' from the coverage signal; grep across src, tests and documentation finds no residual 'skills' dataClass literal or stale doc reference\.
- L2 — repeated available names dedup to a documented representative locator — passed: extract\.ts:133\-142 keeps the last 'available' entry per name by identity; extract\.test\.ts:153\-160 asserts 'available\-one' resolves to recordIndex 3, and claude\-code\.md plus both user\-guide pages document 'latest recorded locator'\.
- L2 — invoked occurrences retained; budget, locators and counts stay honest — passed: The filter passes every \`evidence === 'invoked'\` entry through \(extract\.ts:140\), asserted by both invoked\-one occurrences at extract\.test\.ts:146\-165; coverage captured counts the deduplicated available set \(2, extract\.test\.ts:172\-176\) consistent with the documented contract\.
- Docs, versions and changelog updated with the behaviour change — passed: Four canonical bumps \(observer 1\.0\.77, export 2\.0\.28, collab 1\.0\.65, fork 0\.2\.42\) with a matching CHANGELOG\.md Unreleased Fixed entry naming each; schema and user\-guide docs describe thread\_id identity, ownership boundaries, dedup, empty\-vs\-absent listings and the metadata trim order, all matching the code\.
- Generated payload parity spot\-check — passed: source\-skill\-names, recordedThreadId and retainOptionalSourceMetadata all present in skills/session\-observer/scripts/session\-observer\.mjs, plugins/consensus/skills/observer/scripts/lib/digest\.mjs and skills/session\-export\-transcript/scripts/session\-export\-transcript\.mjs; all six generated SKILL\.md versions equal their canonical sources\.
- Prior\-review findings all dispositioned in code — passed: Each of H1, H2, M1, M2, L1 and L2 has a located implementation plus at least one regression test; no accepted finding is unimplemented\.

## Suggested verification

- Vitest suite \(pnpm run test / test:vitest\) — not\_run: Prohibited by the review brief \(no tests\)\. The implementation's claimed 83/83 activity and 869/869 consumer results are unverified here\.
- pnpm run build:check / build regeneration parity — not\_run: Prohibited by the review brief \(no builds\)\. Generated parity was spot\-checked by symbol and version presence only\.
- pnpm run type\-check — not\_run: Prohibited by the review brief\. Type soundness of the new required ownership field and the exported correlate helpers was assessed by reading only\.
- pnpm run validate / validate:skill\-versions — not\_run: Prohibited by the review brief\. Version bumps and the changelog entry were confirmed by inspection instead\.
- Scoped lint and format \(oxlint / oxfmt \-\-check\) — not\_run: Prohibited by the review brief \(no formatters\)\.
- Docs site production build — not\_run: Prohibited by the review brief\. Documentation was reviewed as source Markdown against the implemented behaviour\.

## Artifact paths

- Run directory: `/Users/tstang/.local/state/consensus/d43c2739a174d5c14978abd503dd1c07b810241e7d4a5214ccd077c30348dcb7/reviews/c79c4062-1531-457e-9b7e-79049cd55942`
- Captured request: `/Users/tstang/.local/state/consensus/d43c2739a174d5c14978abd503dd1c07b810241e7d4a5214ccd077c30348dcb7/reviews/c79c4062-1531-457e-9b7e-79049cd55942/request.txt`
- Captured evidence: `/Users/tstang/.local/state/consensus/d43c2739a174d5c14978abd503dd1c07b810241e7d4a5214ccd077c30348dcb7/reviews/c79c4062-1531-457e-9b7e-79049cd55942/evidence.json`
- Host result JSON: `/Users/tstang/.local/state/consensus/d43c2739a174d5c14978abd503dd1c07b810241e7d4a5214ccd077c30348dcb7/reviews/c79c4062-1531-457e-9b7e-79049cd55942/result.json`
