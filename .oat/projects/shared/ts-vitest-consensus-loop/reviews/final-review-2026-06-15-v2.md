---
oat_generated: true
oat_generated_at: 2026-06-15
oat_review_scope: final
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/ts-vitest-consensus-loop
---

# Code Review: final (second opinion)

**Reviewed:** 2026-06-15
**Scope:** final — full branch range `71a10e9a751f5bf4072af667482754a2b4531aa7..HEAD` on `feat/ts-vitest-consensus-loop`
**Files reviewed:** 38 changed (focus: build/runtime contract, generated output, CI/worktree validation, TypeScript loop source, wrapper compatibility)
**Commits:** 22 commits in range
**Workflow mode:** quick (no `spec.md` / `design.md`; evidence = `discovery.md`, `plan.md`, `implementation.md`, `state.md`)
**Invocation:** manual second-opinion review (independent of the prior auto `final-review-2026-06-15.md`)

## Summary

This is an independent, fresh-context final review that re-derived its conclusions by reading the diff and running the full verification suite rather than trusting the prior auto review. Every critical invariant — generated/source parity, dependency-free shipped runtime, wrapper compatibility, intact runtime validation at untrusted JSON boundaries, lint/format/CI exclusion sync, and CI/worktree command ordering — holds. All gates pass cleanly (type-check, build:check, build-diff, 529 Node + 3 Vitest tests, validate, smoke, `git diff --check`, clean tree). The deferred-findings ledger is confirmed genuinely empty. No new findings; my conclusions concur with the prior auto review, reached independently.

## Findings

### Critical

None

### Important

None

### Medium

None

### Minor

None

## Independent Invariant Verification

Each invariant below was checked directly against source/diff, not inferred from artifacts.

1. **Generated output is in sync with canonical source — VERIFIED.**
   - `pnpm run build:check` → `consensus-loop: in sync`.
   - `pnpm run build && git diff --exit-code -- plugins/consensus/skills/refine/scripts/consensus-loop.mjs` → no diff (rebuilt output is byte-identical to committed).
   - Generated `.mjs` carries the banner `// GENERATED by scripts/build-generated.mjs. Do not edit directly.` (`plugins/consensus/skills/refine/scripts/consensus-loop.mjs:1-2`). No evidence of hand-editing; rebuild reproduces the committed bytes exactly.

2. **Shipped runtime stays dependency-free — VERIFIED.**
   - Generated `.mjs` imports only `node:` stdlib (`node:child_process`, `node:crypto`, `node:fs/promises`, `node:path`, `node:url`); grep for any non-`node:` import returns NONE.
   - `package.json` has no `dependencies` block at all; TypeScript, Vitest, esbuild, oxlint/oxfmt are all `devDependencies` (`package.json:29-39`).

3. **Wrapper compatibility preserved — VERIFIED.**
   - Exported-name set of the migrated generated `.mjs` is byte-for-byte identical to the pre-migration `.mjs` (39 exports, set diff empty). esbuild emits a consolidated `export { ... }` block (`consensus-loop.mjs:2403-2442`); names match the base commit one-for-one.
   - CLI direct-exec guard preserved identically: `process.argv[1] && path.resolve(process.argv[1]) === fileURLToPath(import.meta.url)` (`consensus-loop.mjs:2396`).
   - Schema path resolution preserved as relative `../schemas/...` from the generated file's `scripts/` location, which is correct because the generated output stays in the original directory.
   - `tests/sequential-wrapper.test.mjs:136-150` locks `consensus-refine.mjs` to importing `./consensus-loop.mjs` AND asserts the GENERATED banner.

4. **Runtime validation intact at untrusted JSON boundaries — VERIFIED.**
   - Byte-cap constants are byte-for-byte identical to the base commit (`VERDICT_CAPS` / `SYNTHESIS_CAPS`: reasoning 16 KiB, proposed_artifact 256 KiB, total_verdict 512 KiB, max_disagreements 20, etc.) (`src/consensus-loop.ts:322-335`).
   - The `as` casts at the JSON boundaries are correctly guarded: `validateSynthesisCaps` (`ts:949-952`) and `validateVerdictCaps` (`ts:1012-1018`) each run `validate*Shape` and early-return on failure *before* narrowing with `as`. The peer-invocation path normalizes then calls `validateVerdictShape` and throws `INVALID_VERDICT_SHAPE` before using the value (`ts:1978-1992`). Types complement validation; no real runtime check was dropped behind a cast.

5. **Lint/format/CI exclusions consistent — VERIFIED.**
   - `.oxfmtrc.json:25`, `.oxlintrc.json:34`, `.lintstagedrc.mjs:17-19`, and the CI workflow regexes (`.github/workflows/validate.yml:77,84`) all exclude `plugins/consensus/skills/refine/scripts/consensus-loop.mjs`. All four locations are in sync.

6. **CI + worktree validation run the full ordered gate and assert a clean tree — VERIFIED.**
   - CI (`validate.yml:24-32`): install → build → verify-committed-diff → type-check → build:check → test → validate → smoke.
   - Worktree (`scripts/worktree/validate.sh:31-42`): assert-clean → install → build → assert-clean → type-check → build:check → test → validate → smoke → final build:check → assert-clean.
   - `tests/validate-script.test.mjs:153-181` order-asserts both sequences, so drift in command ordering is itself test-guarded.

## Verification Commands (actual observed results)

```bash
pnpm run type-check                                  # PASS (tsc --noEmit, exit 0)
pnpm run build:check                                 # PASS ("consensus-loop: in sync")
pnpm run build && git diff --exit-code -- \
  plugins/consensus/skills/refine/scripts/consensus-loop.mjs  # PASS (no diff)
pnpm test                                            # PASS (529 Node tests, 0 fail; 2 Vitest files / 3 tests pass)
pnpm run validate                                    # PASS ("validation passed")
pnpm run smoke                                       # PASS ("smoke passed")
git diff --check 71a10e9..HEAD                       # PASS (no whitespace errors)
git status --short                                   # clean (empty)
```

Note on the documented `tests/session-observer/watch.test.mjs` timing flake: it did **not** reproduce in this run — the full `pnpm test` passed all 529 Node tests on the first attempt, including the watcher test. No flake observed; nothing to attribute to this migration.

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `plan.md`, `implementation.md`, `state.md`, prior reviews (`p01-review-2026-06-15-r2.md`, `p02-review-2026-06-15.md`, `p03-review-2026-06-15.md`, `final-review-2026-06-15.md`), and the changed files in the full branch range.

Design artifact alignment: not applicable (quick mode; no `spec.md`/`design.md` present for this mode).

### Discovery Success Criteria Coverage

| Criterion | Status | Notes |
| --------- | ------ | ----- |
| TS/Vitest/build tooling installed dev-only and documented | implemented | `package.json:29-39` devDeps only; documented in README, AGENTS, decision-record DR-020 |
| `pnpm test` runs Node + Vitest without reducing coverage | implemented | 46 `.test.mjs` total; node runs 45, the 1 excluded (`generated-output-sync.test.mjs`) runs under Vitest; 1 `.test.ts` runs under Vitest. No test dropped from both runners; no double-run. 529 Node tests (up from 526 baseline). |
| Build script generates `consensus-loop.mjs` from TS source | implemented | `scripts/build-generated.mjs` + esbuild, write/`--check` modes, banner |
| Drift guard fails on source/output mismatch | implemented | `tests/generated-output-sync.test.mjs` invokes `--check`; verified in suite |
| Real TS source + useful domain types | implemented | `src/consensus-loop.ts` (3554 lines), discriminated unions for verdicts/synthesis/records/escalation/peer boundaries; `tsc --noEmit` strict passes |
| `validate` and `smoke` still pass | implemented | both pass |
| CI builds, type-checks, tests, validates, smokes | implemented | `validate.yml:24-32` |
| Docs/decision records explain canonical-TS + committed `.mjs` contract | implemented | DR-020 in decision-record; AGENTS / README updates |

### Extra Work (not in declared requirements)

- `premerge` convenience script (`package.json:14`) composing the same gate sequence. Defensible: CI and `worktree:validate` keep their command order explicit and independent of it; this is a thin convenience alias, not a coverage substitute. Not scope creep.
- `tests/package-metadata.test.mjs` and `tests/validate-script.test.mjs` additions were pulled into-scope to lock the new `test` composition and the validation command order. Both are directly implied by the relevant tasks and recorded as deltas in `implementation.md`. Not unrequested scope.

## Deferred Findings Ledger Disposition

**Empty — confirmed, not assumed.** I verified each prior cycle closed its own findings:

- p01 first review: 1 Important (`@types/node` floor vs Node 22) + 1 Minor. Fixed in `5658e63` (`@types/node` pinned to `^22.19.21`, verified in `package.json:32`); p01 re-review (`p01-review-2026-06-15-r2.md`) shows Critical/Important/Medium/Minor all "None".
- p02 review: 0 findings.
- p03 review: 1 Minor (`oat_last_commit` lag), bookkeeping-only, resolved by `059273a`.
- Prior auto final review (`final-review-2026-06-15.md`): all severities "None"; 1 Minor stale-plan-sentence note resolved in bookkeeping.

Deferred Medium = 0, Deferred Minor = 0. No carry-forward debt to disposition.

## Notable Independent Observations (vs prior auto review)

- I reached the same pass conclusion **independently**: rebuilt the generated output and byte-compared it to the committed file, byte-compared the export-name set against the base commit, byte-compared the cap constants against the base commit, and confirmed each `as` cast is shape-validated first. These are stronger parity checks than restating the prior review.
- The documented session-observer flake did **not** recur in this run; the full suite was green on first attempt. This strengthens (does not weaken) the prior conclusion.
- No divergence from the prior auto review. No artifact drift found: `state.md`, `implementation.md`, and `plan.md` Reviews table are consistent with the shipped code and with each other.

## Recommended Next Step

Run the `oat-project-review-receive` skill to record this passing second-opinion review. There are no findings to convert into plan tasks; the project remains ready for HiLL approval and PR handoff.
