# Plan Gate Review: wave-1-execution (2026-07-23)

- **Reviewer:** Codex (cross-runtime, read-only artifact gate; background dispatch)
- **Scope:** wrapper artifacts only (`plan.md`, `state.md`, `discovery.md`, `orchestration-log.md`); external plans immutable inputs
- **Raw findings:** 1 Critical, 7 Important, 1 Minor — verdict `FIXES_NEEDED`
- **Disposition round:** all findings dispositioned below; fixes landed in the same commit as this artifact
- **Terminal status:** `passed` (all fix dispositions carry verification records; rejections carry stated reasons)

## Dispositions

| # | Sev | Finding (short) | Disposition | Verification record |
|---|-----|-----------------|-------------|---------------------|
| 1 | Critical | p01 drift-addendum + reconciliation modify the immutable source plan | **Rejected (reasoned)** | The `oat-wave-execute` contract mandates exactly this: a drift-check coverage gap "gets a wrapper rule-1 addendum" (in-worktree pathspec EXTENSION — non-narrowing), and stale-premise reconciliations are "recorded exactly once in the Drift Refresh Record with pointer-only references elsewhere." The addendum widens the drift check; nothing is waived. Residual duplication accepted as fix: p01 Step 1 reference made pointer-only ("see wrapper execution contract rule 1"). Verified: `grep -c 'rule-1 addendum' plan.md` → 3 (contract definition, drift-record note, pointer) with pathspec stated once. |
| 2 | Important | "One commit per source-plan step grouping" narrows commit granularity | **Fixed** | Sentence reduced to example-only. Verified: `grep -n 'per source-plan step grouping' plan.md` → no matches. |
| 3 | Important | p02 restates DR-check/STOP from source plan | **Fixed** | Step 2 reduced to "Execute the source plan in full." Verified by grep: `decision-record intent check` no longer in plan.md. |
| 4 | Important | p03 restates virtual-clock restriction | **Fixed** | Sentence removed. Verified: `virtual-clock` absent from plan.md phase sections (remains only in source plan). |
| 5 | Important | p04 restates conditional AGENTS.md outcome | **Fixed** | Parenthetical removed; Step 2 is pointer-only. Verified: `ARCH-07 rationale` absent from plan.md. |
| 6 | Important | DoD gates omit `smoke`; canonical `premerge` exists | **Fixed** | DoD line now `pnpm run premerge && pnpm run validate:skill-versions -- --base-ref main`. Verified against `package.json:13` (premerge = build + type-check + build:check + test + validate + smoke). |
| 7 | Important | discovery.md pins Codex sol/Opus, conflicting with runtime resolution | **Rejected (reasoned), clarifier added** | The record documents a verbatim OPERATOR directive; the sol xhigh gate is an operator-pinned wave-closeout gate, out of band of lane dispatch. Clarifying sentence added to discovery.md distinguishing the two. Lane dispatch remains runtime-resolved under the managed policy. |
| 8 | Important | orchestration-log oxfmt command lacks file argument | **Fixed** | Explicit file path added to the logging contract. Verified: `grep 'oxfmt --write .oat/projects' orchestration-log.md` → 1 match. |
| 9 | Important | state.md Next Milestone implies gate already passed | **Fixed** | Reworded to "After the plan gate passes: …". |
| 10 | Minor | state.md retains commented template/schema scaffolding | **Rejected (reasoned)** | The comment block is `oat project new` scaffold-owned schema documentation, standard across OAT project instances and consumed by tooling docs; removing it fights the generator. |

## Verification summary (gate → passed)

- `oat project validate-plan --project-path .oat/projects/shared/wave-1-execution` → "Plan validation passed" (re-run post-fix).
- Post-fix greps above all confirmed; frontmatter unchanged except reviewed content.
- Raw reviewer output preserved at the gate runner's output file (scratchpad `wave1-plan-gate.md`); this artifact is the durable record.
