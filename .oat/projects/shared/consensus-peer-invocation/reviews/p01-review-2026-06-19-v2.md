---
oat_generated: true
oat_generated_at: 2026-06-19
oat_review_scope: p01
oat_review_type: code
oat_project: .oat/projects/shared/consensus-peer-invocation
---

# Code Review: p01 Re-review

**Reviewed:** 2026-06-19
**Scope:** Phase 1 CLI Contract and Generated Entrypoint re-review (`575eccd..90f19d4`)
**Files reviewed:** 17
**Commits:** 7 (`575eccd..90f19d4`)

## Summary

Phase 1 now satisfies the prior blocking parser-contract finding. The `run` parser accepts runtime-policy flags and `--max-depth`, normalizes them into the request model, rejects them as `--request-json` conflicts, documents them in generated CLI help, and covers the behavior with unit and process tests. No Critical or Important source findings remain for p01; one Minor artifact-alignment note remains for a stdin prompt example that differs from the plan and implementation.

The prior implementation-tracking Medium is intentionally not counted as a code finding in this re-review per dispatch guidance; it remains orchestrator bookkeeping after code review passes.

## Findings

### Critical

None

### Important

None

### Medium

None

### Minor

- **Design stdin prompt example omits the implemented marker** (`.oat/projects/shared/consensus-peer-invocation/design.md:480`)
  - Issue: The design's stdin prompt example shows shell redirection without the `-` stdin marker, while the p01 plan explicitly asks for "prompt via stdin marker" and the implemented/generated CLI help advertises `[-|--prompt <text>|--prompt-file <path>]`. The implementation is defensible because it matches the p01 plan and process tests, but the design example can mislead later consumers of the CLI contract.
  - Suggestion: Align the design example with the accepted contract by adding the `-` marker, or explicitly revise the plan/code/tests if stdin should be implicit when no prompt source flag is present.

## Requirements/Design Alignment

**Evidence sources used:** `spec.md`, `design.md`, `plan.md`, `implementation.md`, `discovery.md`, prior review artifact `reviews/p01-review-2026-06-19.md`, and the code diff for `575eccd..90f19d4`.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| FR1 | implemented for p01 | Provider inventory, preflight, generated entrypoint, request parsing, and machine-readable usage envelopes are present. The prior missing runtime-policy/depth run flags are now accepted and tested. |
| FR2 | partial as planned | First-scope IDs and capability model are present; real adapter capability objects are p02 scope. |
| FR5 | implemented for p01 | Provider list/preflight skeleton emits machine-readable provider entries and treats provider absence as data. |
| FR6 | implemented for p01 | Provider-neutral error codes and envelope/exit-code helpers are present. |
| FR8 | implemented | Generated CLI mapping, committed `plugins/consensus/scripts/consensus.mjs`, lint/format exclusions, and generated-output drift checks are in sync. |
| FR9 | implemented for p01 | Host-native dispatch is represented as unsupported/reserved; `run --max-depth` is accepted and normalized into host context. Enforcement of recursion guard behavior remains p02 scope. |
| NFR1 | implemented | Shipped runtime remains dependency-free generated `.mjs` using Node standard library APIs. |
| NFR4 | implemented for p01 | Unit and process tests cover p01 models, parsing, envelopes, commands, generated process behavior, runtime-policy/depth flag acceptance, and generated-output drift. |
| NFR5 | implemented for p01 | Prompt input avoids argv by default; runtime-policy flags are accepted and conflict-checked; process spawning and environment allowlist enforcement remain later p02 scope. |

### Prior Important Finding

Resolved. `parseRunCommand` now allows `--permission-mode`, `--sandbox`, `--approval-policy`, `--env-allow`, and `--max-depth`; `normalizeRunRequest` maps runtime-policy flags to `ConsensusCliRunRequest.runtime_policy` and maps `--max-depth` to `host.max_depth`; request-json conflict detection includes these flags; generated help and `plugins/consensus/scripts/consensus.mjs` include the new contract; tests cover unit and process behavior.

### Extra Work (not in declared requirements)

None

## Verification Commands

Run these to verify the implementation:

```bash
pnpm exec vitest run tests/consensus/provider-cli/types.test.ts tests/consensus/provider-cli/args.test.ts tests/consensus/provider-cli/envelope.test.ts tests/consensus/provider-cli/commands.test.ts tests/consensus/provider-cli/cli-process.test.ts tests/tooling/generated-output-sync.test.ts
pnpm run build:check
pnpm run type-check
npm run validate
npm run smoke
```

Observed during re-review:

- Focused p01/generated-output suite passed: 6 files, 48 tests.
- `pnpm run build:check` passed and reported `consensus-provider-cli: in sync`.
- `pnpm run type-check`, `npm run validate`, and `npm run smoke` passed.
- Direct generated-CLI probe with the prior failing runtime-policy/depth flags no longer returns `Unknown flag`; it reaches the expected p01 placeholder `Command is not implemented yet: run`.
- Direct generated-CLI probe for `run --request-json - --max-depth 2 --json` rejects the conflict with `CONSENSUS_CLI_USAGE`, as required by the design.

## Recommended Next Step

Record this re-review as passed and let orchestration handle implementation tracking bookkeeping.
