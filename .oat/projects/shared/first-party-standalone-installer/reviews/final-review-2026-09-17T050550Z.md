---
oat_generated: true
oat_generated_at: 2026-09-17T05:05:50Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: gate
oat_project: .oat/projects/shared/first-party-standalone-installer
oat_review_head_sha: 9054500256e71ee9487f14e50f387ebdb7ea97e9
oat_review_range: 7a85fd33013191b27a6a4d2a4c3ad6affba6a47e..9054500256e71ee9487f14e50f387ebdb7ea97e9
oat_prior_review_artifact: .oat/projects/shared/first-party-standalone-installer/reviews/archived/final-review-2026-09-17T020642Z.md
oat_prior_review_head_sha: 7a85fd33013191b27a6a4d2a4c3ad6affba6a47e
oat_gate_run_id: f673a067-7275-4ef8-9a0a-55e88e880a5b
oat_gate_target: claude-fable-skip-permissions
oat_gate_runtime: claude
oat_invocation_model: fable
oat_invocation_reasoning_effort: provider-default
oat_invocation_source: exec-target-config
---

# Code Review: final

**Reviewed:** 2026-09-17T05:05:50Z
**Scope:** Final gate re-review, narrowed to the delta since the prior gate review on the same target (`7a85fd33013191b27a6a4d2a4c3ad6affba6a47e..9054500256e71ee9487f14e50f387ebdb7ea97e9`). Requirements coverage outside this range is inherited from the prior gate artifact named above.
**Files reviewed:** 8 changed files (3 product/test/config files inspected line by line: `install.sh`, `tests/tooling/standalone-installer.test.ts`, `.oxlintrc.json`; 5 project bookkeeping files inspected for scope and consistency).
**Commits:** 21 commits in range.
**Verdict:** Passed. 0 Critical, 0 Important, 0 Medium, 4 Minor (3 carry-forward, 1 new). No blocking findings.
**Reconnaissance:** not-attempted

Re-review scope: range=7a85fd33013191b27a6a4d2a4c3ad6affba6a47e..9054500256e71ee9487f14e50f387ebdb7ea97e9; classification=substantive; reason=narrowed from guarded prior reviewed head (prior gate artifact `oat_review_head_sha` and the tracked `plan.md` gate row for target `claude-fable-skip-permissions` agree; commit exists and is an ancestor of HEAD).

## Dispatch Audit

- Gate route: `inline` (runtime=claude, cliRoot=/Users/tstang/Library/pnpm/store/v11/links/@open-agent-toolkit/cli/0.2.77/d9bcf1efab193b3f50b4a9c1dd921b1d2a818293c9e44f3789f0f41ed09b7842/node_modules)
- Route reason (helper output): "Provider runtime marker matches expected runtime claude; model evidence is unavailable."
- Dispatch stamp (copied verbatim from resolver `dispatchStamp`): `Dispatch: scope=final action=review role=reviewer producer=unknown provenance=unknown model_axis=selected:fable effort_axis=not-applicable dispatch_policy=frontier dispatch_ceiling=fable target=fable`
- target: `fable` (launcher-selected/config-declared)
- model_axis: `selected:fable` (launcher-selected/config-declared)
- effort_axis: `not-applicable` (launcher-selected/config-declared)
- Inline execution guard: the resolver's exact managed Claude target (`providers.claude.dispatchArgs.model = fable`) equals the current host model (`claude-fable-5-1`), and the validated gate route helper selected `inline`. No child was launched; runtimeIdentity remains `not-reported`.
- Workflows role root: `${WORKFLOWS_AGENT_PROVIDER_ROOT}=/Users/tstang/.agents` (canonical `agents/oat-reviewer.md`, version 1.2.4); its checklist, severity model, and artifact template were applied inline.

## Summary

The delta since the prior gate contains exactly three substantive changes: the shared Node-version failure message in `install.sh` is now installer-neutral (resolving gate m3), a standalone-path regression with a mocked Node 21 proves the neutral wording and no destination mutation, and `.oxlintrc.json` excludes the `oat sync` provider skill mirrors so repository-wide lint passes. All three are correct, proportionate, and consistent with the design; the regression lives under `tests/` so the skill-version validator still reports zero changed skills. Independent execution at HEAD passed 70 focused tests, lint, format, Bash syntax, whitespace, and version checks. The three carry-forward Minor items (m1, m2, m4) are unchanged in source and their user-selected deferrals remain appropriate; one new Minor documentation-alignment item is recorded.

Findings: 0 critical, 0 important, 0 medium, 4 minor

## Findings

### Critical

None

### Important

None

### Medium

None

### Minor

Carry-forward IDs preserve the prior gate's numbering. Resolved m3 and m5 are not renumbered or re-deferred.

- **m1 (carry-forward): Direct helper invocation through a symlinked directory silently exits 0** (`scripts/install-standalone.mjs:317`)
  - Issue: Unchanged since the prior gate. The CLI guard compares an unresolved `argv[1]` with the realpathed module URL, so direct `node` invocation via a symlinked checkout returns without output. The documented `install.sh` route resolves physically (`install.sh:75`) and is unaffected.
  - Disposition: Retain the user's explicit deferral (revisit only if direct helper execution becomes a supported interface).
  - Suggestion: Guarded `realpathSync(process.argv[1])` comparison, or an explicit `install.sh:` error when the guard fails.

- **m2 (carry-forward): Global/system Git configuration isolation is not explained in the installation guide** (`scripts/install-standalone.mjs:93`)
  - Issue: Unchanged since the prior gate. Proxy, private-CA, and `insteadOf` settings that exist only in Git config do not apply to the tag fetch. The isolation is deliberate and `--repository` supports a local mirror.
  - Disposition: Retain the user's explicit deferral (next installation-guide polish or an affected user report).
  - Suggestion: One sentence in the first-party installer section stating the boundary and the local-mirror workaround.

- **m4 (carry-forward): 60-second Git timeout surfaces as an opaque error** (`scripts/install-standalone.mjs:111`)
  - Issue: Unchanged since the prior gate. A killed subprocess reports the raw command failure with no timeout wording. Acquisition precedes destination reservation, so no partial install results.
  - Disposition: Retain the user's explicit deferral (improve diagnostics if a timeout is observed or support demand appears).
  - Suggestion: Detect `error.killed`/`error.signal` and report the timeout explicitly.

- **m6 (new): Repository lint/format guidance and format config do not reflect the new provider-mirror lint exclusions** (`AGENTS.md:83`, `.oxfmtrc.json:16`)
  - Issue: Commit `bbe37778` adds `.claude/skills/**` and `.cursor/skills/**` to `.oxlintrc.json` ignore patterns. The root `AGENTS.md` Linting section still enumerates only `.agents/**`, `.claude/rules/**`, and `.cursor/rules/**` as never-lint/format paths, and `.oxfmtrc.json` did not receive the parallel globs. Practical impact is nil today: the 72 tracked `.claude/skills/` entries are extension-less symlinks into `.agents/skills/` (already ignored by both configs, lint-staged, and the CI changed-file filters), `.cursor/skills/` is absent, and a scoped `oxfmt --check` on a mirror entry passes. This is documentation/config-symmetry drift, not a defect in the lint change itself.
  - Disposition: Artifact/documentation alignment; the shipped lint config is the defensible source of truth.
  - Suggestion: Add `.claude/skills/**` and `.cursor/skills/**` to the `AGENTS.md` never-lint list and to `.oxfmtrc.json` `ignorePatterns` during the next tooling or docs touch. Not required for closeout.

## Deferred Findings Disposition

| Prior finding | Sources | Disposition | Current evidence |
| --- | --- | --- | --- |
| p01/final M1: ambiguous bootstrap tag selection | `reviews/p01-review-2026-09-17T012147Z.md`; `reviews/archived/final-review-2026-09-17T012706Z.md` | Resolved (p02-t01); inherited from prior gate | Outside the narrowed range; unchanged since `7a85fd33`. Release-contract collision regression passed in this run. |
| Gate m1, m2, m4 | `reviews/archived/final-review-2026-09-17T020642Z.md`; `implementation.md` deferred ledger | Retain explicit user deferrals | `scripts/install-standalone.mjs` is not in the narrowed diff; dispositions re-stated above with the user's selected triggers. |
| Gate m3: Consensus-specific Node error | same | Resolved by p03-t01 (`266dd3c3`), retained through p03-t02 (`3d9da99d`) | `install.sh:68` now reads "required to run this installer"; regression at `tests/tooling/standalone-installer.test.ts:107` asserts the neutral message, rejects `/consensus/i`, and verifies untouched project/HOME roots. Passed in this run. |
| Gate m5: nonexistent quick-mode spec reference | same | Resolved | `implementation.md` References now records `Spec: N/A (quick mode)`. |
| p03 M1: regression placed under distributed source | `reviews/archived/p03-review-2026-09-17T044145Z.md` | Resolved by p03-t02 | `src/plugins/consensus/install-sh.test.ts` is not in the narrowed diff (net cancellation); `validate:skill-versions --base-ref origin/main` reports 0 changed skills at HEAD. |

Deferred Medium count: **0**. Deferred Minor count at review start: **3** (m1, m2, m4), all re-evaluated and retained. New Minor: **1** (m6). No finding is silently deferred.

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md` (Phase 3 tasks and Reviews ledger), `implementation.md` (p03 records, deferred ledger, final-verification resolution), `state.md`, the prior gate artifact, the p03 and post-p03 final lifecycle reviews, root `AGENTS.md`, `.lintstagedrc.mjs`, `.github/workflows/validate.yml`, and the full narrowed diff. Quick mode: `spec.md` is absent by design and not treated as missing.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------------------------------- | ------- |
| D1 Zero-argument Consensus path unchanged | implemented (re-verified) | Only `require_node_22`'s message string changed (`install.sh:68`); `main`, pins, overrides, and checksum flow are byte-identical. Legacy `install-sh` and `install-contract` suites pass. |
| Neutral Node-version requirement for both entry paths (p03-t01) | implemented (re-verified) | Shared check keeps the Node 22 floor and `fail` exit; wording no longer names Consensus. Regression exercises the real shell entrypoint with standalone flags. |
| Regression placement outside distributed source (p03-t02) | implemented (re-verified) | Test lives in `tests/tooling/`; Consensus test path absent from the narrowed diff; 0 changed skills. |
| Provider-mirror lint exclusion (operator-authorized closeout correction) | implemented (re-verified) | Exactly two ignore globs added; no rule, severity, or override changed; repository-wide `pnpm lint` passes with the same four pre-existing warnings in authored source. See m6 for doc symmetry. |
| D2–D12 (scope flags, exact tag, generated payload boundary, host/scope mapping, refusal/integrity, injection seam, GIT scrub, authenticity wording, live-acceptance boundary) | inherited | Not touched by the narrowed range; coverage inherited from `reviews/archived/final-review-2026-09-17T020642Z.md` at `7a85fd33`. `scripts/install-standalone.mjs`, `installation.md`, `RELEASING.md`, and the release-contract test are unchanged since that head. |
| Constraint: no runtime dependencies; Node ≥ 22 | inherited + re-verified | No dependency manifests in the narrowed diff. |
| Constraint: no skill version bump | implemented (re-verified) | `validate:skill-versions -- --base-ref origin/main`: 0 changed skills. |

### Extra Work (not in declared requirements)

None of product scope. The `.oxlintrc.json` change is operator-authorized closeout tooling recorded in `implementation.md` (Final Verification Resolution). All other changed files in range are OAT project bookkeeping (implementation, plan, state, project log, and the archived prior gate artifact).

### Design drift

No new drift. The previously recorded test-placement deviation (standalone tests under `tests/`) is now also the resolution of p03 M1 and is documented in `implementation.md` Deviations; the plan's original file lists for p01-t01/p03-t01 remain the stale side and do not mislead.

## Verification Commands

All executed independently at `9054500256e71ee9487f14e50f387ebdb7ea97e9` with a clean worktree:

```bash
pnpm run test:vitest tests/tooling/standalone-installer.test.ts tests/release/standalone-install-contract.test.ts src/plugins/consensus/install-sh.test.ts src/plugins/consensus/install-contract.test.ts tests/tooling/generated-output-sync.test.ts   # 70 passed / 5 files
pnpm exec oxlint tests/tooling/standalone-installer.test.ts scripts/install-standalone.mjs   # clean, exit 0
pnpm lint                                                                                    # passes; 4 pre-existing warnings, 0 errors
pnpm exec oxfmt --check tests/tooling/standalone-installer.test.ts .oxlintrc.json            # clean
bash -n install.sh
git diff --check 7a85fd33013191b27a6a4d2a4c3ad6affba6a47e..HEAD
pnpm run validate:skill-versions -- --base-ref origin/main                                   # 0 changed skills
```

Additional probes: structural diff of `.oxlintrc.json` against `bbe37778^` (two added globs only); inventory of tracked `.claude/skills/` entries (72 symlinks into `.agents/skills/`); `.cursor/skills/` absent; scoped `oxfmt --check .claude/skills/analyze` passes; CI `validate.yml` and `.lintstagedrc.mjs` exclusion filters read for m6. The full `premerge`, docs production build, and PJM doctor runs recorded in `implementation.md` were not repeated; the narrowed range touches no generated outputs or documentation.

Artifact hygiene: the repository's file-scoped formatter (`pnpm exec oxfmt --write`) excludes `.oat/**`, so this artifact and the `plan.md` ledger row were checked directly for frontmatter, severity headings, the single reconnaissance signal, and whitespace.

## Recommended Next Step

Run `oat-project-review-receive` to record the passing gate result. The gate threshold (`important`) is not reached, so this review does not block implementation exit. Preserve the m1/m2/m4 deferrals with their existing triggers; m6 is a documentation/config alignment item suitable for judgment-sweep deferral or a small fix in the next tooling touch. Live host/scope acceptance remains a separately authorized release step.
