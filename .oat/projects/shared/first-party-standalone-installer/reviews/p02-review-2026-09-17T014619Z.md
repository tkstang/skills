---
oat_generated: true
oat_generated_at: 2026-09-17T01:46:19Z
oat_review_scope: p02
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/first-party-standalone-installer
oat_review_head_sha: f0879afabb0556f836eae1849edafd3393dd90e7
---

# Code Review: p02

**Reviewed:** 2026-09-17T01:46:19Z
**Scope:** Task `p02-t01`, the documented bootstrap tag correction and its offline regression.
**Files reviewed:** 2 changed files, with supporting project artifacts and repository instructions.
**Commits:** `e2fe6c4106e2949b2ebefe1816cdae1622bc7b6c..f0879afabb0556f836eae1849edafd3393dd90e7` (one task commit).
**Verdict:** Passed. Prior final-review M1 is resolved within this phase's scope.
**Reconnaissance:** not-attempted

## Summary

The guide now fetches the fully qualified release tag and checks out its peeled commit before selecting the installer. The new regression verifies commit identity and installer bytes for a divergent same-named branch and annotated tag; an independent execution of the actual documented commands also confirms the fix, with the prior command selecting the branch as a negative control. The change is confined to the two planned files and adds no runtime or installation lifecycle machinery.

Findings: 0 critical, 0 important, 0 medium, 0 minor

## Findings

### Critical

None.

### Important

None.

### Medium

None.

### Minor

None.

## Requirements/Design Alignment

**Evidence sources used:** Complete `discovery.md`, `design.md`, `plan.md`, `implementation.md`, and `state.md`; prior finding M1 in `reviews/archived/final-review-2026-09-17T012706Z.md`; root, documentation, and test instructions; `package.json`; and the authoritative commit diff and both changed files. `spec.md` is absent by design in quick mode. Tracking artifacts still show the task in progress pending root reconciliation; those unchanged files are context, not changes under review.

### Requirements Coverage

| Requirement | Status | Evidence |
| --- | --- | --- |
| Fetch an unambiguous release tag into a fresh checkout | Implemented | `documentation/docs/user-guide/installation.md:127` creates the checkout, and line 130 fetches `refs/tags/v0.1.2` with `--no-tags --depth=1`. |
| Select the tag's commit before using its installer | Implemented | `documentation/docs/user-guide/installation.md:131` detaches at `FETCH_HEAD^{commit}` before assigning `INSTALLER` on line 132. This handles annotated tags without branch-name resolution. |
| Prove selection despite a same-named divergent branch | Implemented | `tests/release/standalone-install-contract.test.ts:78` creates the annotated tag, line 85 creates a same-named branch with different content, and lines 109-113 assert both checkout HEAD and installer bytes. |
| Protect the documented command from the original regression | Implemented | Exact command assertions at `tests/release/standalone-install-contract.test.ts:63` bind the fixture procedure to the current guide. Restoring the old clone command would fail these assertions; the independent old-command control also selected the wrong commit and bytes. |
| Preserve offline, bounded verification and honest release claims | Implemented | The regression uses a temporary local repository, reads rather than executes its installer, and cleans only its own temporary root. The existing planned-release and live-acceptance caveats remain unchanged. |

The implementation matches the relevant exact-tag and source-identity decisions. The single collision regression is proportionate to the observed defect and exercises Git behavior rather than only mirroring prose. No requirements outside `p02-t01` were re-reviewed or inherited as newly verified claims.

### Extra Work (not in declared requirements)

None. The authoritative range contains only `fix(installer): pin documented bootstrap tag`, changing the guide and its release-contract test as planned.

## Verification Results

Independently executed against `f0879afabb0556f836eae1849edafd3393dd90e7`:

- Focused release-contract suite: **3 tests passed**. The runner used an isolated temporary `HOME`, disabled global/system Git configuration, an empty Git template directory, and file-only Git transport.
- Independent local fixture: extracted the actual current and prior guide bootstrap blocks, replaced only the repository URL with a temporary local source, and stopped before the `INSTALLER` assignment. The current block selected the annotated tag's exact commit and installer bytes; the prior block selected the distinct branch commit and bytes. No fixture installer was executed.
- Changed-test oxlint and changed-file oxfmt checks: passed.
- Authoritative-range whitespace check: passed.
- Range inventory: exactly the two declared task files and the expected single commit. The worktree was clean before writing this artifact.

No network, real-home installation, provider execution, implementation edit, or tracking edit was performed. Full repository and documentation builds were not repeated for this bounded review; its verdict is based on the focused checks and direct command reproduction above.

Artifact hygiene uses the repository's file-scoped formatter command. The `.oat/**` exclusion prevents formatting this review artifact, so its frontmatter, severity counts, single reconnaissance signal, and whitespace are checked directly.

## Verification Commands

```bash
pnpm run test:vitest tests/release/standalone-install-contract.test.ts
pnpm exec oxlint tests/release/standalone-install-contract.test.ts
pnpm exec oxfmt --check documentation/docs/user-guide/installation.md tests/release/standalone-install-contract.test.ts
git diff --check e2fe6c4106e2949b2ebefe1816cdae1622bc7b6c..f0879afabb0556f836eae1849edafd3393dd90e7
```

Run the fixture suite with a temporary home and local-only Git configuration when reproducing the review's isolation conditions.

## Recommended Next Step

Return the passing phase review to the root workflow for tracking reconciliation and the required final lifecycle re-review. No fix task is needed from this phase review; use `oat-project-review-receive` when receiving the final review result.
