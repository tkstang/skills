---
oat_generated: true
oat_generated_at: 2026-09-17T01:27:06Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: auto
oat_project: .oat/projects/shared/first-party-standalone-installer
oat_review_head_sha: cfe7cc01ee606523e2d14ffbea0fbcfd08124168
---

# Code Review: final

**Reviewed:** 2026-09-17T01:30:49Z
**Scope:** Final quick-project review of p01-t01, p01-t02, p01-t03, authorized recovery, and lifecycle bookkeeping.
**Files reviewed:** 16 changed files, plus supporting requirements, instructions, and compatibility evidence.
**Commits:** `08f5945946995b06873697fe8bf16714c664aa8b..cfe7cc01ee606523e2d14ffbea0fbcfd08124168` (25 commits).
**Verdict:** Changes requested. One unresolved Medium finding must be fixed before final pass.
**Reconnaissance:** not-attempted

## Summary

The installer follows the scoped, dependency-free design: it acquires an exact tagged generated payload, refuses existing destinations, writes files exclusively, and verifies bytes and executable modes before reporting success. Independent focused tests, build freshness, skill-version validation, and static checks pass; however, the documented bootstrap still selects a same-named branch ahead of the promised release tag. Deferred phase finding M1 is confirmed and dispositioned **fix-now**, while separately authorized live acceptance correctly remains pending.

Findings: 0 critical, 0 important, 1 medium, 0 minor

## Findings

### Critical

None.

### Important

None.

### Medium

- **M1: Pin the bootstrap checkout to the fully qualified release tag** (`documentation/docs/user-guide/installation.md:127`)
  - Issue: `git clone --branch v0.1.2 --single-branch ...` can select a branch named `v0.1.2` instead of the tag. The following commands execute that checkout's installer, so the helper's later exact-tag payload acquisition cannot establish the identity of code that has already run. This contradicts the preceding instruction to use the release's checkout and leaves the first-party procedure incompletely pinned.
  - Evidence: An independently constructed temporary local Git repository with an annotated `v0.1.2` tag and a different same-named branch reproduced the issue. The documented clone selected the branch commit and `branch installer` bytes, not the tag commit and `tag installer` bytes. A control using an explicit `refs/tags/v0.1.2` fetch and detached checkout of `FETCH_HEAD^{commit}` selected the tag commit and tag bytes. Neither installer was executed; the reproduction used no network and cleaned up only its own temporary fixture.
  - Fix: Change the guide's bootstrap to initialize a fresh checkout, fetch `refs/tags/v0.1.2` explicitly with `--no-tags --depth=1`, and detach at the peeled fetched commit before assigning `INSTALLER` or invoking it. Add a local regression exercising the documented bootstrap with a same-named divergent branch and annotated tag, asserting both checkout identity and installer bytes. The existing helper collision test at `tests/tooling/standalone-installer.test.ts:262` covers payload acquisition, while the documentation contract at `tests/release/standalone-install-contract.test.ts:16` only extracts final installer invocations and cannot catch the bootstrap defect.
  - Requirement: Discovery decisions 3 and 11 and p01-t02's pinned first-party procedure. This is a documentation/bootstrap defect; the helper's fully qualified fetch at `scripts/install-standalone.mjs:150` is correct.
  - Disposition: **Fix-now**. This carries forward phase-review M1 rather than introducing a second finding for the same defect.

### Minor

None.

## Deferred Medium Disposition

| Prior finding | Source | Disposition | Reason |
| --- | --- | --- | --- |
| M1 | `reviews/p01-review-2026-09-17T012147Z.md` | Fix-now; unresolved | Reproduced at the final reviewed head. Bootstrap identity is part of the pinned installation procedure, and correction requires only a bounded documentation/test change. The planned-release caveat does not make selection of the wrong installer acceptable. |

No Medium finding is accepted for continued deferral. Final receive must create a fix task and retain the final review as requiring fixes until the correction is verified.

## Requirements/Design Alignment

**Evidence sources used:** Complete `discovery.md`, `design.md`, `plan.md`, `implementation.md`, `state.md`, `project-log.md`, the prior p01 review, backlog item `BL-260916-add-a-first-party-install.md`, and its kickoff handoff. Supporting evidence includes root/source/test/documentation/PJM instructions, the generated-runtime architecture contract, `package.json`, the authoritative diff and commit history, installer source, and the two new test suites. `spec.md` is absent by design in quick mode; no required quick-mode artifact is missing. All coverage below was independently assessed in this final pass.

### Requirements Coverage

| Requirement | Status | Evidence |
| --- | --- | --- |
| Preserve zero-argument Consensus behavior | Implemented | The shell delta at `install.sh:120` only dispatches argument-bearing calls. Original Consensus test files have no net change against the final base; both focused compatibility suites pass. |
| Explicit skill, host, scope, and exact tag | Implemented | `scripts/install-standalone.mjs:45` validates inputs; parser at line 76 rejects unknown, duplicate, and incomplete flags. Required/invalid argument tests pass. |
| Generated payload only, exact-tag acquisition, no selected-source execution | Implemented | `scripts/install-standalone.mjs:143` uses a private bare repository, qualified tag fetch, peeled commit, tree/blob reads, regular-file modes, and required `SKILL.md`. Missing-tag, branch-only, source-only, symlink, gitlink, reserved-marker, and tag/branch collision tests pass. |
| Confine Git routing and inherited configuration | Implemented | Every Git subprocess passes through `scripts/install-standalone.mjs:93`, removing inherited `GIT_*`; initialization disables templates. The decoy environment test confirms the intended source and unchanged decoy refs/index/config. |
| Project and user placement for all three hosts | Implemented | Host map at `scripts/install-standalone.mjs:22`, physical root resolution at line 257, provider-only parent creation, and invocation output at line 308. All six host/scope fixture cases pass with a temporary HOME. |
| Refuse symlinked provider ancestors and every existing final destination | Implemented | `ensureDirectory` uses `lstat` at `scripts/install-standalone.mjs:201`; exclusive final `mkdir` at line 265 refuses existing files/directories/symlinks, and `wx` prevents payload overwrite. Existing-byte-preservation and ancestor-symlink tests pass. |
| Complete paths, bytes, and executable-mode fidelity | Implemented | Buffer reads and SHA-256 fingerprints at `scripts/install-standalone.mjs:182`, descriptor writes/modes at line 212, and deterministic destination inventory comparison before marker removal at line 296. Binary/resource/runtime fixtures verify the installed bytes and modes. |
| Retain failed new installations for deliberate recovery | Implemented | Marker precedes payload writes; failure at `scripts/install-standalone.mjs:302` reports the retained path without deleting it. The injected mid-copy failure leaves the marker and copied file; a retry refuses the existing destination. |
| Pinned first-party guide beside Skills CLI | Partial | Six explicit host/scope commands, release caveat, refusal/recovery guidance, and fidelity limits are present. M1 leaves the bootstrap itself ambiguous. |
| Honest release and backlog acceptance | Implemented | `RELEASING.md:67` separates automated fidelity from six live host/scope checks. Backlog evidence keeps those checks pending, the item active, and the kickoff handoff present. No release or fresh-provider verification is claimed. |
| No generated payload changes or unrelated skill version bumps | Implemented | No net canonical skill/shared runtime/generated installation payload changes. Independent build freshness passes; final-base version validation reports zero changed skills. |

### Design and task boundaries

The helper follows the selected private-bare-repository, inventory, exclusive-publication, and retained-failure design. It adds no package dependency, source build, receipt lifecycle, cross-provider mirror, automatic dependency installation, or environment-driven failure protocol.

The feature and documentation commits retain their planned boundaries. Authorized recovery `372a69c0a2eeb7a2b83a0c39441af6a7862fdb3e` relocates standalone assertions to `tests/` and restores the original Consensus test files. `implementation.md` explicitly records this justified deviation and its version-gate rationale; it does not warrant restoring the earlier test placement or bumping unrelated skill versions. Historical planning instructions should be read with that recorded recovery, not treated as an implementation defect.

The earlier phase is recorded as passed under its stated phase threshold while M1 remains visible in the implementation ledger. This final review applies the stricter final-pass requirement and does not inherit a clean verdict from the phase bookkeeping.

### Extra Work (not in declared requirements)

No significant product scope expansion. The initial OAT sync commit upgrades `.oat/sync/manifest.json` from schema 1/OAT 0.2.27 to schema 2/OAT 0.2.77 and adds an empty `collections` array. Structural comparison confirms all 82 provider entries are unchanged; the large textual delta is primarily property order. The remaining non-product changes are project planning, review, authorized recovery, and acceptance bookkeeping.

## Verification Results

Independently executed at `cfe7cc01ee606523e2d14ffbea0fbcfd08124168`:

- Focused standalone, release-contract, legacy installer, installation-contract, and README suites: **65 tests passed across 5 files**.
- `pnpm run build:check`: passed without repairing tracked output.
- `pnpm run validate:skill-versions -- --base-ref 08f5945946995b06873697fe8bf16714c664aa8b`: passed with **zero changed skills**.
- Changed implementation/test lint and authored implementation/test/docs format checks: passed.
- Bash syntax and authoritative-range whitespace checks: passed.
- Local bootstrap collision reproduction: confirmed M1; fully qualified tag control selected the correct commit and bytes.
- Original Consensus tests have no net change; OAT manifest provider entries are structurally identical across the range.

The implementation artifact reports the full premerge run with 2,029 passing tests and one skipped live test, documentation build, and PJM doctor. Those broader runs were read as recorded evidence and were not independently repeated here. The worktree was clean before creating this artifact. No network, live provider, real-home mutation, push, publication, release, backlog archival, handoff deletion, or implementation change was performed during this review.

Review-artifact hygiene: the file-scoped `oxfmt --write` invocation was attempted, but the repository configuration excludes `.oat/**` and reported no target files. No usable formatting override is documented for these artifacts: `no format command discovered in repo instructions; skipping`. Required frontmatter, severity sections/counts, provenance, the single reconnaissance signal, and whitespace were checked directly.

## Verification Commands

After adding the bootstrap regression that fails on the current documented command, run:

```bash
pnpm run test:vitest tests/tooling/standalone-installer.test.ts tests/release/standalone-install-contract.test.ts src/plugins/consensus/install-sh.test.ts src/plugins/consensus/install-contract.test.ts tests/repo/readme-scope.test.ts
pnpm run build:check
pnpm run validate:skill-versions -- --base-ref 08f5945946995b06873697fe8bf16714c664aa8b
bash -n install.sh
git diff --check 08f5945946995b06873697fe8bf16714c664aa8b..HEAD
```

Format and lint the changed authored documentation and regression test with the repository's file-scoped commands. Rebuild documentation if the guide changes, inspecting any generated drift.

## Recommended Next Step

Run `oat-project-review-receive` to create a bounded fix task for M1, implement the bootstrap correction with a local regression, and obtain a clean final re-review. Final pass is blocked by this unresolved Medium finding. Keep live host/scope acceptance and real user-home installation pending their separate authorization.
