---
oat_generated: true
oat_generated_at: 2026-09-13T17:22:48Z
oat_review_scope: p01
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/skill-source-organization
oat_review_head_sha: 737e7c06041f7344bf8eeed0cfbc4b79877c72f8
oat_review_full_range: 348d46caead591060ba00581dd6add22654120c8..737e7c06041f7344bf8eeed0cfbc4b79877c72f8
---

# Code Review: p01

**Reviewed:** 2026-09-13T17:22:48Z
**Scope:** Phase p01 implementation, `348d46caead591060ba00581dd6add22654120c8..737e7c06041f7344bf8eeed0cfbc4b79877c72f8`
**Files reviewed:** 16
**Commits:** 9 commits in the authoritative range

## Summary

The second bounded fix closes all three cycle-2 findings: replacement rejects symlinked output ancestors, legacy files and declared trees publish through one rollback-capable transaction with staged/source revalidation, and raw `.`/`..` declaration segments are rejected. The earlier containment, multi-unit rollback, and formatter-inventory fixes also remain closed, and every required p01 verification gate passes. One Important gap remains in the read-only freshness path: a declared output symlinked to a matching external tree is followed and reported as in sync, so p01 remains below its zero-Critical/zero-Important pass threshold after the final permitted automatic review cycle.

Findings: 0 critical, 1 important, 0 medium, 0 minor

## Dispatch Audit

- Target: `oat-reviewer-gpt-5-6-sol-high`
- Model axis: `selected:gpt-5.6-sol`
- Effort axis: `selected:high`
- Policy / ceiling: `high` / `high`
- Selection reason: fresh project-managed High reviewer for the final permitted p01 review cycle
- Dispatch: `Dispatch: scope=p01 action=review role=reviewer producer=unknown provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:high dispatch_policy=high dispatch_ceiling=high target=oat-reviewer-gpt-5-6-sol-high`

## Review Orchestration

| Wave | Task class | Classification rationale | Selected target | Acceptance / outcome | Floor satisfaction | Fallback |
| --- | --- | --- | --- | --- | --- | --- |
| 1, containment lane | intelligent-recon | Symlink and traversal auditing has silent-miss risk and requires semantic path analysis. | `explorer` / `gpt-5.6-terra` high | accepted; completed with two candidate gaps | satisfied | none |
| 1, transaction lane | intelligent-recon | Cross-output rollback and revalidation require semantic control-flow tracing. | `explorer` / `gpt-5.6-terra` high | accepted; completed with one evidence-gap candidate | satisfied | none |

**Primary reconciliation:** Reopened every cited implementation and test path, reran the full required p01 gate set, and independently reproduced the output-symlink freshness false pass in isolated temporary directories. Accepted the output-check candidate; rejected a missing direct source-drift regression and rollback-failure regression as findings because the implementation paths are present and the absence of an additional automated test alone does not establish a defect. The contained source-ancestor alias candidate is included in the fix guidance because the design declares symlinks unsupported unless explicitly allowed, but it does not independently raise severity.

## Findings

### Critical

None

### Important

- **Declared-output freshness follows symlinks and can certify an external tree as in sync** (`scripts/lib/packaging.ts:801`)
  - Issue: `checkDeclaredDistributions` passes the declared output directly to `inventoryTree`, while `walkRegularFiles` checks only descendants and never `lstat`s or containment-checks the output root. An isolated probe built a valid `skills/example` unit, copied its exact payload to an external temporary directory, replaced `skills/example` with a symlink to that directory, and received `[]` from `checkDeclaredDistributions`. The same gap applies when an output ancestor is symlinked. Consequently `build:check` can claim freshness even though the repository contains no owned generated tree and installation or publication may ship an unsupported link instead of the declared payload. `resolveInputRoot` at line 290 also rejects only a final source-root symlink, allowing an undeclared symlink in the source ancestor chain when it resolves inside the repository; this weaker manifestation violates the same explicit symlink contract even though realpath containment prevents an external source escape.
  - Fix: Add a read-only root validator that `lstat`s the declared output itself, walks every existing repository-relative ancestor, rejects any symlink, and requires each resolved segment to remain inside the real repository root before inventorying. Apply the same segment validation to source and allowed-source roots, or explicitly define and test any supported internal-symlink contract. Add negative controls for direct and ancestor declared-output symlinks pointing to same-content external trees and for a source-root ancestor symlink; each freshness case must return a failure rather than an empty result.
  - Requirement: p01-t02 unsupported-symlink rejection and trustworthy freshness; discovery line 63, design lines 69-71 and 128, plan line 114.

### Medium

None

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md`, `implementation.md`, `state.md`, `migration-inventory.md`, both prior p01 reviews, and all 16 files in the authoritative full range. This is a quick-mode project; `spec.md` is intentionally absent.

### Prior Finding Re-Review

| Finding | Status | Evidence |
| --- | --- | --- |
| Cycle-2 output-ancestor symlink replacement containment | closed | `validateMutationPath` checks ancestors before backup, after parent creation, before installation, and during rollback/cleanup; standalone and plugin fixtures assert that external targets remain unchanged. The Important finding above is a distinct read-only freshness path. |
| Cycle-2 unified atomic publish and immediate consistency validation | closed | `writeGenerated` stages legacy outputs and passes them with declared trees to one `writeStagedOutputs` call. The mixed failure regression fails after a legacy file and one declared tree publish, then confirms all three prior outputs restore. Staged hashes/inventories and legacy/declared source fingerprints are revalidated before mutation. |
| Cycle-2 raw traversal rejection | closed | `assertRelativePath` rejects raw `.` and `..` segments before normalization; the non-escaping traversal negative control passes. |
| Cycle-1 arbitrary output/plugin/source-root containment | closed | Standalone/plugin outputs are derived from validated identifiers; direct source and allowed-root symlinks and external realpaths are rejected. |
| Cycle-1 multi-unit rollback | closed | All prior outputs move to retained backups before any staged installation; later failure removes installed replacements and restores every moved backup in reverse order. |
| Cycle-1 formatter inventory omissions | closed | The p01-t02 inventory includes `tests/tooling/vitest-config.test.ts`; p01-t03 includes `scripts/lib/packaging.ts`; the changed authored paths pass file-scoped formatting checks. |

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| p01-t01 verified migration inventory | implemented | Baseline ownership, names, versions, source/resources/tests, output targets, and formatter path contracts remain complete; the prior omissions stay corrected. |
| p01-t02 declared generation, safe publication, and freshness | partial | Declaration validation, staging, source fingerprinting, unified legacy/tree transaction, rollback and recovery reporting, and write-side containment work. Read-only freshness still follows symlinked output roots or ancestors and can falsely pass an external tree. |
| p01-t03 representative installed-boundary proof | implemented | Prompt-only, shared-runtime executable, standalone consensus, and complete-plugin fixtures run outside the checkout with isolated environment and inventory checks. |

### Extra Work (not in declared requirements)

None. The task commits match the declared file boundaries, the two bounded fix commits stay within the packaging builder/helper and focused regressions, and review bookkeeping is isolated in separate commits.

### Saturation Diagnostic

The known whole-suite session-observer help timeout under host saturation is outside the required p01 gate. Its affected file passes 49/49 alone, and no reviewed p01 behavior depends on treating the unconstrained diagnostic as acceptance evidence.

## Verification Commands

Run these after fixing the remaining finding:

```bash
pnpm exec vitest run tests/tooling/generated-output-sync.test.ts tests/tooling/skill-packaging.test.ts tests/tooling/vitest-config.test.ts tests/consensus/install-contract.test.ts --maxWorkers=4
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run smoke
pnpm exec oxfmt --check package.json scripts/build-generated.ts scripts/lib/packaging.ts src/distributions.ts tests/tooling/generated-output-sync.test.ts tests/tooling/skill-packaging.test.ts tests/tooling/vitest-config.test.ts vitest.config.mjs .oat/projects/shared/skill-source-organization/migration-inventory.md
git diff --check 348d46caead591060ba00581dd6add22654120c8..737e7c06041f7344bf8eeed0cfbc4b79877c72f8
```

Independent checks performed for this review: the required four-file suite passed 59/59; `pnpm run type-check`, `pnpm run build:check`, `pnpm run validate`, `pnpm run smoke`, changed-file `oxfmt --check`, and full-range `git diff --check` passed. Two bounded read-only lanes also passed 55 focused tests independently. Temporary-directory probes reproduced the declared-output freshness false pass and the accepted internal source-ancestor symlink.

## Recommended Next Step

Run the `oat-project-review-receive` skill to record the unresolved Important finding and its manual disposition. Governance review cycle 3 is final; do not launch an automatic fourth review cycle.
