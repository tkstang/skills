---
oat_generated: true
oat_generated_at: 2026-09-13T20:26:00Z
oat_review_scope: p02
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/skill-source-organization
oat_review_head_sha: a2014e9b8641cf0af03fe89eb634494614874260
oat_review_full_range: 4c51573706198f385c6578c42a43430f89563049..a2014e9b8641cf0af03fe89eb634494614874260
---

# Code Review: p02

**Reviewed:** 2026-09-13T20:26:00Z
**Scope:** Phase p02 implementation, `4c51573706198f385c6578c42a43430f89563049..a2014e9b8641cf0af03fe89eb634494614874260`
**Files reviewed:** 462
**Commits:** 7 commits in the authoritative range

## Summary

The canonical-source migration preserves every baseline source/test identity, removes the old source trees, keeps generated payloads fresh, and passes the complete 1,975-test suite plus the type, build, validation, smoke, and version gates. Three Important callsite/reference gaps remain: tag releases omit the now-required target selector, the live-E2E package script points to a deleted test path, and canonical shipped references still tell operators to run deleted source/test paths. The implementation's handoff test split is defensible and fully discovered, but the migration inventory still records the pre-split ownership and formatter list.

Findings: 0 critical, 3 important, 1 medium, 0 minor

## Dispatch Audit

- Target: `oat-reviewer-gpt-5-6-sol-high`
- Model axis: `selected:gpt-5.6-sol`
- Effort axis: `selected:high`
- Policy / ceiling: `high` / `high`
- Selection reason: project-managed High reviewer for a load-bearing canonical-source and version-authority migration
- Dispatch: `Dispatch: scope=p02 action=review role=reviewer producer=unknown provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:high dispatch_policy=high dispatch_ceiling=high target=oat-reviewer-gpt-5-6-sol-high`

## Review Orchestration

| Wave | Task class | Classification rationale | Selected target | Acceptance / outcome | Floor satisfaction | Fallback |
| --- | --- | --- | --- | --- | --- | --- |
| 1, owner/test lane | mechanical-recon | Baseline-to-head owner and test identity is a deterministic inventory and discovery check. | `explorer` / `gpt-5.6-luna` high | accepted; completed with two candidate gaps | satisfied | none |
| 2, version/backlog lane | intelligent-recon | Version authority, release isolation, and backlog lifecycle require semantic contract interpretation. | `explorer` / `gpt-5.6-terra` high | accepted; completed with one functional and one deferred-documentation candidate | satisfied | none |
| 2, build/CI lane | intelligent-recon | Generated-output derivation and CI ordering can fail silently across several consumers. | none | launch rejected before start because the agent thread limit was reached; covered inline | unsatisfied for launch; satisfied by primary review | caller-inline |

**Primary reconciliation:** Independently reopened every promoted source and artifact location, reproduced the tag-release and live-E2E failures, confirmed all cited legacy paths are absent, and checked the migrated replacement paths. Accepted the two functional callsite failures, the shipped-reference drift, and the inventory alignment gap. Deferred the root AGENTS/RELEASING prose drift because p04 explicitly owns maintained documentation. The conservative declaration-derived version closure was not promoted: every consensus distribution declares the plugin runtime as an allowed runtime root, so broad bump propagation is safe and consistent with the current packaging boundary even when a narrower import-only interpretation could reduce bumps.

## Findings

### Critical

None

### Important

- **Tag releases omit the required plugin target** (`.github/workflows/release.yml:39`)
  - Issue: `bump-version.ts` now requires exactly one `--plugin` or `--skill` target, but the release workflow still invokes `pnpm tsx scripts/bump-version.ts --check-tag "$GITHUB_REF_NAME"` without either selector. The command exits 1 with `choose exactly one version target with --plugin or --skill`, so every tag-triggered release stops after the preceding checks instead of validating and publishing the selected plugin. Adding `--plugin consensus` makes the current `v0.1.0` check pass.
  - Fix: Pass the intended target explicitly, currently `--plugin consensus`, or introduce an explicit tag-to-target matrix before the session plugin ships. Add a workflow/callsite contract test that executes the exact target-scoped check so the CLI and workflow cannot drift again.
  - Requirement: p02-t03 explicit target-scoped plugin release selection; p02-t04 affected release callers.

- **The live-E2E package command points to a deleted test path** (`package.json:12`)
  - Issue: `test:live-e2e` still selects `tests/consensus/provider-cli/e2e/submit-live.e2e.test.ts`, while p02 moved the suite to `src/plugins/consensus/provider-cli/e2e/submit-live.e2e.test.ts`. Running the package command exits 1 with `No test files found` before it can exercise any provider, and `.github/workflows/live-e2e.yml` delegates to this broken script.
  - Fix: Update the script to the colocated `src/plugins/consensus/provider-cli/e2e/submit-live.e2e.test.ts` path and add a package-script selection test or other path-existence guard. Verify discovery without credentials first; keep the authenticated live-provider run opt-in.
  - Requirement: p02-t02 moved plugin-level runtime/tests; p02-t04 affected compatibility callers and preserved live workflow.

- **Canonical shipped references still direct operators to deleted paths** (`src/skills/evaluate/references/operator-qa.md:37`)
  - Issue: The moved Evaluate operator checklist runs three deleted `tests/consensus/evaluate/*` files. The moved collaboration runtime references likewise cite deleted `tests/session-observer*` and `tests/transcript-core` paths (`runtime-claude-code.md:143`, `runtime-codex.md:204`, and `runtime-cursor.md:130-131`), while the observer/export transcript references identify deleted `src/transcript/core/runtimes.ts` as canonical (`session-observer/references/transcript-formats.md:3,491` and `session-export-transcript/references/transcript-formats.md:5,95`). Direct execution of the Evaluate command exits 1 with `No test files found`. These are canonical skill resources copied into generated payloads, so the stale paths ship to users and undermine the acceptance evidence they describe.
  - Fix: Rewrite each command/source pointer to its `src/skills`, `src/plugins/consensus`, or `src/shared/transcript` destination, search all p02 canonical sources for the deleted roots, and regenerate the declared outputs. Add a lightweight canonical-resource path check where the text is intended to be executable repository guidance.
  - Requirement: p02-t01/p02-t02 affected resource paths and p02-t04 compatibility caller cleanup.

### Medium

- **The migration inventory assigns five tooling tests to the distributable handoff skill** (`.oat/projects/shared/skill-source-organization/migration-inventory.md:56`)
  - Issue: The implementation correctly retains five experimental behavior/CLI/handoff/provider/reconcile tests beside `src/tools/coding-session-handoff`, while eight product-guidance tests live under `src/skills/session-fork-to-destination/src`; all 13 identities are discovered. The inventory and its p02-t01 formatter list instead place all handoff tests under the skill owner, including nonexistent skill paths at lines 120-142. This conflicts with the plan's explicit retained-tooling boundary and leaves the artifact used to reconcile moved test identity and formatting inaccurate.
  - Fix: Split the handoff inventory row between the distributable skill and retained tooling owner. Replace the five nonexistent skill test entries in the p02-t01 formatter list with their actual `src/tools/coding-session-handoff/*.test.ts` paths while preserving the eight skill-owned tests.

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md`, `implementation.md`, `state.md`, `migration-inventory.md`, `.oat/repo/pjm/AGENTS.md`, the archived BL-260723 record and completed backlog index, and all 462 files in the authoritative range. This is a quick-mode project; `spec.md` is intentionally absent.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| p02-t01 standalone owner/test colocation | partial | All source and test identities moved and remain discovered, the old roots are gone, and the narrow authored collab boundary is documented. Canonical references and the handoff inventory/formatter list retain deleted or incorrect paths. |
| p02-t02 consensus owner/plugin/shared-runtime colocation | partial | Owners, plugin runtime, tests, manifests, and generated payloads are colocated and behavior passes. The live-E2E package selector and Evaluate operator checklist retain deleted test paths. |
| p02-t03 canonical discovery/version authority | partial | Authored discovery, sole stable `metadata.version`, legacy owner baseline, local edit states, ownerless-output checks, independent targets, and BL-260723 lifecycle pass. The release workflow does not supply the required target selector. |
| p02-t04 tooling/bridge cleanup | partial | The old mapping bridge is removed, declared output roots drive dynamic consumers, static exclusions have parity guards, and CI base/head/freshness logic passes. Two affected callers and several canonical reference paths were not migrated. |

### Extra Work (not in declared requirements)

None. The four planned task commits, the bounded p02-t02 recovery, bookkeeping reconciliation, and final stale-reference recovery form a linear seven-commit range and remain within the declared migration and recovery contract.

## Verification Commands

Run these after fixing the findings:

```bash
pnpm run test
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run validate:internal-flags
pnpm run smoke
pnpm run validate:skill-versions -- --base-ref origin/main
pnpm tsx scripts/bump-version.ts --check-tag v0.1.0 --plugin consensus
pnpm exec vitest run src/plugins/consensus/provider-cli/e2e/submit-live.e2e.test.ts
git diff --check 4c51573706198f385c6578c42a43430f89563049..a2014e9b8641cf0af03fe89eb634494614874260
```

Independent checks performed for this review: the full suite passed 1,974 tests with one skipped across 135 passing files and one skipped file; the focused release/tooling suite passed 107 tests; `type-check`, `build:check`, `validate`, `validate:internal-flags`, `smoke`, `validate:skill-versions` for 12 changed skills, and full-range `git diff --check` passed. The unscoped tag check and current `test:live-e2e` package script both failed as described, while the explicit consensus tag check passed. Moved test identity and Vitest discovery were reconciled against the baseline and migration inventory.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert these findings into bounded plan tasks before continuing the project workflow.
