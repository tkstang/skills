---
oat_generated: true
oat_generated_at: 2026-09-13T16:59:21Z
oat_review_scope: p01
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/skill-source-organization
oat_review_head_sha: c14f9d524554f49f01080f3e9502696b9b3a19a3
oat_review_full_range: 348d46caead591060ba00581dd6add22654120c8..c14f9d524554f49f01080f3e9502696b9b3a19a3
---

# Code Review: p01

**Reviewed:** 2026-09-13T16:59:21Z
**Scope:** Phase p01 implementation, `348d46caead591060ba00581dd6add22654120c8..c14f9d524554f49f01080f3e9502696b9b3a19a3`
**Files reviewed:** 15
**Commits:** 7 commits in the authoritative range

## Summary

The bounded fix closes the prior review's four named containment negatives, restores every prior declared unit after a later installation failure, and corrects both formatter inventory omissions. The broader phase still has two Important safety gaps: output operations can follow a symlinked parent outside the repository, and the top-level build does not commit legacy outputs and declared units through one validated transaction. Phase p01 therefore remains below its zero-Critical/zero-Important pass threshold.

Findings: 0 critical, 2 important, 1 medium, 0 minor

## Dispatch Audit

- Target: `oat-reviewer-gpt-5-6-sol-high`
- Model axis: `selected:gpt-5.6-sol`
- Effort axis: `selected:high`
- Policy / ceiling: `high` / `high`
- Selection reason: fresh project-managed High reviewer required after a bounded fix iteration
- Dispatch: `Dispatch: scope=p01 action=review role=reviewer producer=unknown provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:high dispatch_policy=high dispatch_ceiling=high target=oat-reviewer-gpt-5-6-sol-high`

## Findings

### Critical

None

### Important

- **A symlinked output ancestor redirects the replacement transaction outside the repository** (`scripts/lib/packaging.ts:734`)
  - Issue: `declaredOutput` now constrains the lexical target path, but `writeDeclaredDistributions` resolves that path and immediately creates or renames beneath its parent without checking the existing ancestor chain. A read-only adversarial fixture set `plugins/demo` to a symlink targeting an external temporary directory; the accepted `plugins/demo/skills/example` declaration then installed `SKILL.md` under that external directory and returned successfully. The four named negatives from the prior review now pass, but this remaining output-side symlink escape still violates the declared containment and no-unsupported-symlink contract.
  - Fix: Before any `mkdir`, backup, or install operation, resolve the nearest existing ancestor for every output and backup path, reject symlinks in the repository-relative ancestor chain, and require the resolved ancestor to remain inside the real repository root. Recheck at the mutation boundary and add standalone/plugin fixtures whose `skills` or `plugins/<name>` ancestor is a symlink outside the repository; assert that no external path is created or changed.
  - Requirement: p01-t02 output containment and symlink rejection; discovery line 63; design lines 69-71; plan line 114.

- **The top-level build can still exit after partially publishing a mixed generation** (`scripts/build-generated.ts:701`)
  - Issue: `writeGenerated` builds the declared units, then writes every legacy generated file directly at lines 702-711, and only afterward starts the rollback-capable declared-unit transaction at line 714. A later declared replacement failure restores declared units but leaves already-written legacy outputs at the new source state. In addition, `writeDeclaredDistributions` installs each `stagedPath` without comparing its current inventory to the recorded validated inventory; an adversarial fixture changed staged `SKILL.md` after `buildDeclaredDistributions` returned, and the writer installed the changed file successfully while retaining the original recorded hash. The new multi-unit regression proves atomic rollback inside the declared-unit subset, but it does not establish the plan's whole-build stage/validate/replace boundary.
  - Fix: Build all legacy mappings and declared units without writing owned outputs, revalidate every staged payload and relevant source fingerprint immediately before mutation, then publish both file outputs and declared trees under one rollback-capable transaction. Add an integration regression at the `runBuildGenerated`/`writeGenerated` boundary that injects a failure after at least one legacy output and one declared unit would otherwise publish, and assert that every prior output inventory remains unchanged. Add a staged-inventory drift negative that fails before moving any output.
  - Requirement: p01-t02 whole-build safe replacement and input consistency; discovery line 63; design lines 49 and 71; plan line 114.

### Medium

- **Normalized traversal segments remain valid declaration inputs** (`scripts/lib/packaging.ts:99`)
  - Issue: `assertRelativePath` rejects only paths whose normalized value escapes the root. It accepts a declaration source such as `src/skills/one/../two`, which was confirmed by a direct validation probe. The path remains repository-contained, but it can select a different authored owner than the declaration text suggests and conflicts with p01-t02's explicit rejection of traversing sources.
  - Fix: Reject any raw `.` or `..` path segment before normalization for declaration sources, allowed source roots, and runtime entrypoints. Add a negative case for a non-escaping traversal such as `src/skills/one/../two`.
  - Requirement: p01-t02 traversing-source rejection; plan line 114.

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md`, `implementation.md`, `state.md`, `migration-inventory.md`, the prior p01 review, and the authoritative changed files in `348d46caead591060ba00581dd6add22654120c8..c14f9d524554f49f01080f3e9502696b9b3a19a3`. This is a quick-mode project; `spec.md` is intentionally absent.

### Prior Finding Re-Review

| Prior finding | Status | Evidence |
| --- | --- | --- |
| Declared output/source-root containment | Partially closed | The exact standalone-output, traversing-plugin-identifier, source-root-symlink, and allowed-root-symlink negatives pass and the implementation rejects them. Output ancestor symlinks and non-escaping traversal remain open above. |
| Whole-build atomic multi-unit replacement | Partially closed | The new later-unit failure regression restores both prior declared inventories with no recovery directory. The top-level builder still writes legacy outputs outside that transaction and does not revalidate staged inventories before publication. |
| Formatter inventory omissions | Closed | `tests/tooling/vitest-config.test.ts` is listed for p01-t02 and `scripts/lib/packaging.ts` is listed for p01-t03. The eight changed authored paths pass file-scoped `oxfmt --check`. |

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| p01-t01 verified migration inventory | implemented | Baseline ownership/output evidence remains present and both prior formatter-list omissions are corrected. |
| p01-t02 declared installation-unit generation and safe replacement | partial | Declaration rendering, staging, bundling, inventory comparison, input fingerprints, and declared-unit rollback work, but output-ancestor containment and the whole-build validated transaction remain incomplete. |
| p01-t03 representative installed-boundary execution tests | implemented | Prompt-only, executable/shared-runtime, standalone consensus, and complete-plugin fixtures run outside the checkout; 54 scoped tests pass. |

### Extra Work (not in declared requirements)

None. The fix commit is limited to the packaging helper, its focused suite, and the migration inventory correction.

### Full-Suite Saturation Check

The bounded full suite completed with 1,951 passed, 1 skipped, and the already documented `tests/session-observer/cli.test.ts` `review --help` subprocess timeout as its sole failure under saturation. The exact failing case passed alone with one worker. This is not a p01 regression and does not change the findings above.

## Verification Commands

Run these after fixing the findings:

```bash
pnpm exec vitest run tests/tooling/generated-output-sync.test.ts tests/tooling/skill-packaging.test.ts tests/tooling/vitest-config.test.ts tests/consensus/install-contract.test.ts --maxWorkers=4
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run smoke
pnpm run test -- --maxWorkers=4
git diff --check 348d46caead591060ba00581dd6add22654120c8..c14f9d524554f49f01080f3e9502696b9b3a19a3
```

Independent checks performed for this re-review: the four-file scoped suite passed 54/54; `pnpm run type-check`, `pnpm run build:check`, `pnpm run validate`, `pnpm run smoke`, changed-file `oxfmt --check`, and range `git diff --check` passed. The isolated saturated full-suite test passed alone. Separate temporary-directory probes reproduced the output-parent symlink escape, staged-inventory drift, and accepted non-escaping source traversal.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into bounded plan tasks.
