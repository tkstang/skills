---
oat_generated: true
oat_generated_at: 2026-09-14T00:26:32Z
oat_review_scope: p04
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/skill-source-organization
oat_review_head_sha: 0055176770dc0e869fa952978faf5832cba7c1c4
oat_review_range: 20bb893ef6bcdd30704c681e12c60702b7c89bb8..0055176770dc0e869fa952978faf5832cba7c1c4
---

# Code Review: p04

**Reviewed:** 2026-09-14T00:26:32Z
**Scope:** Independent publication gate for the effective p01-p04 public migration delta, `20bb893ef6bcdd30704c681e12c60702b7c89bb8..0055176770dc0e869fa952978faf5832cba7c1c4`; p04 phase base `1ab02c9f2631172935b7b0f75926704ff793d5c7`
**Files reviewed:** 577 changed paths, plus the governing quick-mode artifacts and active maintained instruction surfaces
**Commits:** 53
**Reconnaissance:** attempted
**Disposition:** fail; address the Important and Medium findings before publication

## Summary

The canonical `src/skills`, `src/plugins`, and `src/shared` ownership model, the 18 declared installation targets, clean-break generated outputs, session and consensus manifests, dependency guards, skill versions, complete generated payloads, and local behavioral evidence pass independent verification. The recorded absence of live/paid provider evidence is an honest release limitation and does not violate the plan.

Publication should wait because active contributor/release instructions still point to deleted pre-migration owners, and `plan.md` contradicts the settled p04 completion ledger. A separate freshness-helper defect makes `checkGenerated({ repoRoot, declarations })` inspect the checkout instead of its supplied fixture root, leaving a declared-output test seam unreliable.

Findings: 0 critical, 2 important, 1 medium, 0 minor

## Review Orchestration

| Wave | Task class | Classification rationale | Selected target | Acceptance / outcome | Floor satisfaction | Fallback |
| --- | --- | --- | --- | --- | --- | --- |
| mechanical-gate | mechanical-recon | Parity, inventory, absence, and command results have visible failures and are directly repeatable. | `explorer` / `gpt-5.6-luna`, high, read-only recon | Accepted; completed. Reported two stale-maintenance candidate gaps and otherwise green deterministic checks. | satisfied | none |
| semantic-docs-ledger | intelligent-recon | Documentation and ledger consistency carry silent-miss risk across several artifacts. | `explorer` / `gpt-5.6-terra`, high, read-only recon | Blocked before start by the native agent-thread limit. | satisfied by selected target; not launched | caller-inline |

**Primary reconciliation:** The reviewer reopened every load-bearing source cited by the completed lane, repeated its searches and decisive checks, rejected a false obsolete-output lead after verifying the final tree, and covered documentation, ledger, packaging-code, and requirements alignment inline. Provider guidance was review-required by date; current repository policy, the live native catalog, and the supplied managed High ceiling independently supported both selected targets. No worker assigned severity or wrote an output artifact.

## Findings

### Critical

None.

### Important

- **Active maintenance and release instructions still point to deleted owners** (`CONTRIBUTING.md:25`)
  - Issue: The p04 documentation migration is incomplete. `CONTRIBUTING.md:25` directs maintainers to the deleted `scripts/validate.mjs`; `RELEASING.md:21` and `.github/workflows/live-e2e.yml:3` cite the deleted `tests/consensus/provider-cli/e2e/submit-live.e2e.test.ts`; `plugins/consensus/AGENTS.md:7-20` says the plugin ships only seven skills and directs agents to deleted `src/consensus` owners; `tests/AGENTS.md:10-40` directs new tests into deleted pre-colocation directories; and `shared/transcript-core/README.md:3-31` names deleted source, generator, and standalone output paths. The passing docs test actively requires one obsolete path at `tests/repo/docs-presence.test.ts:575`. Provider manifest descriptions such as `.claude-plugin/marketplace.json:11` and `plugins/consensus/.codex-plugin/plugin.json:4-19` also omit the two observer skills now shipped by consensus. These are current contributor, scoped-agent, release, and compatibility surfaces, so following them sends work to nonexistent owners or produces unusable release evidence.
  - Fix: Update the active instructions and manifests to the final `src/skills`, `src/plugins/consensus`, `src/shared/transcript`, colocated-test, `scripts/*.ts`, and `src/plugins/consensus/provider-cli/e2e` paths; describe all nine consensus skills where the manifest summarizes inventory. Change the docs-presence assertion to require current paths and reject obsolete active maintenance paths. Preserve historical old names only in explicitly historical records and the version-baseline map.
  - Requirement: Discovery clean-break/maintained-reference success criteria; plan p02-t01/p02-t02/p02-t04 and p04-t01 documentation contracts.

- **The plan's implementation ledger contradicts the settled p04 state** (`.oat/projects/shared/skill-source-organization/plan.md:374`)
  - Issue: `plan.md:369-376` still reports p04 as 0/2 and the project as 11/14, while `implementation.md:17-24` and `state.md:65-67` report p04 as 2/2 and the public milestone as 13/14. The final recovery history itself is consistent: p04 usage is 2 with no pending marker, and commits `ff615c3f...`, `08db238e...`, and `00551767...` preserve the reservation, fix, and settlement sequence. The stale plan row nevertheless conflicts with that authoritative completion record and makes the publication handoff unauditable.
  - Fix: Align the preserved plan progress table with the settled 13/14 state and p04's 2/2 implementation completion while leaving p05 pending. Re-run the project artifact formatter and verify the review/recovery rows still preserve their historical dispositions.
  - Requirement: Plan p04-t02 public-milestone bookkeeping and review-readiness contract.

### Medium

- **Custom-root generated-output checks inspect the real checkout's declared outputs** (`scripts/build-generated.ts:512`)
  - Issue: `checkGenerated()` resolves `const root = options.repoRoot ?? repoRoot` and builds declarations under `root`, but passes the module-global `repoRoot` into `checkDeclaredDistributions` at line 512. A temporary fixture that successfully ran `writeGenerated({ repoRoot: fixture, declarations: [demo] })` then failed `checkGenerated` with `skills/demo: missing output`, because the check looked in this checkout. The existing custom-root test at `tests/tooling/generated-output-sync.test.ts:385` supplies `declarations: []`, so it cannot expose the defect. The normal repository `pnpm run build:check` path is unaffected because both roots are the same, but the exported fixture/programmatic contract is unreliable.
  - Fix: Pass `{ repoRoot: root, built: declared }` to `checkDeclaredDistributions` and add a nonempty-declaration custom-root regression that writes a fixture distribution, checks it cleanly, and detects stale/missing/orphaned declared content within that fixture.
  - Requirement: Design minimum proof for freshness and representative outside-checkout installation boundaries; plan p01-t02/p01-t03.

### Minor

None.

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md`, `implementation.md`, `state.md`, `migration-inventory.md`, `validation.md`, `project-log.md`, the latest p02/p03 review artifacts, the authoritative Git range, canonical and generated skill/plugin sources, provider/marketplace manifests, active repository instructions, maintained docs, release workflows, and focused/full local verification. `spec.md` is absent by design in quick mode.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| One canonical authored owner per skill | implemented | Thirteen canonical owners are under `src/skills`; plugin-level and shared code are under `src/plugins` and `src/shared`; generated payloads are derivative. Active maintenance instructions still need the Important alignment fix above. |
| Declared standalone/plugin packaging | partial | All 18 declared targets exist, are fresh, contain no shipped TS/dev dependencies, and pass isolated execution. The default checkout check works; the custom-root declared-output check has the Medium defect above. |
| Clean-break session renames | implemented | Final product outputs contain no `skills/export-session-transcript`, `skills/coding-session-handoff`, legacy session-plugin target, alias, redirect, or wrapper. Historical version mapping remains non-user-facing. |
| Session and consensus plugin manifests | implemented | Both plugins are declared across Claude, Codex, Cursor, and marketplace surfaces with independent versions and valid target inventories. Descriptive consensus metadata needs the Important documentation alignment fix above. |
| Shared runtime and workflow dependency semantics | implemented | Shared runtime is bundled into installation units; observer-collab declares and checks the real observer prerequisite across standalone/plugin forms; handoff integrations remain optional; no auto-install or sibling runtime dependency is introduced. |
| Skill and plugin version contracts | implemented | Twelve changed canonical skills pass strict base-relative version validation; `metadata.version` is sole authored authority; plugin release targets remain independent; the transitive shared-consumer backlog item is closed. |
| p04 maintained documentation and release guidance | partial | The main docs site, README, changelog, installation, engineering, and release-limitation narratives are accurate, but several active scoped and release surfaces retain deleted paths and incomplete consensus inventory. |
| Public milestone validation evidence | partial | Full suite, focused packaging/docs/manifests, type-check, freshness, repository validation, smoke, internal flags, whitespace, and docs build pass. The validation ledger should not treat the custom-root helper seam as proven until fixed. |
| Complexity-review conclusion | implemented | One catalog, one build/version pipeline, and the existing Vitest infrastructure remain. Generated duplication is required for self-contained installation units; no second builder, updater, registry, or test framework was found. |
| Live/provider publication readiness | limited by design | No live/paid provider gate, marketplace publication, global install, or private p05 work ran. Artifacts and manifests label those gaps as unverified rather than passed. |

### Extra Work (not in declared requirements)

None. The retained `src/tools/coding-session-handoff` experiment remains a separate repository tool and does not produce a legacy public skill output.

## Dispatch Audit

Request `c5b9abd3-e471-4b12-a08e-95a8261437a9` used the supplied project-managed High reviewer target:

`Dispatch: scope=p04 action=review role=reviewer producer=unknown provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:high dispatch_policy=high dispatch_ceiling=high target=oat-reviewer-gpt-5-6-sol-high`

## Verification Commands

```bash
pnpm run validate:skill-versions -- --base-ref 20bb893ef6bcdd30704c681e12c60702b7c89bb8
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm exec vitest run tests/tooling/skill-packaging.test.ts tests/repo/layout.test.ts tests/repo/plugin-manifests.test.ts tests/repo/marketplace-manifests.test.ts tests/release/versioning.test.ts tests/release/skill-version-bumps.test.ts tests/repo/docs-presence.test.ts tests/repo/readme-scope.test.ts
pnpm run test
pnpm run smoke
pnpm run validate:internal-flags
pnpm --dir documentation run build
git diff --check 20bb893ef6bcdd30704c681e12c60702b7c89bb8..0055176770dc0e869fa952978faf5832cba7c1c4
```

Review-time results: the 12-skill version gate, type-check, generated-output freshness, repository validation, smoke, internal flags, and range whitespace checks passed. The focused suite passed 125 tests across eight files; the full suite passed 135 files and 1,987 tests with one file/test skipped; the docs build produced 38 static routes. A separate temporary-root reproduction confirmed the `checkGenerated` root-selection defect. No live provider, authentication, network, paid operation, global installation, publication, private repository, or p05 action was performed.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert these findings into bounded plan tasks. Re-run the p04 review after the fixes before publication because the reviewed basis will have changed.
