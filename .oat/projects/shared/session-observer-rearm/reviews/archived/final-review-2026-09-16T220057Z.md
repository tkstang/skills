---
oat_generated: true
oat_generated_at: 2026-09-16T22:00:57Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: gate
oat_project: .oat/projects/shared/session-observer-rearm
oat_review_head_sha: ef7d8a08d0954c0bd2b223533fdd18cf9eacbfa3
oat_gate_headless: true
oat_gate_run_id: 5d81d0bd-08cf-4353-be4c-1fba7b9c14f6
oat_gate_target: claude-fable-skip-permissions
oat_gate_runtime: claude
oat_invocation_model: fable
oat_invocation_reasoning_effort: provider-default
oat_invocation_source: exec-target-config
---

# Code Review: final

**Reviewed:** 2026-09-16T22:00:57Z
**Scope:** Full lite-project branch diff, exact range `49b4baf3c89de08ee23d100bc299f9f9a64f47be..ef7d8a08d0954c0bd2b223533fdd18cf9eacbfa3` (initial gate-lineage review; no prior `gate` final review exists for target `claude-fable-skip-permissions`, so no narrowing applied)
**Files reviewed:** 26 changed files plus the unchanged observer runtime (`watch.ts`, `observe.ts`), CLI, and generated payloads needed to validate documentation claims
**Commits:** 20

## Summary

The branch fully satisfies the lite plan contract. The new deterministic watch tests prove that an exact `codex:<session-id>` pin re-armed with `catch-up-then-watch` emits a renderable message appended while stopped, across `watch-ctl stop`, max-runtime, and spawned-CLI SIGTERM lifetimes, and they separately characterize filtered-only advancement, startup appends, rejected stdout, and both competing-consumer interleavings. No runtime source changed, which matches the plan's "fix only a demonstrated bounded defect" decision because no supported clean path lost content.

Every load-bearing claim in the rewritten Claude Code Monitor guidance was re-verified against the unchanged runtime: legacy catch-up persists `lastRecordIndex = nextIndex` before returning, signal stop breaks the loop without a flush while max-runtime flushes first, plain `watch` consumes a stored-offset gap and emits `baseline-gap`, and a losing contender restores the consumed baseline before rejection. Version closure, generated parity, PJM archive, and all non-live repository gates pass independently in this review.

Findings: 0 critical, 0 important, 0 medium, 0 minor

**Blocking verdict:** PASS — no blocking findings.

**Deferred findings:** No unresolved deferred findings carried forward (0 Medium, 0 Minor in `implementation.md` or archived review artifacts).

## Dispatch Audit

- Gate route: `inline` (runtime=claude, cliRoot=/Users/tstang/Library/pnpm/store/v11/links/@open-agent-toolkit/cli/0.2.77/d9bcf1efab193b3f50b4a9c1dd921b1d2a818293c9e44f3789f0f41ed09b7842/node_modules); route reason: "Provider runtime marker matches expected runtime claude; model evidence is unavailable."
- Configured gate invocation (immutable, copied from gate context): target `claude-fable-skip-permissions`, runtime `claude`, model `fable`, reasoning effort `provider-default`, source `exec-target-config`.
- Project managed reviewer resolution (`oat project dispatch-ceiling resolve --provider claude --role reviewer --preflight --report-scope final --report-action review --project-path <project>`): policy `high` from project state; Claude target `opus`, model axis `selected:opus`, effort axis `not-applicable`, selection branch `matrix-pinned`. The gate resolves its exec target independently; the review executed inline in the gate-configured context per the validated route.
- Dispatch stamp (copied verbatim from resolver `dispatchStamp`): `Dispatch: scope=final action=review role=reviewer producer=unknown provenance=unknown model_axis=selected:opus effort_axis=not-applicable dispatch_policy=high dispatch_ceiling=opus target=opus`
- Runtime identity: `not-reported` (no independent observation). Non-authoritative self-report: the executing host identifies as Claude Fable 5.1.
- Reconnaissance: not-attempted; the primary reviewer covered every lane inline.

## Findings

### Critical

None

### Important

None

### Medium

None

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `plan.md` (Summary, Decisions, Product Behavior, Technical Design, Assumptions, Out of Scope, Validation Criteria), `implementation.md`, `state.md`, `project-log.md`, all four archived review artifacts, the archived backlog record and PJM completed/index files, root `AGENTS.md`, the authoritative full-range diff, canonical runtime sources `src/skills/session-observer/src/lib/watch.ts` and `observe.ts`, the CLI entry `session-observer.ts`, and generated payloads. Discovery, spec, design, and import reference are absent by design for lite mode. Design alignment is not applicable as a separate artifact; the plan's Product Behavior and Technical Design sections served as the design contract.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| Product Behavior 1 / VC1: exact-pin clean re-arm after SIGTERM, `watch-ctl stop`, max-runtime | implemented | Parameterized virtual-clock lifetimes for `watch-ctl` and `max-runtime` (`src/skills/session-observer/src/watch.test.ts:531`) and a spawned-CLI SIGTERM lifetime that waits on `watch.json.active` before signalling (`src/skills/session-observer/src/watch.test.ts:648`). Each asserts raw `fromIndex`/`nextIndex`, rendered range, digest text, persisted `lastRecordIndex`, and captured stdout independently. Signal handling verified at `src/skills/session-observer/src/lib/watch.ts:1618` and loop exit at `:1727`. |
| Product Behavior 2 / VC2: honest range accounting for filtered-only records | implemented | `function_call`-only record advances persisted offset to 3 with no delta; the next renderable message renders from index 3 (`src/skills/session-observer/src/watch.test.ts:764`). Quiet-empty suppression path confirmed at `src/skills/session-observer/src/lib/watch.ts:1479`. |
| Product Behavior 3 / VC2: startup append and competing consumers | implemented | Stat-hook barrier appends during replacement startup and yields exactly one delta (`src/skills/session-observer/src/watch.test.ts:874`). Contender-first interleaving proves offset restore before rejection (`src/skills/session-observer/src/watch.test.ts:2706`; runtime `src/skills/session-observer/src/lib/watch.ts:1167`). Owner-polls-first interleaving is characterized as the CAS limitation with a later recovery re-arm (`src/skills/session-observer/src/watch.test.ts:2810`). No timing sleeps decide outcomes. |
| Product Behavior 4 / VC3: bounded delivery claim in guidance | implemented | Reference distinguishes persisted consumption, completed stdout, and unverified observing-agent delivery (`src/skills/session-observer-collab/references/runtime-claude-code.md:169`). Pre-stdout checkpoint verified at `src/skills/session-observer/src/lib/observe.ts:425` and `:1493`. Contract test pins the wording (`src/skills/session-observer-collab/src/runtime-claude-code-reference.test.ts:43`). |
| Rejected-stdout negative control | implemented | Injected sink rejects the delta after observing the chunk; persisted offset is already 3 and a subsequent exact-pin re-arm neither replays nor renders it (`src/skills/session-observer/src/watch.test.ts:956`). |
| Product Behavior 5 / AC5: portable sanitized evidence | implemented | All fixtures use synthetic paths, IDs, and messages; the reference carries only sanitized summary evidence and no longer names the ignored collaboration log or local session identifiers. |
| Decision: `catch-up-then-watch` is the supported re-arm; plain `watch` warns | implemented | Guidance at `src/skills/session-observer-collab/references/runtime-claude-code.md:146`; runtime `hasStandaloneGap` requires a stored offset and emits `baseline-gap` (`src/skills/session-observer/src/lib/watch.ts:1194`, `:427`). |
| Decision: SIGTERM does not force flush; max-runtime flushes | implemented | Stop-request break precedes any flush (`src/skills/session-observer/src/lib/watch.ts:1727`); max-runtime calls `flushPendingBeforeMaxRuntime` (`:1735`). |
| Decision: duration is session-specific, not a cap | implemented | `src/skills/session-observer-collab/references/runtime-claude-code.md:130` and evidence table row "Task lifetime"; test asserts absence of `30-minute ceiling` (`src/skills/session-observer-collab/src/runtime-claude-code-reference.test.ts:39`). No other authored skill, reference, or docs-site page carries a duration cap claim. |
| VC4: version bumps and generated parity | implemented | `session-observer` 1.0.39→1.0.40, `session-observer-collab` 1.0.28→1.0.29, transitive `session-fork-to-destination` 0.2.3→0.2.4; `validate:skill-versions` verifies three changed skills; `build:check` in sync; canonical and both generated reference copies byte-identical. |
| VC5: non-live repository gates | implemented | This review independently ran full Vitest (135 files passed, 1 skipped; 2002 tests passed, 1 skipped), `type-check`, `build:check`, `validate`, `smoke`, scoped oxlint/oxfmt on changed authored files, and `git diff --check`. All pass. |
| VC6 / Decision: conditional PJM closeout | implemented | Item closed under `archived/`, newest-first completed entry, index summary rewritten, active row removed; `oat pjm doctor --json` passes. The acceptance-criteria rewrite occurred at plan time (`7896b63b`), before implementation, and was covered by the passed plan artifact review. |
| Out of Scope boundaries | implemented | No runtime source, activity feature, messaging, N>2, Consensus cleanup, live provider run, global install, push, or PR change is present in the range. |
| Lite resumability and review coherence | implemented | `plan.md` Reviews ledger records both p01 reviews and the manual final review; `implementation.md` records 3/3 tasks, the I1 continuation, final verification, and the exit-gate launch intent; `state.md` names the exit gate as next milestone; `oat project validate-plan` and `oat project status` pass. |

### Extra Work (not in declared requirements)

None. Helper additions (`appendCodexRecord`, `parseJsonLines`, `legacySessionState`) are colocated test utilities that reduce duplication as the plan allows.

## Verification Commands

Commands run by this review, all passing:

```bash
git diff --name-status 49b4baf3c89de08ee23d100bc299f9f9a64f47be..ef7d8a08d0954c0bd2b223533fdd18cf9eacbfa3
git diff --check 49b4baf3c89de08ee23d100bc299f9f9a64f47be..HEAD
pnpm run test:vitest src/skills/session-observer/src/watch.test.ts src/skills/session-observer/src/observe.test.ts src/skills/session-observer/src/integration.test.ts src/skills/session-observer-collab/src/runtime-claude-code-reference.test.ts
pnpm run type-check
pnpm run build:check
pnpm run validate:skill-versions -- --base-ref 49b4baf3c89de08ee23d100bc299f9f9a64f47be
pnpm exec oxlint src/skills/session-observer/src/watch.test.ts src/skills/session-observer-collab/src/runtime-claude-code-reference.test.ts
pnpm exec oxfmt --check src/skills/session-observer/src/watch.test.ts src/skills/session-observer-collab/src/runtime-claude-code-reference.test.ts src/skills/session-observer/SKILL.md src/skills/session-observer-collab/SKILL.md src/skills/session-observer-collab/references/runtime-claude-code.md src/skills/session-fork-to-destination/SKILL.md
pnpm test
pnpm run validate
pnpm run smoke
oat pjm doctor --json
oat project validate-plan --project-path .oat/projects/shared/session-observer-rearm
cmp src/skills/session-observer-collab/references/runtime-claude-code.md skills/session-observer-collab/references/runtime-claude-code.md
cmp src/skills/session-observer-collab/references/runtime-claude-code.md plugins/consensus/skills/observer-collab/references/runtime-claude-code.md
```

## Recommended Next Step

Run the `oat-project-review-receive` skill to receive this passing gate review and continue the implementation exit-gate closeout.
