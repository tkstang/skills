---
oat_generated: true
oat_generated_at: 2026-09-16T21:21:06Z
oat_review_scope: p01
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/session-observer-rearm
oat_review_head_sha: 7399f07125a15cfbaec6fb397906497898b6c693
---

# Code Review: p01

**Reviewed:** 2026-09-16T21:21:06Z
**Scope:** Phase 1 implementation, exact range `73e887ac96f920c0a86089656ac09c00d40e3f9d..7399f07125a15cfbaec6fb397906497898b6c693`
**Files reviewed:** 17 changed files plus the governing lite-project and repository artifacts
**Commits:** 3 (`74a68ee26736f59d115ce5fa687b4d5cc099ca11`, `74240d787837412c4f2241dc273a9a2ae334aebc`, `7399f07125a15cfbaec6fb397906497898b6c693`)

## Summary

The implementation evidence supports the bounded conclusion: supported clean exact-pin re-arms after SIGTERM, `watch-ctl stop`, and max-runtime preserve renderable content, while filtered-only advancement, startup appends, and both competing-consumer orderings are characterized without timing-based success sleeps. The rejected-stdout case correctly remains a distinct legacy pre-stdout persistence/no-replay limitation, and the operator guidance accurately separates persisted consumption, completed stdout, and unverified observing-agent delivery.

The code, documentation, generated payloads, transitive version closure, and PJM archive satisfy the ticket, but the resumable OAT execution artifacts still report the pre-implementation state. That stale project state is an Important artifact-alignment blocker before phase closeout; no runtime fix is warranted by the reviewed evidence.

Findings: 0 critical, 1 important, 0 medium, 0 minor

**Blocking verdict:** BLOCKING on project bookkeeping alignment only. The implementation behavior and backlog disposition are otherwise accepted.

## Dispatch Audit

- Request ID: `review-p01-20260916-observer-rearm-01`
- Reviewer target: `oat-reviewer-gpt-5-6-sol-high`
- Model axis: `selected:gpt-5.6-sol`
- Effort axis: `selected:high`
- Selection reason: managed/high review matrix target
- Dispatch stamp: `Dispatch: scope=p01 action=review role=reviewer producer=unknown provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:high dispatch_policy=high dispatch_ceiling=high target=oat-reviewer-gpt-5-6-sol-high`

## Review Orchestration

| Wave | Lane | Task class | Classification rationale | Selected target | Acceptance / outcome | Floor satisfaction | Fallback |
| ---- | ---- | ---------- | ------------------------ | --------------- | -------------------- | ------------------ | -------- |
| R1 | Runtime evidence | intelligent-recon | Semantic audit of unfamiliar concurrency/process tests where a silent miss could validate the wrong reliability claim. | Codex native `explorer`, `gpt-5.6-terra` / `high` | Accepted; completed read-only report with runtime test execution and file:line evidence. | satisfied | none; caller-inline remained configured |
| R1 | Docs, generated output, and PJM | intelligent-recon | Semantic and governance audit where misleading delivery claims, parity drift, or lifecycle omissions could be silent. | Codex native `explorer`, `gpt-5.6-terra` / `high` | Accepted; completed read-only report with parity, contract-test, version, and PJM evidence. | satisfied | none; caller-inline remained configured |

**Primary reconciliation:** The primary reviewer reopened all load-bearing sources and independently ran the focused and full verification commands. The runtime lane's concern that the new re-arm fixtures contain only one Codex candidate was not promoted: the reviewed implementation selects the explicit pinned session in `observePinnedSession()`, the fixtures pass `codex:<id>` throughout and assert the resulting session/ranges, and the existing suite separately protects against same-CWD candidate switching. The docs/PJM lane's stale-project-state observation was accepted after direct comparison of `implementation.md` and `state.md` with the three authoritative commits and closed backlog record. No worker assigned severity or wrote an artifact.

## Findings

### Critical

None.

### Important

- **Resumable project state still says Phase 1 has not been implemented** (`.oat/projects/shared/session-observer-rearm/implementation.md:6`)
  - Issue: The authoritative range contains all three task commits and the backlog item is closed, but `implementation.md` still points to `p01-t01`, reports `0/3` complete, labels every task pending/in progress, and leaves outcome, verification, and final summary as pending (`implementation.md:23-25`, `implementation.md:34-64`, `implementation.md:121-135`). `state.md` likewise points to `p01-t01`, records no last commit, and calls implementation in progress (`state.md:2-3`, `state.md:16-17`, `state.md:94-105`, `state.md:118-126`). The plan's durable reference still names the moved active-item path instead of the archived record (`plan.md:238`). This is artifact drift rather than an implementation defect, but it can misroute resume/closeout logic and makes the audited phase status false.
  - Fix: Update `implementation.md` with p01-t01 through p01-t03 completion, their full commits, the verified test/gate results, the no-runtime-fix conclusion, and the documented limitation. Advance `state.md` to the post-phase review milestone with the authoritative last commit and accurate phase/task status. Change the plan reference to `.oat/repo/pjm/backlog/archived/BL-260916-session-observer-re-armed.md`; historical task instructions may retain their original pre-closeout paths.
  - Requirement: Lite workflow resumability and p01-t03 closeout governance.

### Medium

None.

### Minor

None.

## Requirements/Design Alignment

**Evidence sources used:** `plan.md`, `implementation.md`, `state.md`, the archived `BL-260916-session-observer-re-armed.md`, root `AGENTS.md`, `src/AGENTS.md`, `.oat/repo/AGENTS.md`, `.oat/repo/pjm/AGENTS.md`, the authoritative commit diff, canonical runtime/tests/reference sources, and generated payloads. Discovery, spec, design, and import-reference artifacts are not required and are absent by design for this lite workflow.

Design alignment is not applicable as a separate artifact check because lite mode has no `design.md`; the plan's Product Behavior and Technical Design sections were used as the design contract.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| Ticket AC1 / Plan VC1: clean exact-pin re-arm | implemented | Exact `codex:<id>` fixtures cover SIGTERM, `watch-ctl stop`, and max-runtime. SIGTERM waits for observable active-target state; virtual clocks and barriers drive the other lifetimes. Range, rendered content, stdout, and persisted offsets are asserted independently. |
| Ticket AC2 / Plan VC2: filtered, startup, contention, and stdout-failure boundaries | implemented | Filtered-only advancement preserves the next renderable message; startup append is caught; contender-first restores the shared offset; owner-polls-first is characterized and recoverable; rejected stdout proves persisted consumption and no replay. |
| Ticket AC3: fix only a demonstrated bounded defect | implemented | Supported clean paths retain renderable content. No runtime source changed. The known pre-stdout and shared-offset/CAS boundaries are preserved as explicit limitations instead of being misrepresented as a clean-shutdown defect. |
| Ticket AC4 / Plan VC3: bounded Monitor/re-arm guidance | implemented | Canonical guidance gives the exact-pin procedure, treats approximately 30 minutes as session-specific, distinguishes persistence/stdout/agent delivery, and explicitly denies synthetic live-harness proof. The five focused contract tests pass. |
| Ticket AC5: portable sanitized evidence | implemented | Fixtures and guidance use synthetic IDs, paths, and content; they do not depend on the ignored local collaboration log or real session identifiers. |
| Plan VC4: versions and generated parity | implemented | `session-observer` is `1.0.40`, `session-observer-collab` is `1.0.29`, and the transitive `session-fork-to-destination` owner is `0.2.4`. Version validation reports three changed skills and `build:check` passes; canonical and generated runtime references are byte-identical. |
| Plan VC5: normal non-live repository gates | implemented | Independent `pnpm run premerge` passed 135 files with 1 skipped and 2,002 tests with 1 skipped; build, type-check, generated parity, structural validation, and smoke all passed. |
| Plan VC6: conditional backlog archive | implemented | The item is closed under `backlog/archived/`, the completed entry is newest-first, the generated active index no longer lists it, the curated overview links the archive, and `oat pjm doctor --json` passes all lifecycle checks. |
| Lite workflow resumability | partial | Implementation and state bookkeeping were not advanced after the three task commits; see Important finding. |

### Extra Work (not in declared requirements)

None. The `watch-ctl stop` case, contract-test expansion, transitive version bump, regenerated payloads, and atomic backlog closeout all map to the reviewed plan or repository governance contract.

## Verification Commands

The primary reviewer ran:

```bash
pnpm run test:vitest src/skills/session-observer/src/watch.test.ts src/skills/session-observer/src/observe.test.ts src/skills/session-observer/src/integration.test.ts src/skills/session-observer-collab/src/runtime-claude-code-reference.test.ts
pnpm run type-check
pnpm run build:check
pnpm run validate:skill-versions -- --base-ref 49b4baf3c89de08ee23d100bc299f9f9a64f47be
oat pjm doctor --json
pnpm run premerge
git diff --check 73e887ac96f920c0a86089656ac09c00d40e3f9d..7399f07125a15cfbaec6fb397906497898b6c693
```

Observed results: 88 focused tests passed; type-check and generated parity passed; three changed skills satisfied version closure; PJM adoption and backlog lifecycle checks passed; full premerge passed 135 files with 1 skipped and 2,002 tests with 1 skipped; validation and smoke passed; the authoritative diff is whitespace-clean.

After fixing the Important artifact-alignment finding, additionally run:

```bash
git diff --check -- .oat/projects/shared/session-observer-rearm/implementation.md .oat/projects/shared/session-observer-rearm/state.md .oat/projects/shared/session-observer-rearm/plan.md
oat project status --project-path .oat/projects/shared/session-observer-rearm
```

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert the Important artifact-alignment finding into bounded bookkeeping corrections, then re-run the phase review/receive gate as required by the lifecycle.
