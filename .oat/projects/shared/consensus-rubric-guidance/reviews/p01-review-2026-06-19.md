---
oat_generated: true
oat_generated_at: 2026-06-19
oat_review_scope: p01
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/shared/consensus-rubric-guidance
---

# Code Review: p01

**Reviewed:** 2026-06-19
**Scope:** Phase p01 — frontmatter contract and validator support (p01-t01, p01-t02)
**Files reviewed:** 5
**Commits:** 07e4a01..136d991 (c584c8c, 136d991)

## Summary

Phase p01 is implemented correctly and matches the plan precisely. The validator now resolves an effective skill version from top-level `version` (falling back to `metadata.version`), validates semver, and rejects top-level/metadata mismatches while preserving the critical metadata-only regression path. Both consensus skills carry matching top-level `version` and a useful `argument-hint`. All verification commands pass and the change is tightly scoped. **Verdict: PASS** (0 Critical, 0 Important).

## Findings

### Critical

None

### Important

None

### Minor

- **Dead/unreachable final semver check** (`scripts/validate.mjs:183-185`)
  - Issue: The closing `if (!isValidSemver(effective))` branch can never fire. At that point `effective` is either `topLevel` (already validated at lines 171-173) or `metaVersion` (already validated at lines 175-177), and the `!effective` empty-case is handled at lines 165-167. The branch is harmless but is unreachable code that can mislead a future reader into thinking a third validation path exists.
  - Suggestion: Remove the lines 183-185 block, or add a short comment noting it is a defensive no-op. Not blocking.

- **Mismatch-but-both-malformed ordering is benign but worth noting** (`scripts/validate.mjs:165-181`)
  - Issue: If both `version` and `metadata.version` are present, malformed, and unequal, the malformed top-level check (line 171) fires first and the mismatch message is never shown. This is the more useful message anyway (fix the format first), so behavior is correct; noting only so future maintainers do not assume mismatch always wins.
  - Suggestion: No change required.

## Requirements/Design Alignment

**Evidence sources used:** `plan.md` (quick mode), `discovery.md`. No `spec.md`/`design.md` (quick mode — not expected, not flagged).

### Requirements Coverage

| Requirement (plan) | Status | Notes |
| --- | --- | --- |
| p01-t01: helper resolving effective version (top-level else metadata) | implemented | `resolveEffectiveSkillVersion` at `scripts/validate.mjs:160`; `effective = topLevel ?? metaVersion`. |
| p01-t01: validate semver | implemented | Uses `isValidSemver` from `bump-version.mjs` for top-level, metadata, and effective. |
| p01-t01: both present must match | implemented | `scripts/validate.mjs:179-181` returns a clear `version mismatch:` message naming both values. |
| p01-t01: keep existing required-field checks | implemented | Name/folder, description, license, compatibility checks untouched; only the version block was swapped. |
| p01-t01: metadata-only must still pass (regression guard) | implemented | Critical path preserved. Verified repo-wide: `skills/export-session-transcript` and `skills/session-observer` are metadata-only (`metadata.version: '1.0.0'`, no top-level), and `pnpm run validate` passes. |
| p01-t01: 4 temp-repo test cases | implemented | `tests/validate-script.test.mjs:286-369` — matching passes, metadata-only passes, mismatch fails (`/version mismatch/i`), malformed top-level fails (`/version must be valid semver/i`). Real assertions on `result.ok` + error text, not smoke. |
| p01-t02: top-level `version: '0.1.0'` on both skills, keep metadata.version | implemented | refine + evaluate SKILL.md; both match (verified via repo `parseFrontmatter`). |
| p01-t02: add `argument-hint` to both | implemented | refine: `<input-artifact.md> [--goal "<refinement goal>"]`; evaluate: `<artifact.md> [--rubric <rubric.md>]` — shapes match plan intent. |
| p01-t02: frontmatter test covers both skills, asserts top==metadata, checks argument-hint | implemented | `tests/skill-frontmatter.test.mjs` adds 6 new assertions across refine+evaluate; top-level==metadata asserted with explicit message; argument-hint checked (`/--rubric/` for evaluate, `/goal/i` for refine). |

### Extra Work (not in declared requirements)

None. The refine frontmatter test was also broadened (description length, license, allowed-tools, metadata regex), but this is reasonable in-task hardening of the same test file and does not constitute scope creep.

## Scope Discipline

- Only 5 files changed: `scripts/validate.mjs`, both consensus `SKILL.md`, and two test files — exactly the plan's declared file sets.
- No generated `.mjs` (banner) files hand-edited; no runtime/`src/` changes; no unrelated churn.
- Commits split cleanly per plan: `c584c8c` = validator + validate-script test (t01); `136d991` = frontmatter + frontmatter test (t02).
- `bump-version.mjs` still only tracks `refine` and reads only `metadata.version` — correctly deferred to p03-t01, so its omission here is in-scope-correct, not a gap.

## Verification Commands

```bash
node --test tests/validate-script.test.mjs tests/skill-frontmatter.test.mjs
pnpm run validate
```

**Actual results (run during review):**

- `node --test ...` — 18 tests, 18 pass, 0 fail (includes all 4 new validator cases + 6 new frontmatter assertions).
- `pnpm run validate` — `validation passed`.
- Repo `parseFrontmatter` confirmed it reads top-level `version`, matching `metadata.version`, and `argument-hint` for both skills (valid YAML).

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks. Both findings are Minor and optional; the phase passes without them.
