---
oat_generated: true
oat_generated_at: 2026-09-19T19:27:12Z
oat_review_scope: p03
oat_review_type: code
oat_review_invocation: subagent
oat_project: .oat/projects/shared/agent-messaging
oat_review_head_sha: 8e461d899975956e57d56481c96b9b4c60a2533e
---

# Code Review: p03 Re-review

**Reviewed:** 2026-09-19T19:27:12Z
**Scope:** Complete Phase 3 contract after fix round 1, including disposition of the four prior findings and the two deferred Phase 2 corrections
**Files reviewed:** 69
**Commits:** `254e190e081fc7af07931bc5b118b1d80643065d..8e461d899975956e57d56481c96b9b4c60a2533e` (7 commits)

## Summary

All four prior Phase 3 findings are fixed: observer bundles now carry a
versioned content-bound composition capability, registration preserves the
active epoch's controller across the covered lease changes, the cursor test uses
the real public observer state, and release wording distinguishes fixture from
live evidence. Phase 3 still does not pass because the standalone Stop and watch
execution paths do not apply that same immutable-controller constraint; after
observer ownership appears, they can accept an `observer-collab` assessment and
continue emitting through a `standalone-messaging` activation.

Findings: 0 critical, 1 important, 0 medium, 0 minor

## Review Orchestration

| Wave | Task class        | Classification rationale                                                                  | Selected target                | Acceptance / outcome                                                         | Floor satisfaction                   | Fallback      |
| ---- | ----------------- | ----------------------------------------------------------------------------------------- | ------------------------------ | ---------------------------------------------------------------------------- | ------------------------------------ | ------------- |
| W1   | intelligent-recon | Controller and composition semantics can fail silently despite green tests.               | `gpt-5.6-terra`, high          | Accepted; read-only runtime lane completed and ran 193 tests across 12 files | Satisfied                            | None          |
| W2   | mechanical-recon  | Generated parity, version, documentation, and non-live evidence checks are deterministic. | `gpt-5.6-luna`, high requested | Blocked before start by the native agent-thread limit                        | Target met floor; launch unavailable | Caller-inline |

**Primary reconciliation:** The primary reviewer reopened the canonical source,
tests, generated payloads, documentation, and changelog. It independently
verified the runtime lane's controller-mismatch claim at every cited call site
and reproduced the relevant repository checks. It covered the blocked mechanical
lane inline with `build:check`, skill-version validation, generated-source
searches, release-wording review, and a clean-worktree check. No worker wrote
files or assigned severity.

## Findings

### Critical

None.

### Important

- **Standalone boundaries accept observer ownership without matching the immutable activation controller** (`src/skills/agent-messaging/src/hooks/common.ts:138`)
  - Issue: `handleBoundary` first requires a `standalone-messaging` activation,
    but each ownership assessment at lines 138-147, 198-207, and 227-237 omits
    `requestedController: activation.controller` and checks only
    `automaticAllowed`. With a valid exact-session observer lease and verified
    observer bundle, `assessAutomaticOwnership` selects `observer-collab` and
    returns allowed. A stale standalone callback can therefore continue through
    claims and host output after observer ownership appears instead of failing
    closed. The foreground watch repeats the same mismatch at
    `src/skills/agent-messaging/src/watch.ts:126`. This is inconsistent with the
    corrected registration path, which passes the immutable controller at
    `src/skills/agent-messaging/src/agent-messaging.ts:723`, and violates
    p03-t02's rule that p02's active-observer refusal is replaced only for an
    explicitly composed epoch.
  - Fix: pass the activation's controller as `requestedController` to every
    standalone hook/watch ownership assessment and require the returned
    controller to equal it before claiming or emitting. Add fixtures with a
    complete schema-v6 observer lease and recognized composition-capable bundle
    that appear after standalone activation, covering an in-flight Stop callback
    and a foreground watch; both must emit nothing and retain their finite state.
  - Requirement: p03-t02 immutable controller binding, active-observer refusal
    outside explicit composition, and single continuation ownership.

### Medium

None.

### Minor

None.

## Prior Finding Disposition

| Prior finding                                                     | Disposition            | Evidence                                                                                                                                                                                                                                                                                              |
| ----------------------------------------------------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Historical observer bundles accepted as composition-capable       | Fixed                  | Capability/version and SHA-256 content binding are produced by `src/skills/session-observer-collab/src/lib/codex-install.mjs:18-75` and verified by `src/skills/agent-messaging/src/registration.ts:129-183`; legacy-manifest and content-drift fixtures pass.                                        |
| Registration could switch away from the active epoch's controller | Fixed for registration | `delivery register` requires the exact active collaboration/session/worktree and passes `requestedController` at `src/skills/agent-messaging/src/agent-messaging.ts:685-735`; both pre-registration lease-arm/disarm cases pass. The new Important finding is the separate runtime-boundary omission. |
| Public cursor proof used an unrelated file                        | Fixed                  | `src/skills/session-observer-collab/src/messaging-composition.test.ts:470-538` seeds and reads the real `STATE_DIR/state.json` through the observer state API while separately checking the private lease.                                                                                            |
| Changelog blurred fixture and live acceptance                     | Fixed                  | `CHANGELOG.md:7-11` now says `fixture-tested, composition-capable`; docs and acceptance matrices keep every live row unverified and Claude p04/Cursor manual boundaries explicit.                                                                                                                     |

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md`,
`implementation.md`, the prior review artifact, root/source/documentation build
contracts, the authoritative commit range, canonical source/tests, generated
standalone/plugin payloads, docs, changelog, and release guidance.

### Requirements Coverage

| Requirement                                           | Status      | Notes                                                                                                                                                                   |
| ----------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| p03-t01 shared collaboration container/log            | Implemented | Both observer distributions bundle the shared immutable log/container without enabling mailbox delivery; shared-log and generated parity tests pass.                    |
| p03-t02 verified composed adapter                     | Implemented | Legacy manifests and changed bundle content fail closed; generated installer and recognition logic agree.                                                               |
| p03-t02 immutable controller and single owner         | Partial     | Registration is bound correctly, but standalone hook/watch runtime ownership checks can accept a different controller.                                                  |
| p03-t02 inbox-first/shared budget/cursor preservation | Implemented | Message selection precedes observation, shared slots precede observer CAS, exact-ID guidance is present in both skills, and real public/private cursor evidence passes. |
| p03-t02 Claude/Cursor boundary                        | Implemented | Claude composed Monitor remains `composed-monitor-unavailable` pending p04; Cursor remains manual/unverified.                                                           |
| p03-t03 docs/distribution/release                     | Implemented | Canonical/generated forms, versions 1.0.11 and 1.0.36, changelog, docs navigation, and live-evidence labels are consistent.                                             |
| p02-r2-M1 exact expiry equality                       | Implemented | `activationStatus` expires at `now >= effectiveExpiresAt`; focused and full tests pass.                                                                                 |
| p02-r2-M2 final-veto diagnostic ordering              | Implemented | Stop and watch attempt diagnostics follow their final ownership/veto rechecks; focused and full tests pass.                                                             |

### Extra Work (not in declared requirements)

None.

## Verification Commands

Commands actually run by the primary reviewer:

```bash
pnpm run test:vitest src/skills/session-observer-collab/src/messaging-composition.test.ts src/skills/session-observer-collab/src/shared-log.test.ts src/skills/session-observer-collab/src/codex-install.test.ts src/skills/agent-messaging/src/owner-contract.test.ts src/skills/agent-messaging/src/hooks.test.ts src/skills/agent-messaging/src/watch.test.ts src/skills/agent-messaging/src/registration.test.ts src/skills/agent-messaging/src/packaging.test.ts src/shared/collaboration/activation.test.ts src/shared/collaboration/claims.test.ts tests/tooling/skill-packaging.test.ts
pnpm run test
pnpm run build:check
pnpm run type-check
pnpm run validate
pnpm run smoke
pnpm run validate:skill-versions -- --base-ref 254e190e081fc7af07931bc5b118b1d80643065d
git diff --check 254e190e081fc7af07931bc5b118b1d80643065d..8e461d899975956e57d56481c96b9b4c60a2533e
```

Results: 119/119 focused tests passed; full suite passed 2,214 with 1 skipped;
generated build freshness, type-check, validation, smoke, two-skill version
validation, and diff checks passed. The worktree remained clean before this
review artifact was written.

Deliberately not run: live provider hooks, provider installs/trust/configuration,
user-level skill installs or syncs, paid/live gates, publication, push/PR/merge,
and backlog closure. The fix changed no authored docs after the prior successful
54-page production build, so this re-review did not rerun the mutating docs build.

## Recommended Next Step

Run the parent `oat-project-implement` review-fix loop for the Important
controller-boundary finding, then request a fresh independent Phase 3 review.
