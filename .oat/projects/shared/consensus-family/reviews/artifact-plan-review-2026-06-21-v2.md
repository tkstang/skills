---
oat_generated: true
oat_generated_at: 2026-06-21
oat_review_scope: plan
oat_review_type: artifact
oat_review_invocation: manual
oat_project: .oat/projects/shared/consensus-family
---

# Artifact Review: plan (re-review)

**Reviewed:** 2026-06-21
**Scope:** Re-review of `plan.md` for the `consensus-family` spec-driven project, after review-receive (`b516c2a`) and the docs-IA rebase onto PR #32 (`62c26a2`, refreshed in `3ef49a2`/`74b46a0`)
**Files reviewed:** 4 artifacts (plan/spec/design + archived prior review) + ~15 source/test/tooling/docs files verified against the rebased tree
**Commits:** N/A (artifact review); verified HEAD contains `62c26a2`, `b516c2a`, `3ef49a2`, `74b46a0`

## Summary

The plan is implementation-ready and the docs-IA refresh is accurate against the rebased
tree. All four prior Important/Medium findings are resolved: `CHANGELOG.md` is now in every
docs task's Files with a RED-step `## [Unreleased] / ### Added` assertion; the three provider
`plugin.json` manifests are now in `p02-t04`; the SKILL.md required sections and both
`skill-frontmatter.test.ts` allowlists are named; and the spec Requirement Index now includes
`p01-t02`. The new docs tasks correctly target the post-#32 IA (`documentation/docs/user-guide/consensus/`
per-skill pages + `index.md`/`configuration.md`/`meta.json` + the `oat docs generate-index`
regenerate of `documentation/index.md`), and they satisfy the rewritten `docs-presence.test.ts`
and `readme-scope.test.ts` invariants rather than stale pre-#32 ones. No Critical or Important
findings. The remaining items are Medium/Minor precision gaps (a second hardcoded skill allowlist
in `versioning.test.ts` not explicitly named; an `index.md` "future work" line the plan must
delete; minor index-regen and engineering-meta confirmations).

## Findings

### Critical

None.

### Important

None.

### Medium

- **A third hardcoded shipped-skill allowlist (`tests/release/versioning.test.ts`) is in the Files lists but not named in the task guidance** (`plan.md:502`, `739`, `975`; `tests/release/versioning.test.ts:26-28`)
  - Issue: The prior Medium #3 was about the two allowlists in `tests/repo/skill-frontmatter.test.ts` (the `skillPaths` parametrization at `:13` and the `['refine','evaluate']` name check at `:35`), and the plan now names both explicitly. But `tests/release/versioning.test.ts:26-28` carries its OWN hardcoded `skillFiles = ['.../refine/SKILL.md', '.../evaluate/SKILL.md']` array that the new skills must be added to. The plan lists `tests/release/versioning.test.ts` in the Files of `p02-t04`/`p03-t04`/`p04-t04`, so a RED-then-extend implementer will likely hit it — but the task prose ("the shipped-skill test allowlists (`skillPaths` plus the name allowlist)") refers only to `skill-frontmatter.test.ts`, leaving this third allowlist implicit.
  - Fix: In each `pNN-t04` task, extend the allowlist callout to read e.g. "add the new skill to the hardcoded shipped-skill lists in BOTH `tests/repo/skill-frontmatter.test.ts` (`skillPaths` + the name allowlist) AND `tests/release/versioning.test.ts` (`skillFiles`)." This is guidance precision; the file is already correctly scoped.

- **The consensus `index.md` "future work" line for create/decide/plan must be deleted as each ships, and the plan should say so explicitly** (`plan.md:564`, `800`, `1038`; `documentation/docs/user-guide/consensus/index.md:70-71`)
  - Issue: `documentation/docs/user-guide/consensus/index.md:70-71` currently lists `consensus-create`, `consensus-decide`, `consensus-plan` as "future work." `readme-scope.test.ts:79` only asserts the docs site *contains* the string `consensus-create` (satisfied by the new `create.md` page), so it will NOT catch a stale "future work" line — the contradiction (a shipped skill still listed as future work) would ship silently. The plan's Step 3 refactor notes do say to "remove stale 'future work' wording for shipped skills" (`p03-t05:807`, `p04-t05:1045`), but `p02-t05` (the create phase, where create stops being future work) does not call out editing this specific Limitations line, and none of the tasks point at `index.md:70-71` by location.
  - Fix: In `p02-t05` (and reaffirm in `p03-t05`/`p04-t05`), explicitly note that the `## Limitations` "future work" bullet in `documentation/docs/user-guide/consensus/index.md` must drop each skill as it ships (leaving only `consensus-research` deferred), and consider a RED assertion that the docs site does NOT describe a shipped skill as "future work." Low risk, but it is exactly the stale-doc drift the repo convention forbids.

### Minor

- **`documentation/index.md` is generated by `predev`/`prebuild`; the explicit regenerate + git-add is correct but worth a one-line note** (`plan.md:564`, `581`; `documentation/index.md:1`; `documentation/docs/engineering/contributing/documentation/authoring.md:46-58`)
  - Issue: `documentation/index.md` carries an autogenerated banner ("Do not hand-edit; clobbered on every `predev`/`prebuild`"). The plan correctly runs `cd documentation && oat docs generate-index --docs-dir docs --output index.md` and stages `documentation/index.md`. The only subtlety: the regen is keyed off each new page's frontmatter + its parent `## Contents`/`meta.json`, so the regen is a no-op unless the per-page edits land first. The task ordering already does the page edits before the regen, so this is correct — flagging only so the implementer treats `documentation/index.md` as derived output, never hand-edited.
  - Suggestion: No change required; optionally add "(generated; do not hand-edit)" beside the `documentation/index.md` Files entries.

- **`generated-runtime.md` table needs three new rows but is NOT covered by the index regen; confirm it is edited directly** (`plan.md:1019`, `1038`; `documentation/docs/engineering/architecture/generated-runtime.md:30-38`; `documentation/docs/engineering/architecture/meta.json`)
  - Issue: `p04-t05` lists `documentation/docs/engineering/architecture/generated-runtime.md` and says to update the "generated-runtime architecture page." That page's canonical-source→generated-output table (`:30-38`) currently maps only refine/evaluate/transcript; create/decide/plan loop + wrapper outputs must be added as new rows. This file is a leaf page (not regenerated by `oat docs generate-index`), so it is edited directly — correct. Its parent `meta.json` (`["index","transcript-core","generated-runtime"]`) already lists the page, so no `meta.json` edit is needed there.
  - Suggestion: Optionally have `p04-t05` Step 1 add a RED assertion that the generated-runtime table names the create/decide/plan generated `.mjs` paths, so the table edit is test-enforced rather than discretionary.

- **Discovery's closeout/DR-promotion success criterion is deferred to `oat-project-complete` — now explicitly stated (prior Minor resolved)** (`discovery.md:96`, `plan.md:1061-1063`)
  - Issue: The prior Minor flagged that discovery Success Criteria (`discovery.md:96` — promote DRs + update the four backlog items / `completed.md` / `current-state.md` / `roadmap.md`) had no covering task. The plan now has a "Closeout Bookkeeping" section (`plan.md:1061-1063`) explicitly deferring this to `oat-project-complete`. Resolved.
  - Suggestion: No change required.

## Prior Findings Resolution

| Prior finding | Severity | Status | Evidence |
| ------------- | -------- | ------ | -------- |
| #1 CHANGELOG.md updated + test-enforced | Important | **Resolved** | `CHANGELOG.md` now in Files of `p02-t05:550`, `p03-t05:786`, `p04-t05:1024`; each RED step (`:557`, `:793`, `:1031`) asserts create/decide/plan "recorded under `## [Unreleased]` / `### Added`" and runs `tests/repo/readme-scope.test.ts` (which already owns CHANGELOG assertions at `:118-139`). The new assertion is additive to that file. |
| #2 Provider plugin manifests updated (FR4 "manifest entries") | Important | **Resolved** | `plugins/consensus/.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`, `.cursor-plugin/plugin.json` now in `p02-t04` Files (`:495-497`) and git-add (`:533`); Step 2 (`:514`) broadens the manifest `description` to the create family and explicitly says "Do not bump provider manifest versions" — correct, since `plugin-manifests.test.ts:31` pins `0.1.0` and does not assert description content, while `docs-presence.test.ts:153-158` only requires `refine`+`evaluate` substrings (a broadened description still passes). |
| #3 skill-frontmatter.test.ts both allowlists + docs-presence alignment | Medium | **Resolved (with a residual: see Medium above)** | `p02-t04:507` now names both places: "the `skillPaths` parametrization and the `['refine','evaluate']` name allowlist"; `p03-t04:744`/`p04-t04:980` repeat the allowlist callout. `tests/repo/skill-frontmatter.test.ts` and `tests/repo/docs-presence.test.ts` are in each `pNN-t04` Files list. Residual: a THIRD allowlist in `tests/release/versioning.test.ts:26-28` is in Files but not named in prose (Medium finding above). |
| #4 spec Requirement Index omits `p01-t02` | Medium (artifact drift) | **Resolved** | `spec.md:193` FR1 row now reads `p01-t01, p01-t02, p01-t03, p01-t04, p01-t05`; `spec.md:194` FR2 row now includes `p01-t02`. Matches `plan.md:101-144` (Task p01-t02 "Thread Cold Start Into Prompt Builders"), which is load-bearing per `design.md:72`. |
| Minor: closeout/DR-promotion covering task | Minor | **Resolved** | `plan.md:1061-1063` "Closeout Bookkeeping" explicitly defers DR promotion + backlog/roadmap/`completed.md`/`current-state.md` updates to `oat-project-complete`. |
| Minor: `p01-t06` / SKILL_FILES scoping | Minor | **Resolved (confirmation)** | `SKILL_FILES` in `scripts/bump-version.mjs:18-21` already contains refine+evaluate, so `p01-t06` needs no `bump-version.mjs` edit; the new-skill tasks correctly list it. No change needed; confirmed accurate. |
| Minor: design groups decide+plan, plan splits to Phase 3/4 | Minor | **Resolved (defensible divergence)** | Plan keeps Phase 3 = decide, Phase 4 = plan, encoding FR5-then-FR6 separate-PR sequencing; design `Implementation Phases` Phase 3 = decide+plan "separate PRs." Improvement, not a defect. Optional design note only. |

## Docs-IA Alignment (NEW)

**Verified against the rebased tree** (`documentation/docs/user-guide/consensus/` contains `index.md`, `configuration.md`, `meta.json`, `refine.md`, `evaluate.md`):

- **Per-skill pages + nav wiring:** `p02-t05`/`p03-t05`/`p04-t05` add `documentation/docs/user-guide/consensus/{create,decide,plan}.md` and modify `index.md`, `meta.json` (and `configuration.md` for create's `independent_draft` semantics). This matches the authoring contract (`authoring.md:14-28`): every page needs frontmatter, a `## Contents` link in the parent `index.md`, and an entry in `meta.json` (`["index","refine","evaluate","configuration"]` → must gain the three new pages). **Correct.**
- **Index regeneration:** `p02-t05:564,581` runs `cd documentation && oat docs generate-index --docs-dir docs --output index.md` and stages `documentation/index.md`. The command, flags, and target are exact (`oat docs generate-index --docs-dir <path> --output <path>` verified live; `documentation/index.md:1` carries the matching autogenerated banner). **Correct.**
- **README stays slim:** the plan updates only the README's short shipped-skill summary/links and never reintroduces dense sections (`p02-t05` Step 3 `:571`; `p04-t05` Step 3 `:1045`). This satisfies the rewritten `readme-scope.test.ts:48-56` (`README` must keep `## Local Git Repository Install`, link `documentation/docs/`, and NOT contain `## Permissions`/`## Advanced Configuration`). **No stale pre-#32 README-as-dense-doc assumption.**
- **`plugins/consensus/README.md` kept as deep operator reference:** the plan treats it as in-scope deep reference (`Pre-Implementation Coordination` `:48`; listed in each docs task's Files). `readme-scope.test.ts:102-116` still asserts the plugin README documents iteration modes/escalation flags — the plan does not strip it. **Correct.**
- **Rewritten repo-invariant gates satisfied, not stale ones:**
  - `docs-presence.test.ts` reads the WHOLE docs site (`:10-22`) and asserts `## Permissions`/`## Limitations` live in the site, generated-runtime contract in the site, and manifests contain `refine`+`evaluate` — the plan's docs edits keep all of these. It hardcodes SKILL.md structure assertions ONLY for `refine`/`evaluate` (via `refineSkillPath`/`evaluateSkillPath` constants `:30-31`), so the plan's `p02-t04:516`/`p03-t04:753`/`p04-t04:989` guidance to author the new SKILL.md required sections (`## When NOT to Use`, `## Examples`, `## Success Criteria`, `## Output Contract`, per-skill invocation) is correct as authoring guidance even though no test currently asserts them for the new skills.
  - `readme-scope.test.ts:79` asserts the docs site contains `consensus-create` — the new `create.md` page satisfies it (see Medium finding re: deleting the stale "future work" line).
  - `generated-runtime.md` (`p04-t05`) is a leaf page edited directly (table rows for new runtimes); not regenerated by the index command — **correct handling.**
- **Pre-Implementation Coordination section (`plan.md:40-51`) accuracy:** all claims verified — branch rebased onto `62c26a2` (confirmed ancestor of HEAD); #32 stood up `documentation/docs/`, slimmed README, rewrote both test files (confirmed by reading them); docs land under `documentation/docs/user-guide/consensus/`; `plugins/consensus/README.md` left untouched by #32 and remains in scope; CHANGELOG/smoke/manifests/generated-output remain implementation surfaces. The section's "adapt or defer to `oat-project-document` if IA changes again" guidance is sound. **Accurate.**

## Spec/Design Alignment

**Evidence sources used:** spec.md, design.md, discovery.md (upstream); plan.md (under review); archived prior review; verified against `src/consensus/core/consensus-loop.ts`, `scripts/bump-version.mjs`, `documentation/docs/user-guide/consensus/{index,configuration,meta.json,refine}.md`, `documentation/docs/engineering/architecture/{generated-runtime.md,meta.json}`, `documentation/docs/engineering/contributing/documentation/authoring.md`, `documentation/index.md`, `tests/repo/{docs-presence,readme-scope,skill-frontmatter,plugin-manifests,layout}.test.ts`, `tests/release/{versioning,smoke-test-script}.test.ts`, `scripts/smoke-test.mjs`, and `tests/fixtures/bin/`.

Core design-decision conformance (re-confirmed against the rebased tree):
- **Round-1-only loop-core cold-start:** `p01-t01`/`p01-t03`. Confirmed code still closes `ColdStartMode = 'shared_input'` (`consensus-loop.ts:22`) and hard-rejects `independent_draft` with "not yet supported" (`:1669-1670`) — the widening is genuinely required and not yet done.
- **`coldStart` threaded to prompt builders (load-bearing wiring):** `p01-t02`. Confirmed `coldStart` lives in loop options (`:119`, `:1612`, `:1691`) but the round-1 prompt builders still need it — matches `design.md:72`.
- **refine/evaluate stay `shared_input`-only; core + new wrappers accept both:** `p01-t05` rewords the guards from "not yet supported" to a semantic restriction; new wrappers (`p02-t01`/`p03-t01`/`p04-t01`) accept both. Aligned with FR1/FR3.
- **decide surfaces dissent at minimal agency:** `p03-t02`/`p03-t03`; matches `design.md:106` and `configuration.md:60-64` (minimal agency surfaces, doesn't decide).
- **Markdown-by-prompt for decide/plan (no machine schema):** `p03-t02`/`p04-t02`.
- **DR-024 seam reused (verdict-parallel/synthesis schemas copied, enum enforced):** `p02-t04`/`p03-t04`/`p04-t04` copy schemas; NFR3 integration tasks enforce the enum.
- **Phasing:** P0 gate (FR1-3, Phase 1) → P0 create (FR4, Phase 2) → P1 decide (FR5, Phase 3) → P1 plan (FR6, Phase 4). Matches spec sequencing note (`spec.md:210`).
- **Build/version discipline:** canonical TS → `pnpm build` → never hand-edit generated `.mjs`; version+metadata bump; `SKILL_FILES` registration via `scripts/bump-version.mjs` (`p02-t04`/`p03-t04`/`p04-t04` list it; `SKILL_FILES` confirmed at `bump-version.mjs:18-21`); full gate set in each `pNN-t05`.

### Requirements Coverage

| Requirement | Status      | Notes                                                                                                   |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------------- |
| FR1         | implemented | p01-t01/t02/t03/t04/t05; spec index now includes p01-t02 (prior #4 resolved)                            |
| FR2         | implemented | p01-t03/t04 cover all 3 modes incl. alternating degenerate path                                          |
| FR3         | implemented | p01-t01/t04/t05 + per-wrapper t01s; cold_start recording + `--cold-start` config                         |
| FR4         | implemented | create wrapper/anatomy/docs/smoke (p02-t01..t05); manifest entries (p02-t04) + CHANGELOG (p02-t05) now covered |
| FR5         | implemented | p03-t01..t05; minimal-agency dissent surfacing explicit                                                  |
| FR6         | implemented | p04-t01..t05; moderate agency, required headings                                                         |
| NFR1        | implemented | stdlib-only reaffirmed (p02-t01 Step 3 / p02-t02); validate gate                                         |
| NFR2        | implemented | p01-t06/p02-t04/p03-t04/p04-t04/p04-t05; build:check + skill-version validators                          |
| NFR3        | implemented | DR-024 seam reuse, schema copy, enum enforcement in integration tasks                                    |
| NFR4        | implemented | untrusted-data framing + 1 MiB cap + path confinement (p01-t03/p02-t02/p03-t02/p04-t02)                  |
| NFR5        | implemented | full gate set in p02-t05/p03-t05/p04-t05 (build:check, type-check, test, validate, validate:skill-versions, smoke) |

FR4 is now fully covered (prior re-review marked it partial only for the manifest + CHANGELOG gaps, both now closed).

### Extra Work (not in declared requirements)

None. Every task maps to a requirement or to required build/version/test/doc plumbing. The plan explicitly excludes outline-first sectioning (`p04-t03` Step 3), machine schemas (`p03-t02`/`p04-t02`), hard `require_submission` (absent), and keeps `consensus-research` out of scope (`p04-t05` Step 3). Closeout bookkeeping is correctly deferred to `oat-project-complete` (`plan.md:1061-1063`), not smuggled into implementation phases.

## Verification Commands

These confirm the current invariants the plan must satisfy (no implementation exists yet):

```bash
# Branch contains the rebase + review-receive commits
git merge-base --is-ancestor 62c26a2 HEAD && echo "docs-IA merge present"
git log --oneline | grep -E "b516c2a|3ef49a2|74b46a0"

# Docs-IA tree the plan targets
ls documentation/docs/user-guide/consensus/   # index.md configuration.md meta.json refine.md evaluate.md
cat documentation/docs/user-guide/consensus/meta.json
oat docs generate-index --help                # confirm --docs-dir / --output flags

# Rewritten repo-invariant gates
grep -n "consensus-create\|future work\|## Permissions\|## Limitations" tests/repo/readme-scope.test.ts
grep -n "refineSkillPath\|evaluateSkillPath\|providerManifests\|readDocsSite" tests/repo/docs-presence.test.ts

# The three hardcoded shipped-skill allowlists the pNN-t04 tasks must extend
grep -n "refine.*evaluate\|skillPaths\|\['refine'" tests/repo/skill-frontmatter.test.ts
grep -n "skillFiles" tests/release/versioning.test.ts
grep -n "SKILL_FILES" scripts/bump-version.mjs

# The stale "future work" line p02-t05 must delete
grep -n "future work" documentation/docs/user-guide/consensus/index.md

# Current loop-core state matches plan assumptions
grep -n "ColdStartMode\|not yet supported" src/consensus/core/consensus-loop.ts

# Once implemented, the plan's own gate set:
pnpm run build:check && pnpm run type-check && pnpm run test && pnpm run validate && pnpm run validate:skill-versions -- --base-ref main && pnpm run smoke
```

## Recommended Next Step

The plan passes re-review with no Critical/Important findings — all four prior
Important/Medium findings and all three Minor items are resolved, and the docs-IA
refresh is accurate. The two Medium items (name the `versioning.test.ts` `skillFiles`
allowlist in the `pNN-t04` task prose; explicitly delete the consensus `index.md`
"future work" line as each skill ships) are guidance-precision improvements that can
be folded in opportunistically via `oat-project-review-receive`, or accepted as-is
since both target files are already in the relevant tasks' Files lists. After that,
mark the plan review `passed` and proceed to `oat-project-implement`.
