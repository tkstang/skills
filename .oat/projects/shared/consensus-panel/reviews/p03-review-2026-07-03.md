---
oat_generated: true
oat_generated_at: 2026-07-03
oat_review_scope: p03
oat_review_type: code
oat_review_invocation: manual
oat_project: /Users/tstang/Code/feat-consensus-panel/.oat/projects/shared/consensus-panel
---

# Code Review: p03

**Reviewed:** 2026-07-03
**Scope:** Phase 3 only: consensus panel runtime, provider execution/shortfall handling, generated panel runtime output, and OAT tracking relevant to p03.
**Files reviewed:** 16 (11 p03 changed files plus 5 OAT artifacts)
**Commits:** 4009bd2, f15566b, 21944ac

## Summary

FAIL. The Phase 3 source-level panel tests and generated-output drift check pass, and the OAT tracking correctly leaves p03 pending review with p04-t01 next and p02 passed. However, the generated panel wrapper resolves its response schema to a non-existent shipped path, so real `consensus run` calls fail with `schema_read_failed`; there is also a path-confinement bug where `--allow-root` rejects canonical-equivalent paths such as macOS `/var` versus `/private/var`.

## Findings

### Critical

- **Generated panel runtime resolves the response schema to a non-existent path** (`plugins/consensus/skills/panel/scripts/consensus-panel.mjs:354`)
  - Issue: The generated wrapper retains the source-relative schema URL (`../../../plugins/consensus/skills/panel/schemas/panel-response.schema.json`). From the shipped runtime location this resolves to `/Users/tstang/Code/feat-consensus-panel/plugins/consensus/plugins/consensus/skills/panel/schemas/panel-response.schema.json`, which does not exist. The real provider CLI reads `schema_path` before invocation and returns `schema_read_failed`, so the generated Phase 3 runtime cannot produce successful provider turns through the shipped path.
  - Evidence: `node --input-type=module -e "import { panelResponseSchemaPath } from './plugins/consensus/skills/panel/scripts/consensus-panel.mjs'; import { existsSync } from 'node:fs'; const p = panelResponseSchemaPath(); console.log(p); console.log(existsSync(p));"` printed the nested `plugins/consensus/plugins/...` path and `false`. Passing that path to `node plugins/consensus/scripts/consensus.mjs run --request-json - --json` returned `CONSENSUS_CLI_USAGE` with `terminal_reason: schema_read_failed`.
  - Fix: Update the canonical source at `src/consensus/panel/consensus-panel.ts:572` so `panelResponseSchemaPath()` is runtime-location-aware. For example, prefer a generated sibling candidate such as `../schemas/panel-response.schema.json` when it exists and fall back to the source/repo path for TypeScript tests. Regenerate with `pnpm run build`, and add a generated-runtime test that imports the shipped wrapper and asserts `existsSync(panelResponseSchemaPath())`.
  - Requirement: p03-t03 generated panel runtime output; p03-t02 provider execution through `consensus run`

### Important

- **`--allow-root` rejects valid canonical-equivalent output paths** (`src/consensus/panel/consensus-panel.ts:400`)
  - Issue: `confinePanelWrite()` performs a lexical `inside(root, target)` check before canonicalizing paths. On macOS, temp paths commonly have `/var/...` and `/private/var/...` aliases; if `--allow-root` is passed as `/var/...` while `process.cwd()` resolves to `/private/var/...`, an output under the same directory is rejected with `WRITE_PATH_OUTSIDE_ROOT`. This weakens the p03-t01 `--output` / `--run-dir` / `--allow-root` contract for valid in-root paths. `confineRead()` has the same pre-realpath shape for question-file reads.
  - Evidence: A generated-runtime probe using a temp project root and its `realpath` cwd failed with `WRITE_PATH_OUTSIDE_ROOT` for an output under the project root when `--allow-root` used the non-canonical temp path.
  - Fix: Canonicalize the allowed root and the nearest existing target parent before deciding containment, then retain the symlink-target checks that prevent escaping the allowed root. Add a regression test with a symlinked root, or specifically the `/var` to `/private/var` alias on macOS, covering both output/run-dir writes and question-file reads.
  - Requirement: p03-t01 output/run-dir/allow-root handling

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md`, `implementation.md`, `state.md`, p03 commit diffs, p03 source/tests, generated panel runtime, generated-output build mapping.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| p03-t01 parser and prompt contract | partial | `--question`/`--question-file`, exactly-one-source validation, empty/oversized rejection, panel flags, neutral prompt framing, response schema validation, and artifact rendering are implemented and tested. `--allow-root` path confinement incorrectly rejects canonical-equivalent in-root paths. |
| p03-t02 provider execution and shortfall handling | partial | Source tests cover independent provider turns, configured defaults, explicit `--panelists`, `panel_size`, unavailable configured panelists, pass/fail status, failed artifacts, and no cross-feeding. The generated runtime schema-path defect prevents the shipped wrapper from successfully using real `consensus run` provider turns. |
| p03-t03 generated runtime output | partial | Build mappings include the panel wrapper and sibling `consensus-config.mjs`; `pnpm run build:check` passes; `--help` exits successfully. The generated wrapper points to a missing schema asset at runtime. |
| OAT tracking | implemented | `implementation.md` marks Phase 3 `review_pending` with 3/3 tasks complete and next task `p04-t01`; `state.md` says p03 code review pending and p04-t01 next; `plan.md` marks p02 code review passed and p03 pending. |

### Extra Work (not in declared requirements)

None significant. The p03 `.oxfmt`/`.oxlint` exclusions for generated panel outputs are in scope for generated runtime handling.

## Verification Commands

Commands run during review:

```bash
pnpm exec vitest run tests/consensus/panel/wrapper.test.ts tests/consensus/panel/panel-schema.test.ts tests/consensus/panel/provider-cli-integration.test.ts
pnpm run type-check
pnpm run build:check
node plugins/consensus/skills/panel/scripts/consensus-panel.mjs --help
node --input-type=module -e "import { panelResponseSchemaPath } from './plugins/consensus/skills/panel/scripts/consensus-panel.mjs'; import { existsSync } from 'node:fs'; const p = panelResponseSchemaPath(); console.log(p); process.exit(existsSync(p) ? 0 : 1);"
```

Results: the targeted Vitest suite, `type-check`, `build:check`, and generated `--help` passed. The generated schema-path existence check failed, matching the Critical finding.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks.
