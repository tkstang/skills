---
oat_generated: true
oat_generated_at: 2026-07-03
oat_review_scope: p01
oat_review_type: code
oat_project: /Users/tstang/Code/feat-consensus-panel/.oat/projects/shared/consensus-panel
---

# Code Review: p01

**Reviewed:** 2026-07-03
**Scope:** Phase 1 code review for `505b227..58bec79`
**Files reviewed:** 6
**Commits:** `505b227..58bec79` (3 commits)

## Summary

Phase 1 adds the shared config module, provider CLI config commands, tests, and regenerated provider CLI output, and the targeted tests, type-check, and generated-output checks pass. The phase is not ready to pass because the resolver still lets lower-precedence persisted panel sizing affect explicit panelist overrides, malformed config files without `schema_version` are accepted as v1, and project bookkeeping still marks all Phase 1 tasks pending. There is also a schema alignment gap between the implementation and `design.md` that should be resolved before downstream docs and panel work build on this persisted config contract.

## Findings

### Critical

None

### Important

- **Panel invocation overrides can still be changed by lower-precedence config** (`src/consensus/config/consensus-config.ts:278`)
  - Issue: `resolvePanelComposition()` picks `panelists` and `panel_size` independently from the first candidate that has each field. If an explicit invocation supplies `panelists` but a project/user config supplies `panel_size`, the persisted size can trim or expand the explicit panelist list. That violates the Phase 1 requirement that explicit invocation overrides win over project/user defaults.
  - Fix: Treat an invocation-provided panelist composition as complete unless invocation also supplies `panel_size`; do not apply lower-precedence `panel_size` to explicit `panelists`. Add tests for `invocation.panelists` with conflicting project/user `panel_size`, and for `invocation.panel_size` source reporting.
  - Requirement: p01-t01 explicit invocation override precedence

- **Config parser accepts files without required `schema_version`** (`src/consensus/config/consensus-config.ts:106`)
  - Issue: `parseConsensusDefaultsConfig()` only rejects `schema_version` when it is present and not `v1`; missing versions are silently rewritten to `schema_version: "v1"`. The design validation rules require `schema_version` to be `v1`, and generated CLI verification confirmed `config set --from-file` accepts an unversioned config and exits 0.
  - Fix: Require `value.schema_version === "v1"` for parsed persisted/from-file configs, then add unit and CLI tests that a missing `schema_version` produces a usage error.
  - Requirement: p01-t01 valid/invalid config parsing; p01-t02 malformed config handling

- **Persisted config schema shape conflicts with the design data model** (`src/consensus/config/consensus-config.ts:49`)
  - Issue: The implementation stores `peers`, `panelists`, `panel_size`, and `roles` as top-level fields and models `roles.advisor` / `roles.synthesizer` as arrays. `design.md` defines `ConsensusDefaultsConfig` with a nested `defaults?: ConsensusDefaults` object and single `advisor` / `synthesizer` role refs. This may be a defensible implementation choice, but the lifecycle artifacts now describe a different persisted schema than the code and tests.
  - Fix: Choose one contract before continuing. Either change code/tests/generated output to match the nested design schema, or update `design.md` to bless the flat persisted schema and array role defaults so Phase 3 docs and panel code build on the real contract.
  - Requirement: Design Data Models; p01-t01 config schema

- **Implementation tracker still marks Phase 1 as not started** (`.oat/projects/shared/consensus-panel/implementation.md:29`)
  - Issue: The dispatch describes a completed Phase 1, and the commit range contains `p01-t01`, `p01-t02`, and `p01-t03`, but `implementation.md` still says Phase 1 is `in_progress`, `0/3` complete, `oat_current_task_id: p01-t01`, and all three task records are pending with no commit SHAs. This can cause OAT resume/review-receive routing to restart completed work or misreport project state.
  - Fix: Update `implementation.md` to record all Phase 1 tasks as completed with commits `42e3d19`, `866b818`, and `58bec79`, set the next task to `p02-t01`, and record the Phase 1 verification commands/results.

### Medium

None

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:**

- `.oat/projects/shared/consensus-panel/discovery.md`
- `.oat/projects/shared/consensus-panel/design.md`
- `.oat/projects/shared/consensus-panel/plan.md`
- `.oat/projects/shared/consensus-panel/implementation.md`
- Git range `505b227..58bec79`
- Changed files in the authoritative range

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| p01-t01 config schema/store/resolver | partial | Module, storage paths, clear-key support, precedence tests, panel sizing, and role persistence exist, but panel invocation precedence is incomplete and schema validation accepts missing `schema_version`. |
| p01-t02 provider CLI config commands | partial | `get`, `list`, `set`, and `clear` are implemented and tested, but `set --from-file` accepts unversioned malformed configs. |
| p01-t03 generated provider CLI runtime | implemented | `plugins/consensus/scripts/consensus.mjs` is regenerated, `pnpm run build:check` passes, and generated `config get/list` commands return JSON envelopes with temp HOME/XDG isolation. |
| Design config data model | partial | The implemented persisted schema and role shapes conflict with the current design artifact; align code or design before downstream phases rely on the contract. |

### Extra Work (not in declared requirements)

None

## Verification Commands

Run these to verify the implementation and fixes:

```bash
pnpm exec vitest run tests/consensus/config/consensus-config.test.ts tests/consensus/provider-cli/config-commands.test.ts
pnpm run type-check
pnpm run build:check
tmp=$(mktemp -d); mkdir -p "$tmp/project" "$tmp/home" "$tmp/xdg"; printf '{"peers":[{"provider":"claude"},{"provider":"codex"}]}' > "$tmp/no-schema.json"; HOME="$tmp/home" XDG_CONFIG_HOME="$tmp/xdg" node plugins/consensus/scripts/consensus.mjs config set --json --scope project --cwd "$tmp/project" --from-file "$tmp/no-schema.json"; rm -rf "$tmp"
```

Expected after fixes: the targeted Vitest suite, type-check, and build-check pass; the unversioned `--from-file` command exits with a usage error instead of writing config.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks.
