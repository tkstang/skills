---
oat_generated: true
oat_generated_at: 2026-06-26
oat_review_scope: p03
oat_review_type: code
oat_project: /Users/tstang/Code/feat-public-discovery/.oat/projects/shared/public-discovery
---

# Code Review: p03

**Reviewed:** 2026-06-26
**Scope:** Phase 3 code review for `p03-t01` and `p03-t02`
**Files reviewed:** 2
**Commits:** `bfe5684733b732e8907d583475e36ab279f96707..e8ab87a23e0107ad65dcaabd28b93c21d556ad24` (2 commits)

## Summary

Phase 3 records the required standalone install/run smoke checks, isolated HOME usage for install and recovery checks, local consensus recovery simulation, hosted skills.sh checks, and the required cat-3 deferral. One Important evidence gap remains: the discovery artifact uses an ellipsis for the `--list` output, so it does not actually preserve the complete evidence needed to prove `session-observer` and `export-session-transcript` are the only standalone `skills/` entries in the remote discovery result.

Artifacts available and used: `plan.md`, `implementation.md`, `discovery.md`, `design.md`, `state.md`, `.oat/projects/shared/public-discovery/verification/cli-discovery.md`, and `.oat/repo/pjm/backlog/items/BL-260621-control-public-skill-discovery.md`. `spec.md` was listed in the dispatch payload but is absent; this is acceptable for quick mode. I also spot-checked the live `skills@1.5.13` CLI and the cited skills.sh/Vercel sources.

## Findings

### Critical

None

### Important

- **Discovery evidence does not preserve the complete `--list` output needed to prove the "only standalone entries" claim** (`.oat/projects/shared/public-discovery/verification/cli-discovery.md:100`)
  - Issue: Plan task `p03-t01` requires the verification artifact to capture the `--list` output and confirm that `session-observer` and `export-session-transcript` are present and the only individually-installable standalone `skills/` entries. The artifact records `Found 64 skills`, the two standalone names, an ellipsis, and the five consensus names, but it does not preserve the complete discovered list or a filtered command/result proving that no other `skills/` entries appeared. The phase's core deliverable is the evidence artifact, so downstream reviewers cannot verify the absence claim from the committed record alone.
  - Fix: Re-run the isolated published-CLI discovery check and update `verification/cli-discovery.md` with either the full `Available Skills` output or an additional concrete filtered check whose output proves the remote discovery result contains exactly the two standalone `skills/` entries plus the expected consensus/plugin and deferred OAT tooling entries.
  - Requirement: `p03-t01`; plan lines 378-415.

### Medium

None

### Minor

- **Backlog item still carries superseded consensus-hiding guidance after recording the new strategy** (`.oat/repo/pjm/backlog/items/BL-260621-control-public-skill-discovery.md:153`)
  - Issue: The new 2026-06-26 finding correctly records that hosted visibility should wait for cat-3 upstream hiding and that consensus hosted `internal` behavior remains unverified. Later in the same backlog item, the unchanged "Open questions / design tensions" section still frames marking consensus `refine`/`evaluate` internal as an active option to verify, even though the project design has resolved category 2 as "stay discoverable + recoverable standalone" across all five consensus skills.
  - Suggestion: Mark that older note as historical/superseded or revise it to the current accepted strategy so future backlog triage does not reopen a design decision this project already settled.

## Requirements/Design Alignment

**Evidence sources used:** quick-mode sources `discovery.md` and `plan.md`; optional `design.md`; implementation tracking in `implementation.md` and `state.md`; changed files in the specified commit range; live spot checks against `skills@1.5.13`, `skills.sh`, Vercel's skills KB, skills.sh docs/API docs, the Vercel changelog, and vercel-labs/skills issue #880.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| `p03-t01` Step 1: verify discovery entries via remote `--list` | partial | Published CLI command is recorded and live spot-check passes, but the artifact does not include full output or filtered proof for "only standalone entries." |
| `p03-t01` Step 2: install and run standalone `session-observer` and `export-session-transcript` in isolation | implemented | Both install commands use temp HOME/cache/XDG paths and record exit 0 plus entrypoint `--help` output. |
| `p03-t01` Step 3: simulate standalone consensus recovery | implemented | The artifact records a copied `refine` skill without plugin scripts, an isolated HOME, the actionable missing-CLI message, `install.sh`, and resolver source `shared-home`. |
| `p03-t02` Step 1: investigate hosted skills.sh crawl/submission behavior | implemented | The backlog finding records dated `skills find`, direct repo page, hosted search, API, and source-doc checks. Live spot checks matched the recorded absence from hosted search and 404 direct repo route. |
| `p03-t02` Step 2: record listing strategy and cat-3 deferral | implemented | The backlog finding records no listing claim now, post-cat-3 seeding/verification strategy, hosted `metadata.internal` uncertainty, and explicit cat-3 deferral. |

### Extra Work (not in declared requirements)

None in the reviewed commit range.

## Verification Commands

Run these to verify fixes:

```bash
tmp_home=$(mktemp -d /tmp/public-discovery-home.XXXXXX)
tmp_cache=$(mktemp -d /tmp/public-discovery-npm.XXXXXX)
HOME="$tmp_home" \
  npm_config_cache="$tmp_cache" \
  XDG_CONFIG_HOME="$tmp_home/.config" \
  XDG_STATE_HOME="$tmp_home/.local/state" \
  XDG_CACHE_HOME="$tmp_home/.cache" \
  CI=1 \
  DO_NOT_TRACK=1 \
  npx -y skills@1.5.13 add tkstang/skills --list
```

```bash
npm_config_cache="$(mktemp -d /tmp/public-discovery-npm.XXXXXX)" \
  CI=1 \
  DO_NOT_TRACK=1 \
  npx -y skills@1.5.13 find tkstang
```

```bash
curl -sS -L -w '\nHTTP_STATUS=%{http_code}\nFINAL_URL=%{url_effective}\n' https://skills.sh/tkstang/skills
```

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks.
