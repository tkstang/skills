---
oat_generated: true
oat_generated_at: 2026-06-26
oat_review_scope: p01
oat_review_type: code
oat_project: /Users/tstang/Code/feat-public-discovery-worktrees/public-discovery/p01/.oat/projects/shared/public-discovery
---

# Code Review: p01

**Reviewed:** 2026-06-26
**Scope:** Phase 1: Consensus standalone recovery
**Files reviewed:** 19
**Commits:** 4071714bf48ca7ecbcb5487a8f4bbf6b3208677f..97e304002f69ee0f303dd1d7b49f567e1c770898

Note: the dispatched head SHA `97e30404030ed458d9308e7570a21bb1d59274dc` was not present after `git fetch origin`. I reviewed the current p01 worktree `HEAD` (`97e304002f69ee0f303dd1d7b49f567e1c770898`), which matches the dispatched abbreviated prefix `97e3040` and contains the four p01 commits.

## Summary

Phase 1 implements the planned consensus standalone recovery path: resolver fallback, shared actionable missing-CLI error, sandboxed installer tests, README/install contract coverage, and generated runtime sync are all present. The phase is blocked by one Important finding: the skill-version gate fails against current `origin/main` because `evaluate` is changed by this branch but its version is no longer ahead of the merge base after `origin/main` advanced.

## Findings

### Critical

None

### Important

- **`evaluate` skill version is not bumped relative to current `origin/main`** (`plugins/consensus/skills/evaluate/SKILL.md:4`)
  - Issue: Phase 1 changes generated runtime under `plugins/consensus/skills/evaluate/`, including `plugins/consensus/skills/evaluate/scripts/consensus-loop.mjs`, but `evaluate` remains at `version: '0.1.2'` / `metadata.version: '0.1.2'`. Current `origin/main` already has `evaluate` at `0.1.2`, so the required gate fails: `pnpm run validate:skill-versions --base-ref origin/main` reports `evaluate: Changed skill evaluate must bump SKILL.md version (still 0.1.2)`.
  - Fix: Bump both `version` and `metadata.version` for `plugins/consensus/skills/evaluate/SKILL.md` to the next release value for the current target base, likely `0.1.3` if `origin/main` still has `0.1.2`, then rerun `pnpm run validate:skill-versions --base-ref origin/main`.
  - Requirement: p01-t05

### Medium

None

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md`, `implementation.md`, changed files in `4071714bf48ca7ecbcb5487a8f4bbf6b3208677f..HEAD`. Quick mode does not require `spec.md`; the listed `spec.md` path was not present.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| p01-t01 Add `~/.consensus/` fallback to shared resolver | implemented | `resolveConsensusCliPathDetails` now checks explicit arg, env, plugin path, then `~/.consensus/consensus.mjs`; resolver tests cover sandboxed `HOME` and missing resolution. Generated outputs are in sync. |
| p01-t02 Centralize actionable missing-CLI error | implemented | Shared `CONSENSUS_PROVIDER_CLI_MISSING` helper is in core and the all-five-wrapper test verifies one shared message for `refine`, `evaluate`, `decide`, `plan`, and `create`. |
| p01-t03 Add pinned-fetch `install.sh` | implemented | `install.sh` is Node-22-aware, idempotent, prefers checkout copy, supports mocked remote fetch, and tests write only under a sandboxed `HOME`. |
| p01-t04 Document alternative install and assert contract | implemented | README documents `v0.1.2` pinned install and the contract test aligns README, `install.sh`, and resolver shared path while rejecting moving refs/placeholders. |
| p01-t05 Final validation | partial | `pnpm run build:check`, `pnpm run validate`, targeted p01 Vitest tests, and `pnpm test` pass. `pnpm run validate:skill-versions --base-ref origin/main` fails for `evaluate`; see Important finding I1. |

### Extra Work (not in declared requirements)

None

## Verification Commands

Run these to verify the implementation and the required fix:

```bash
pnpm run build:check
pnpm run validate
pnpm run validate:skill-versions --base-ref origin/main
pnpm exec vitest run tests/consensus/core/resolve-consensus-cli-path.test.ts tests/consensus/provider-cli/missing-cli-message.test.ts tests/consensus/install-sh.test.ts tests/consensus/install-contract.test.ts
pnpm test
```

Observed during review:

- `pnpm run build:check` passed.
- `pnpm run validate` passed.
- `pnpm exec vitest run tests/consensus/core/resolve-consensus-cli-path.test.ts tests/consensus/provider-cli/missing-cli-message.test.ts tests/consensus/install-sh.test.ts tests/consensus/install-contract.test.ts` passed: 4 files, 10 tests.
- `pnpm test` passed: 88 files passed, 1 skipped; 866 tests passed, 1 skipped.
- `pnpm run validate:skill-versions --base-ref 4071714bf48ca7ecbcb5487a8f4bbf6b3208677f` passed for the review range's local base.
- `pnpm run validate:skill-versions --base-ref origin/main` failed for `evaluate` as described above.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks.
