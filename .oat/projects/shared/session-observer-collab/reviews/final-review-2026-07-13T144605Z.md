---
oat_generated: true
oat_generated_at: 2026-07-13T14:46:05Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: gate
oat_project: .oat/projects/shared/session-observer-collab
oat_gate_run_id: b756e4d5-ec01-420c-bec5-71a136907ed9
oat_gate_target: claude-fable-skip-permissions
oat_gate_runtime: claude
oat_invocation_model: fable
oat_invocation_reasoning_effort: provider-default
oat_invocation_source: exec-target-config
---

# Code Review: final

**Reviewed:** 2026-07-13T14:46:05Z
**Scope:** Independent fresh-context final review of the entire session-observer-collab project (base observer hardening + compositional N=2 collaboration skill), phases p01–p07 plus post-review closeout commits.
**Files reviewed:** 123 changed files across the range
**Commits:** `82a2645da74abc4b4ed2942cfb8d55b66472d54f..HEAD` (110 commits)

**Dispatch evidence:** launcher-selected oat-reviewer (claude); model_axis=selected:opus; effort_axis=not-applicable; provenance=launcher-selected/config-declared; runtime identity: not-reported; managed high policy from project-state.

## Summary

The project ships collaboration-safe transcript semantics, a hardened Session Observer surface (`whoami`, quiet-empty, baseline-gap, newer-session warnings), and a dependency-free `session-observer-collab` sibling skill with versioned lease control, bounded lifecycle continuation, and honest per-runtime wake-tier disclosure. All six phase reviews and the prior final re-review passed; this independent fresh-context pass re-verified the substantive p07 fixes and the post-review closeout commits (docs generation, DR records, and the workflow/architecture diagrams) that no prior review covered. Full verification is green (1090 tests pass with one intentional skip, generated-output parity in sync, repository validation passing), skill versions are internally consistent, and the empty deferred ledger is consistent with implementation.md.

Findings: 0 critical, 0 important, 0 medium, 0 minor

## Findings

### Critical

None

### Important

None

### Medium

None

### Minor

None

## Spec/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md`, `implementation.md`, and cited product surfaces under `src/transcript/**` and `skills/session-observer-collab/**` (quick-mode required artifacts present; design.md present and used).

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| Render queued Claude input exactly once (p01-t01) | implemented | `src/transcript/core/runtimes.ts` normalizes `queue-operation`/`queued_command` into one `queued-user` entry. |
| Classify synthetic wakes as automatic control (p01-t02, p07-t01) | implemented | XML `session_observer_wake` envelope parsed via `parseAutomaticControlEnvelope` (`runtimes.ts:189`) with retained JSON compat (`runtimes.ts:131`); renders `Hook/control (automatic)`, `origin: automatic-control`. |
| Buffer Cursor turns to completion (p01-t03) | implemented | Terminal `turn_ended` buffering; diagnostics for aborted/error/cancelled. |
| Preserve recoverable user messages (p01-t04) | implemented | Recovery pointer with transcript path + zero-based record index. |
| Fail-closed `whoami` + candidate recovery (p02-t01, p07-t04) | implemented | Conflicting signals return exit 3 with `ambiguousIdentity` + candidates (`session-observer.ts:433`, `observe.ts:116`). |
| Quiet-empty / strict-baseline / newer-session (p02-t02..t04) | implemented | Offset-advancing quiet-empty, baseline-gap warnings, non-switching newer-session candidates. |
| Base observer surface regenerated + documented (p02-t05) | implemented | Generated bundles in sync (build:check); observer 1.0.5, export 1.0.3 version-synced. |
| Sibling skill, protocol, lease control, completion selection (p03) | implemented | Public canonical `skills/session-observer-collab/`; versioned XDG leases with CAS cursor/count. |
| Codex lifecycle adapter + live acceptance (p04) | implemented | Thin `codex-stop.mjs`; live evidence recorded; deferred `supportRemoved` declaration alignment resolved (declarations present in `codex-lifecycle.d.ts` + `codex-install.d.ts`). |
| Cursor + Claude adapters, honest labeling (p05) | implemented | Cursor documented-but-unvalidated; Claude Monitor unvalidated/buffered-manual — labeled honestly per discovery constraint. |
| Distribution/docs/PJM closeout (p06) | implemented | Layout/release tooling registered; user+engineering docs; four v2 backlog items; sanitization matrix. |
| Route setup by acting runtime + parameterize peer pin (p07-t02, p07-t06) | implemented | `runtime-claude-code.md` uses parameterized `<peer-runtime>:<peer-session-id>`; hardcoded `claude-code:<peer>` prefix removed. |
| Preserve repeated queued human messages (p07-t03) | implemented | Transaction-ordered `queuedContents` correlation via indexed splice, not global content dedup (`runtimes.ts:725`). |

### Extra Work (not in requirements)

Post-review closeout commit `8b03123` adds six source-backed Mermaid diagrams (collaboration sequence, delta-classification flow, generated-runtime topology, plus observer/export/refine visuals). This was an explicit user-requested documentation pass recorded in implementation.md Run 21; it is docs-only (touches only `documentation/docs/**`, no versioned skill sources), and the diagrams are accurate to design.md's data flow and boundaries. Not scope creep.

### Deferred Findings Re-evaluation

The final-scope deferred ledger is empty (0 Medium, 0 Minor) and nothing in `implementation.md` contradicts it. The single historical p04 Medium (`codex-lifecycle.d.ts` `supportRemoved` shape alignment) was deferred to p06 and resolved there; the declaration is present in both `skills/session-observer-collab/scripts/codex-lifecycle.d.ts:86` and `scripts/lib/codex-install.d.ts:15`, with type-check and build:check green. The other "deferred to p06-t01" references (collaboration skill version bump) are completed — collab is at 1.0.4. All deferral mentions are resolved historical narrative within closed phases, not open items.

## Verification Commands

Run these to verify the implementation:

```bash
npm test               # 108 files passed, 1 skipped; 1090 tests passed, 1 skipped
pnpm run build:check   # all generated bundles report "in sync"
npm run validate       # "validation passed"
```

All three commands were executed during this review and passed. Generated-output parity holds; no hand-edited `.mjs` drift.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks. With zero findings at every severity, this final review confirms the branch is ready for closeout/merge disposition.
