---
oat_generated: true
oat_generated_at: 2026-07-13T14:52:44Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: gate
oat_project: .oat/projects/shared/session-observer-collab
oat_gate_run_id: d356ee0f-16b1-4c8d-bfb2-216d72a05561
oat_gate_target: claude-fable-skip-permissions
oat_gate_runtime: claude
oat_invocation_model: fable
oat_invocation_reasoning_effort: provider-default
oat_invocation_source: exec-target-config
---

# Code Review: final

**Dispatch evidence:** launcher-selected oat-reviewer (claude); model_axis=selected:opus; effort_axis=not-applicable; provenance=launcher-selected/config-declared; runtime identity: not-reported; managed high policy from project-state.

**Reviewed:** 2026-07-13T14:52:44Z
**Scope:** final — full branch `82a2645..HEAD` (p01–p07 plus post-implementation closeout: summary, docs, DR records, diagrams, PR prep)
**Files reviewed:** 124 changed files (product surfaces prioritized; generated `.mjs` mirrors verified via parity, not hand review)
**Commits:** 111 (`82a2645da74abc4b4ed2942cfb8d55b66472d54f..HEAD`)

## Summary

This is a fresh, independent gate final review of the entire session-observer-collab branch. All three required verification gates pass (test 1090 passed / 1 intentional skip, build:check clean, validate clean), and I independently re-verified every substantive p07 final-review fix by reading canonical source, the shipped hook builders, and the cross-contract test rather than trusting prior conclusions. The Critical wake-envelope contract, the acting-runtime routing/pin fixes, the queued-input transaction correlation, and the conflicting-identity candidate recovery are all correctly implemented and covered by tests; the historical p04 Medium (`supportRemoved` declaration) is resolved.

Findings: 0 critical, 0 important, 0 medium, 0 minor

## Findings

### Critical

None

### Important

None

### Medium

None

### Minor

None

## Requirements/Design Alignment

**Evidence sources used:** discovery.md, design.md, plan.md, implementation.md (quick mode; design.md present and used). Canonical source under `src/transcript/`, shipped skill under `skills/session-observer-collab/`, generated mirrors under `skills/session-observer/` and `skills/export-session-transcript/`, and the collaboration/observer test suites.

### Requirements Coverage

| Requirement (design/discovery) | Status | Notes |
| --- | --- | --- |
| Unified automatic wake-envelope contract across Claude/Codex/Cursor (Critical fix, p07-t01) | implemented | `src/transcript/core/runtimes.ts:152-238` adds XML envelope parsing (production wire format) with attribute decoding + range validation, falling back to JSON. Shipped builders emit matching XML (`hooks/codex-stop.mjs:66-75`, `hooks/cursor-stop.mjs:54-63`). `tests/session-observer-collab/wake-envelope-contract.test.ts:67-128` feeds literal builder output through all three normalizers and asserts `Hook/control (automatic)`, `origin: automatic-control`, unengaged/0-genuine engagement, and `selectCompletedContinuation` returning `budgetCost: 0` / `no-continuation`. |
| Acting-runtime setup routing; peer runtime used only for pin (Important, p07-t02/t06) | implemented | No hardcoded `claude-code:` peer prefix remains in `references/runtime-claude-code.md` (grep clean). Monitor recipe consumes the confirmed exact peer pin; routing tests (`runtime-reference-routing.test.ts`, `runtime-claude-code-reference.test.ts`) pass. |
| Repeated queued human messages remain distinct (Medium, p07-t03) | implemented | `src/transcript/core/runtimes.ts` correlates attachment copies with their ordered enqueue/remove transaction (`queuedContents`/`deliveredQueuedContents` splice-by-transaction) instead of transcript-global content dedup. Covered by `fixtures/claude-code/queued-repeated-input.jsonl` + digest tests. |
| Conflicting identity signals → fail-closed candidate recovery, not collapse to noIdentity (Medium, p07-t04) | implemented | `lib/observe.ts` returns `{ ambiguous: true, candidates }` on conflicting harness signals; `session-observer.ts:433-454` maps to exit 3 with `ambiguousIdentity` + candidates, while genuine no-identity stays exit 2 (`:456-463`). CLI/locate tests cover conflicting runtimes and session-ID aliases. |
| Branch-introduced lint warning removed (Minor, p07-t05) | implemented | `digest.ts` `transcriptPath` shadow removed; verification records five remaining warnings as pre-existing/unrelated. |
| Fail-closed `whoami`, quiet-empty offset advance, baseline-gap detection, newer-session warnings, user-recovery pointers | implemented | p01/p02 canonical modules; regenerated bundles in parity (build:check clean). |
| N=2 collaboration skill: protocol, versioned leases, control ops, completion selection, runtime adapters | implemented | `skills/session-observer-collab/` scripts + references; control/completion/codex-hook/cursor-hook suites pass. |
| Codex lifecycle validated live; Cursor + Claude Monitor remain honestly labeled documented-but-unvalidated | implemented | `references/runtime-cursor.md:11,133,138` and `runtime-claude-code.md:21-22,112,130` preserve unvalidated/buffered-manual labels. |
| Deferred v2 capabilities have canonical backlog IDs | implemented | Four `BL-260713-*` items present under `.oat/repo/pjm/backlog/items/`. |
| No live lease state / secrets / home paths shipped | implemented | No lease JSON under the shipped skill (only `lease-state.mjs` source); grep for home paths/API keys in shipped scripts is clean. |
| Shipped runtime dependency-free; skill versions synchronized | implemented | observer 1.0.5/1.0.5 and collab 1.0.4/1.0.4 (top-level == metadata); export carries metadata.version 1.0.3 only (no top-level key — allowed by the "when both present" rule). validate passes. |

### Extra Work (not in declared requirements)

None material. Post-implementation closeout (summary, Mermaid diagrams, DR records, backlog, PR prep) is expected lifecycle bookkeeping within scope.

## Deferred Findings Re-evaluation

- **Deferred Medium ledger (count 0):** Empty. Nothing to re-evaluate.
- **Deferred Minor ledger (count 0):** Empty. Nothing to re-evaluate.
- **Historical p04 Medium — `codex-lifecycle.d.ts` `supportRemoved` alignment (resolved in p06):** Confirmed resolved by direct inspection. `codex-lifecycle.d.ts:86` declares `supportRemoved?: boolean` (optional), which correctly matches the runtime: the no-registration uninstall path (`codex-lifecycle.mjs:334-341`) omits `supportRemoved`, while the removed-script path returns it (`:356-367`); `lib/codex-install.mjs:229-247` supplies the boolean. The optional declaration is accurate to both return shapes. Not re-opened.

## Verification Commands

Run to reproduce this review's results:

```bash
pnpm run test        # 108 files / 1090 tests passed, 1 skipped (exit 0)
pnpm run build:check # generated-output parity clean (exit 0)
pnpm run validate    # repository structure/manifest/docs invariants clean (exit 0)
```

All three passed. Spot-check commands used during review:

```bash
git diff --name-only 82a2645..HEAD
grep -rn "session_observer_wake" skills/session-observer-collab/scripts/hooks/*.mjs
grep -n "claude-code:" skills/session-observer-collab/references/runtime-claude-code.md   # no hardcoded peer pin
```

## Recommended Next Step

Run the `oat-project-review-receive` skill to record this clean final review. No fix tasks are required; this branch is ready for gate pass.
