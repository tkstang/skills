---
oat_generated: true
oat_generated_at: 2026-07-13T04:54:34Z
oat_review_scope: p06
oat_review_type: code
oat_review_invocation: gate
oat_project: .oat/projects/shared/session-observer-collab
oat_gate_run_id: 7c5db570-43cf-4a01-b026-bf9402999552
oat_gate_target: claude-fable-skip-permissions
oat_gate_runtime: claude
oat_invocation_model: fable
oat_invocation_reasoning_effort: provider-default
oat_invocation_source: exec-target-config
---

# Code Review: p06

**Reviewed:** 2026-07-13T04:54:34Z
**Scope:** p06 — Integrate skill distribution/release invariants, publish docs, record v2 deferrals, run the acceptance/sanitization matrix, and verify clean closeout (`3fa3970^..HEAD`)
**Files reviewed:** 31
**Commits:** 12 (`3fa3970`..`c1ea7b5`)

Dispatch: role=reviewer scope=p06 action=review target=opus harness=claude policy=managed/high model_axis=selected:opus effort_axis=not-applicable provenance=launcher-selected/config-declared runtime_identity=not-reported

## Summary

p06 is a well-executed integration/closeout phase: the distribution guards (`validate.mjs`, `bump-version.mjs`, layout/versioning/frontmatter tests) are robust and pin the canonical three-skill public set with dependency-free enforcement; the deferred p04 `codex-lifecycle.d.ts` `supportRemoved` mismatch is correctly resolved and covered; the four v2 backlog items map exactly to the discovery deferrals; and the PJM/reference narrative is unusually honest about unverified external surfaces and validated-versus-documented wake tiers. This fresh re-review found no code defects. It did surface two Medium bookkeeping/artifact-drift issues that the passing test suite and `worktree:validate` do not guard: a stale generated docs navigation manifest, and stray template markers on the new backlog items. Neither blocks functionality; both should be cleaned up before closeout.

Findings: 0 critical, 0 important, 2 medium, 1 minor

## Findings

### Critical

None

### Important

None

### Medium

- **Generated docs navigation manifest is stale — new page missing** (`documentation/index.md`)
  - Issue: p06-t02 added `documentation/docs/user-guide/skills/session-observer-collab.md` and correctly updated the authored `## Contents` in `docs/user-guide/skills/index.md` plus `meta.json`, but the generated root manifest `documentation/index.md` was not regenerated. Running `cd documentation && oat docs generate-index --docs-dir docs --output index.md` produces a one-line drift that inserts `[Session Observer Collaboration](user-guide/skills/session-observer-collab.md)` under the Skills group. The `documentation/AGENTS.md` authoring contract (step 5, "Regenerate the derived navigation artifact") requires this after adding a page. The committed manifest is the machine-readable nav map for agents; until regenerated, the new page is effectively invisible to that map. No test guards it — `tests/repo/docs-presence.test.ts` checks the authored `## Contents` and `meta.json` only, and `worktree:validate` (test + validate + smoke) does not run the docs prebuild — so the drift was not caught even though the phase reported green. It self-heals on the Fumadocs `predev`/`prebuild`, so runtime/user impact is low, but the committed artifact violates the docs contract and the repo's generated-output-freshness discipline.
  - Fix: Run `cd documentation && oat docs generate-index --docs-dir docs --output index.md` and commit the regenerated `documentation/index.md`. Because this is artifact drift on a defensible implementation (authored source is correct), an alignment task that regenerates the manifest is sufficient; no source-page change is needed.
  - Requirement: p06-t02 (publish user/engineering documentation; navigation accuracy)

- **New v2 backlog items carry stray template markers** (`.oat/repo/pjm/backlog/items/BL-260713-*.md`)
  - Issue: All four new items (`BL-260713-per-observer-offsets-and-safe`, `BL-260713-stronger-cursor-collaboration`, `BL-260713-cursor-transcript-store`, `BL-260713-optional-idle-session`) carry `oat_template: true` and `oat_template_name: backlog-item` in frontmatter. None of the nine pre-existing real backlog items carry these keys — they are the scaffold markers left over from the item template. These are real, open, deliverable follow-ups, not templates. They regenerate into the index correctly and `validate` passes, so functional impact today is nil, but any template-aware tooling that filters `oat_template: true` files could mis-handle them (skip, treat as scaffolds, or exclude from real-item processing), and the markers are inconsistent with every other item in the store.
  - Fix: Remove the `oat_template: true` and `oat_template_name: backlog-item` lines from all four `BL-260713-*.md` files (matching the frontmatter shape of existing items such as `BL-260701-add-multi-round-panel.md`), then `oat backlog regenerate-index` and confirm no index change.
  - Requirement: p06-t03 (record intentional v2 deferrals as clean file-backed items)

### Minor

- **No freshness guard for the generated docs manifest** (`tests/repo/docs-presence.test.ts`)
  - Issue: The docs-presence suite asserts the authored `## Contents` link and `meta.json` entry for the new page, but nothing asserts that the committed `documentation/index.md` manifest matches a fresh `generate-index` run. This coverage gap is the direct reason the Medium drift above shipped undetected through a "green" phase. It is a pre-existing test-scope limitation, not introduced by p06, but p06 is the first change to expose it.
  - Suggestion: Consider a lightweight drift guard (regenerate to a temp path and compare, or assert the manifest contains each `meta.json` page) analogous to `tests/tooling/generated-output-sync.test.ts` for `.mjs`. Optional; out of strict p06 scope.

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`, `design.md`, `plan.md`, `implementation.md` (quick mode; `spec.md` absent by design; `design.md` present and applied). Verified against source in `skills/session-observer-collab/`, `scripts/validate.mjs`, `scripts/bump-version.mjs`, `src/transcript/session-observer/`, the four `BL-260713-*` items, and the `documentation/` tree.

### Requirements Coverage

| Requirement | Status | Notes |
| ----------- | ------ | ----- |
| p06-t01 register distribution + release invariants | implemented | `validateCollaborationSkillDistribution` (required-file, public-flag, dependency-free import checks); `SKILL_FILES` includes the collab skill; version bump 1.0.1→1.0.2 (top-level + metadata in sync); layout/versioning/frontmatter guards pin the canonical 3-skill set and enforce no `internal: true`. `validate`, `validate-skill-versions --base-ref origin/main`, and targeted suites pass. |
| p04 deferral: `codex-lifecycle.d.ts` `supportRemoved` | implemented | Declaration now `supportRemoved?: boolean`; runtime no-registration return path (`exact.length === 0`) omits the field while the removal path sets it; `codex-lifecycle-declaration.test.ts` covers both. Correctly closes the deferred p04 Medium. |
| p06-t02 publish user/engineering docs | partial | New collab page, `session-observer.md` collaboration-flags table (all referenced flags verified present in `src/transcript/session-observer/session-observer.ts`), engineering repository-layout + generated-runtime authored-`.mjs` boundary notes, authored `## Contents` + `meta.json` all correct and honest about validation status. Gap: generated `documentation/index.md` manifest not regenerated (Medium finding 1). |
| p06-t03 record v2 deferrals | partial | Four backlog items cover exactly the discovery Deferred Ideas (N>2 offsets, stronger Cursor wake, Cursor transcript-store/slug, optional idle-session); index, current-state, and roadmap updated; shared-log/direct-messaging initiatives preserved. Gap: stray template markers (Medium finding 2). |
| p06-t04 acceptance/sanitization matrix | implemented | `build:check` in sync; targeted repo/release/collab/export/transcript-core suites pass; `.session-observer/` historical wording reconciled in `references/README.md` + `closeout-runbook.md`; scheduled-poll claims conditioned on effective-scheduler proof in SKILL.md/runtime-cursor.md/docs; Cursor fixture terminal `turn_ended` added without regression. Full-gate green state confirmed for the automated subset run here. |
| p06-t05 clean closeout + install handoff | implemented (evidence-recorded) | Operational-only work (freshness fail-closed, no residual watcher/lease, retained trusted Codex hook, user-install + provider-link parity) recorded in implementation.md; produced empty commits with no repository diff, so not independently re-verifiable from the tree — consistent with the documented deviation. |

### Extra Work (not in declared requirements)

None. All changes map to p06 tasks or the explicitly deferred p04 declaration cleanup routed to p06.

## Verification Commands

Run these to verify the current state and the fixes:

```bash
# Green-state checks confirmed during this review
node scripts/validate.mjs
node scripts/validate-skill-versions.mjs --base-ref origin/main
pnpm run build:check
pnpm exec vitest run tests/repo/layout.test.ts tests/repo/docs-presence.test.ts tests/repo/readme-scope.test.ts tests/repo/skill-frontmatter.test.ts tests/release/versioning.test.ts tests/session-observer-collab

# Medium finding 1 — reproduce/confirm the docs manifest drift, then fix
cd documentation && oat docs generate-index --docs-dir docs --output index.md
git -C .. diff --stat documentation/index.md   # expect the collab entry insertion, then commit it

# Medium finding 2 — confirm stray template markers, then fix
grep -l "oat_template: true" .oat/repo/pjm/backlog/items/BL-260713-*.md
# after removing the oat_template/oat_template_name lines:
oat backlog regenerate-index && git diff --check
```

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert these findings into plan tasks. Both Medium findings are artifact-alignment/bookkeeping drift on defensible implementations — suitable for lightweight alignment tasks (regenerate the docs manifest; strip the template markers and regenerate the backlog index) rather than code changes.
