---
oat_generated: true
oat_generated_at: 2026-09-13T04:49:00Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: auto
oat_project: .oat/projects/synced/coding-session-handoff
oat_review_base_sha: 20e86a100832b114a8aa3a20469b849de7ec7f45
oat_review_head_sha: b7a8d35f06acb0a850795e54ad40ea729caac8e5
oat_review_range: 20e86a100832b114a8aa3a20469b849de7ec7f45..b7a8d35f06acb0a850795e54ad40ea729caac8e5
oat_prior_review_artifact: .oat/projects/synced/coding-session-handoff/reviews/archived/final-review-2026-09-13T031000Z.md
oat_prior_review_head_sha: 20e86a100832b114a8aa3a20469b849de7ec7f45
oat_review_request_id: handoff-gate-fixes-final-review-20260913-01
oat_dispatch_scope: final-gate-fixes
oat_dispatch_action: review
oat_dispatch_role: reviewer
oat_dispatch_target: oat-reviewer-gpt-5-6-sol-max
oat_model_axis: selected:gpt-5.6-sol
oat_effort_axis: selected:max
oat_dispatch_policy: frontier
oat_dispatch_ceiling: max
oat_dispatch_rationale: Consequential fresh standard final review of five configured-gate repairs, their full-project integration, and the retained paused-executor boundary.
---

# Code Review: final

**Reviewed:** 2026-09-13T04:49:00Z
**Scope:** Narrowed re-review of the five configured-gate fixes with integration
checked against the complete implementation base
**Files reviewed:** 16 changed paths, plus governing artifacts, prior final reviews,
generated bundles, and retained-executor boundaries
**Commits:** 5
(`20e86a100832b114a8aa3a20469b849de7ec7f45..b7a8d35f06acb0a850795e54ad40ea729caac8e5`)
**Implementation base:** `0042b84937076f875380892b87a39120eeefad9e`
**Reviewed HEAD:** `b7a8d35f06acb0a850795e54ad40ea729caac8e5`
**Verdict:** PASS
**Reconnaissance:** not-attempted

## Summary

All five findings from the configured gate review are resolved in five ordered,
bounded commits. Guidance-only Claude and Codex discovery now retains attributable
source candidates while returning bounded path-free reason counts for individually
unattributable transcripts; strict observer, Cursor, and paused-executor discovery
remain fail closed. Cursor availability prose, interactive cwd refusal, preview
truncation reporting, and installed skill paths now match their contracts. No new
correctness, safety, documentation, generation, or integration finding was found.

Findings: 0 critical, 0 important, 0 medium, 0 minor

## Dispatch Provenance

- Request: `handoff-gate-fixes-final-review-20260913-01`
- Configured role/target: `oat-reviewer-gpt-5-6-sol-max`
- Configured model/effort: `gpt-5.6-sol`, `max`
- Policy/ceiling: Frontier, `max`
- Dispatch: scope=final-gate-fixes action=review role=reviewer producer=unknown
  provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:max
  dispatch_policy=frontier dispatch_ceiling=max
  target=oat-reviewer-gpt-5-6-sol-max

## Prior Gate Finding Dispositions

### I1 — Store-wide unrelated transcript failure: resolved

The shared exact-all reader now has an explicit `fail | summarize` policy whose
default is still `fail` (`src/transcript/session-observer/lib/types.ts:31`,
`src/transcript/session-observer/lib/locate.ts:456`). Only guidance discovery opts
Claude Code and Codex into `summarize`; Cursor retains the strict options
(`src/transcript/coding-session-handoff/guidance-discovery.ts:132`). An individually
malformed, oversized, unreadable, metadata-incomplete, or cwd-less transcript is
omitted under that opt-in and emits only a stable runtime/reason event
(`src/transcript/session-observer/lib/locate.ts:475`). The guidance projection reduces
those events to a sorted provider/reason/count summary and returns it with the exact
candidate set (`src/transcript/coding-session-handoff/guidance-discovery.ts:209`).

Enumeration, stat, aggregate-entry/byte, and deadline failures still throw, so the
summary policy does not represent an incomplete store walk as complete. The fixed
guidance options retain `persistence: forbid`, `recency: exact-all`, and their bounded
50,000-entry, 512 MiB, 256 KiB-per-entry, 30-second envelope
(`src/transcript/coding-session-handoff/guidance-discovery.ts:18`). Existing callers
that omit the new policy, including session-observer defaults and the retained
executor, keep strict all-or-error behavior. Focused strict-default tests and the
complete suite pass.

Filesystem-backed tests retain a Claude source candidate beside an unrelated cwd-less
transcript (`tests/session-observer/locate.test.ts:647`) and a Codex source candidate
beside an unrelated oversized record (`tests/session-observer/locate.test.ts:942`). A
projection test verifies stable aggregation and excludes private paths
(`tests/coding-session-handoff/guidance-discovery.test.ts:226`). An independent
synthetic run of the generated public bundle returned both attributable candidates,
one summary for each provider, no path/content in the summaries, and no persistent
cache file.

### M1 — Cursor discoverability claims: resolved

The public skill description and workflow now say that current Cursor transcript
discovery lacks independent exact cwd evidence, fails with path-free
`discovery-incomplete`, and cannot produce a selectable or previewable candidate
(`skills/coding-session-handoff/SKILL.md:3`,
`skills/coding-session-handoff/SKILL.md:22`). The provider reference identifies its
Cursor capability rows as future-facing evidence
(`skills/coding-session-handoff/references/provider-guidance.md:7`), and the user guide
states the same limitation at both the introduction and limitations surfaces
(`documentation/docs/user-guide/skills/coding-session-handoff.md:16`,
`documentation/docs/user-guide/skills/coding-session-handoff.md:81`). Contract tests
reject the prior reachable-candidate wording.

### M2 — Interactive cwd guard exits the shell: resolved

The destination command is now an `if` conditional: it uses `exec` only when
`pwd -P` equals the canonical destination and otherwise prints the refusal before
returning control to the shell (`src/transcript/coding-session-handoff/guidance.ts:98`).
The regression proves the provider runs in the correct cwd and that a wrong-cwd shell
continues while provider output remains absent
(`tests/coding-session-handoff/guidance.test.ts:95`). Shell-word quoting and
destination-only execution are unchanged.

### m1 — Preview truncation flag: resolved

Preview now records whether any retained entry text was shortened by the 4,000-code-
unit limit and ORs that signal with bounded-reader and entry-count truncation
(`src/transcript/coding-session-handoff/guidance-cli.ts:296`). The filesystem-backed
test covers both more than eight entries and mid-entry trimming
(`tests/coding-session-handoff/guidance-cli.test.ts:312`). Both lossy cases report
`truncated: true`.

### m2 — Installed skill runtime path: resolved

The skill's permission pattern and every workflow command use the sibling
`<skill-dir>/scripts/coding-session-handoff.mjs` convention
(`skills/coding-session-handoff/SKILL.md:9`,
`skills/coding-session-handoff/SKILL.md:34`). The contract test rejects the former
repository-relative path and checks all four occurrences
(`tests/coding-session-handoff/guidance-cli.test.ts:225`). The generated sibling bundle
is present, self-contained, and executable; no global installation or sync was run in
this review.

## Findings

### Critical

None.

### Important

None.

### Medium

None.

### Minor

None.

## Deferred Finding Dispositions

The five inherited Medium findings remain accepted only for the experimental
guidance-only product. The current fixes neither activate the retained executor nor
weaken its strict defaults.

| Inherited finding | Disposition after this review | Current basis |
| --- | --- | --- |
| p02 M1 — exact metadata-cap boundary | Remains accepted | Guidance summarizes the affected transcript as `metadata-prefix-incomplete` rather than failing unrelated attributable candidates; the narrow candidate-availability edge remains explicit. Strict executor behavior is unchanged. |
| p02 M2 — newline-bearing Git evidence schema | Remains accepted | Executor-only schema code is outside the fix range; public guidance emits no executor plan. |
| p03 M1 — empty Claude `--tools` operand round-trip | Remains accepted | Executor argv builder/parser code is unchanged and absent from the public bundle. |
| p03 M2 — authentication-required diagnostic collapse | Remains accepted | Public guidance performs no provider or authentication probe. |
| p03 M3 — raw alias comparison in execute/reconcile | Remains accepted | Public help and exports expose only `discover`, `preview`, and `prepare`; execute/reconcile remain paused. |

Any future executor activation invalidates these dispositions and requires a fresh
review. The new summary policy does not authorize that activation.

## Requirements/Design Alignment

**Evidence sources used:** current `spec.md` Revision Contract R1-R8; current
`design.md` accepted revision and historical strict discovery seam; `plan.md`,
including `prev1-t07` through `prev1-t11`; `implementation.md`; `state.md`; the prior
lifecycle final review through `20e86a100832b114a8aa3a20469b849de7ec7f45`;
configured-gate review `reviews/archived/final-review-2026-09-13T042209Z.md`; the exact
five-commit range; all 16 changed paths; generated runtimes; public skill/docs; and
focused/full verification.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| R1 | implemented | Prior entry-point and explicit-selection coverage is inherited. This pass verifies that unrelated transcript defects no longer erase attributable Claude/Codex candidates and that no recency selector was introduced. |
| R2 | implemented | The current spec/design explicitly separate guidance-only transcript summaries from strict consumers. The opt-in is bounded, zero-persistence, path-free, and limited to Claude/Codex; Cursor remains explicit incomplete. |
| R3 | inherited | Git destination validation is outside the narrowed range and remains covered by the passing prior final review and full suite. |
| R4 | implemented | The changed cwd guard refuses a wrong-worktree launch without closing the interactive destination shell; exact selector, evidence, limitations, and quoting remain intact. |
| R5 | inherited | Destination-session switching and fallback behavior are unchanged and covered by the prior final review and full suite. |
| R6 | implemented | No provider execution, authentication, fork, receipt, reconciliation, or retry route was added. The public bundle still exposes only the guidance CLI. |
| R7 | implemented | Skill, provider reference, and user guide now state Cursor's current unavailability consistently while preserving experimental/not-released and paused-executor labels. |
| R8 | implemented | Canonical TypeScript and generated outputs are in sync; runtime dependencies are unchanged; shared/default consumers remain green; every skill-directory change received its required version increment. |

### Design and Regression Assessment

The accepted p-rev1 design and R2 now name the guidance-only unattributable-summary
policy and preserve strict defaults elsewhere. The implementation follows that split:
the shared locator supplies an opt-in seam, the guidance projection alone selects it,
and the public CLI renders the aggregate result. The public bundle contains no old
probe, plan, execute, receipt, or reconciliation surface. Five task commits are
ordered and scoped as planned; `coding-session-handoff` advances once per changed
canonical skill commit from `0.1.2` to `0.1.7`, while `session-observer` advances to
`1.0.30` for its shared-source change.

### Extra Work (not in declared requirements)

None. Generated runtime changes, skill version bumps, and the user-guide alignment
are necessary consequences of the five fixes.

## Safety, Privacy, and Public Surface

- Unattributable summaries contain only provider, stable reason code, and bounded
  count; they contain no transcript path, native ID, body content, or credential.
- The public policy keeps store enumeration and aggregate limits strict, uses no
  persistent cache, and does not select by time.
- Cursor retains its stronger source-association refusal and cannot be selected or
  previewed without independent exact cwd evidence.
- Wrong-cwd guidance launches no provider and preserves the destination shell.
- The generated public bundle contains no `behavior-verify`, `executeHandoffPlan`,
  reconciliation, provider probe, or default-plan symbol.
- No real provider command, authentication probe, install/sync, feature push, PR
  mutation, merge, publication, release, or cleanup was performed.

## Verification

The reviewer independently verified the unchanged requested head and ran:

- Focused fix suite: 4 files, 120 tests passed.
- Full Vitest suite: 134 files passed and 1 skipped; 1,913 tests passed and 1 skipped.
- `pnpm run type-check`, `pnpm run build:check`, `pnpm run validate`,
  `pnpm run validate:skill-versions --base-ref
  0042b84937076f875380892b87a39120eeefad9e`, and `pnpm run smoke`: passed.
- File-scoped `oxlint` and `oxfmt --check` for every changed authored source, test,
  skill, reference, and docs file: passed.
- Docs page format check and direct Next.js production build: 37 static pages built.
- Narrowed and full-range `git diff --check`, plus Node syntax checks for all three
  changed generated modules: passed.
- `oat --json project validate-plan`: valid.
- Synthetic generated-bundle discovery with an attributable Claude candidate,
  attributable Codex candidate, unrelated cwd-less Claude transcript, and unrelated
  oversized Codex record: 2 candidates and 2 provider summaries returned; summaries
  were path/content-free and no Codex cwd cache was created.
- Public help and bundle-symbol checks confirmed the experimental guidance-only
  surface. Both the feature worktree and synced-project artifact baseline were clean
  before this artifact was written.

The repository formatter excludes `.oat/**`; this artifact was kept manually
formatted and checked for structure and diff hygiene without rewriting other files.

## Verification Commands

```bash
git rev-parse b7a8d35f06acb0a850795e54ad40ea729caac8e5^{commit}
git diff --check 20e86a100832b114a8aa3a20469b849de7ec7f45..b7a8d35f06acb0a850795e54ad40ea729caac8e5
git diff --check 0042b84937076f875380892b87a39120eeefad9e..b7a8d35f06acb0a850795e54ad40ea729caac8e5
pnpm exec vitest run tests/session-observer/locate.test.ts tests/coding-session-handoff/guidance-discovery.test.ts tests/coding-session-handoff/guidance-cli.test.ts tests/coding-session-handoff/guidance.test.ts
pnpm run test
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run validate:skill-versions --base-ref 0042b84937076f875380892b87a39120eeefad9e
pnpm run smoke
pnpm --dir documentation exec oxfmt --check docs/user-guide/skills/coding-session-handoff.md
pnpm --dir documentation exec next build
oat --json project validate-plan --project-path .oat/projects/synced/coding-session-handoff
```

## Recommended Next Step

Run `oat-project-review-receive` to record this passing standard final review at the
corrected head and resurface the five accepted inherited Medium deferrals. If receive
completes, continue to the second configured implementation gate attempt; keep the
retained executor paused and unexposed, and treat push, PR mutation, release, and live
provider validation as separate authorization boundaries.
