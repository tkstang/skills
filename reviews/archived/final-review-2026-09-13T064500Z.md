---
oat_generated: true
oat_generated_at: 2026-09-13T06:45:00Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: auto
oat_project: .oat/projects/synced/coding-session-handoff
oat_review_base_sha: b7a8d35f06acb0a850795e54ad40ea729caac8e5
oat_review_head_sha: f5c6a51884202e44dc92236d267a941b2dbc2722
oat_review_range: b7a8d35f06acb0a850795e54ad40ea729caac8e5..f5c6a51884202e44dc92236d267a941b2dbc2722
oat_prior_review_artifact: .oat/projects/synced/coding-session-handoff/reviews/archived/final-review-2026-09-13T051014Z.md
oat_prior_review_head_sha: b7a8d35f06acb0a850795e54ad40ea729caac8e5
oat_review_request_id: handoff-real-store-fixes-final-review-20260913-01
oat_dispatch_scope: final-real-store-fixes
oat_dispatch_action: review
oat_dispatch_role: reviewer
oat_dispatch_target: oat-reviewer-gpt-5-6-sol-max
oat_model_axis: selected:gpt-5.6-sol
oat_effort_axis: selected:max
oat_dispatch_policy: frontier
oat_dispatch_ceiling: max
oat_dispatch_rationale: Consequential fresh standard final review of realistic-store fixes, strict shared-consumer boundaries, and the retained paused executor.
---

# Code Review: final

**Reviewed:** 2026-09-13T06:45:00Z
**Scope:** Narrowed re-review of the three realistic-store fix commits, with prior
full coverage inherited through `b7a8d35f06acb0a850795e54ad40ea729caac8e5`
and integration checked against implementation base
`0042b84937076f875380892b87a39120eeefad9e`
**Files reviewed:** 17 changed paths, plus governing artifacts, both prior final
reviews at the inherited head, generated runtimes, public docs, shared consumers,
and retained-executor boundaries
**Commits:** 3
(`b7a8d35f06acb0a850795e54ad40ea729caac8e5..f5c6a51884202e44dc92236d267a941b2dbc2722`)
**Reviewed HEAD:** `f5c6a51884202e44dc92236d267a941b2dbc2722`
**Verdict:** CHANGES REQUESTED (1 Important)
**Reconnaissance:** not-attempted

## Summary

The changes resolve stale-cwd summarization, provider scoping, Codex bounded-I/O
charging, discovery failure provenance, and the exact-128 reader boundary without
weakening fresh-cache observer or executor calls. They do not carry the new
continued-prefix policy through the complete guidance path: a realistic Codex
transcript is still rejected by a second strict native-ID read, and preview still
re-enters strict discovery for a realistic Claude or Codex transcript. A shared
classification cache can also transfer summarize acceptance into a later strict call,
and two documentation examples retain the absorbed `--provider all` workflow error.

Findings: 0 critical, 1 important, 1 medium, 1 minor

## Dispatch Provenance

- Request: `handoff-real-store-fixes-final-review-20260913-01`
- Configured role/target: `oat-reviewer-gpt-5-6-sol-max`
- Configured model/effort: `gpt-5.6-sol`, `max`
- Policy/ceiling: Frontier, `max`
- Dispatch: scope=final-real-store-fixes action=review role=reviewer producer=unknown
  provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:max
  dispatch_policy=frontier dispatch_ceiling=max
  target=oat-reviewer-gpt-5-6-sol-max

## Prior Finding Dispositions

| Finding from `final-review-2026-09-13T051014Z.md` | Disposition | Evidence |
| --- | --- | --- |
| I1(a) — large / more-than-128-record bounded-prefix attribution | **Partially resolved; reopened as I1** | The shared locator now accepts exact prefix metadata under guidance summarization, and generated Claude discovery succeeds. Generated Codex guidance discovery still rejects the same shape as `native-id-missing`, while generated preview re-enters strict discovery; see I1. |
| I1(b) — stale recorded cwd handling | Resolved | Non-Cursor canonicalization failure becomes the bounded, path-free `cwd-unresolvable` summary and does not erase an attributable candidate (`guidance-discovery.ts:229-236`). The generated-bundle fixture and focused tests pass. |
| I1(c) — provider-scoped preview/prepare | Resolved as scoped | Both entry points derive the provider from the validated qualified key and request only that provider (`guidance-cli.ts:252-254`, `:286-289`, `:341-344`). The Cursor-blocking integration fixture passes. The same-provider strict preview reread is the separate I1 gap below. |
| M1 — Codex bounded-I/O budget | Resolved | Exact-all Codex charging uses `min(file size, per-entry metadata cap)` (`locate.ts:980-982`). The focused fixture accepts two large-body files at the exact 2,048-byte bounded budget and rejects 2,047 bytes. |
| M2 — path-free failure provenance | Resolved for discovery failures | `GuidanceDiscoveryError` carries allowlisted provider/reason values, locator exceptions map to stable codes, and JSON failures include only those values plus the stable outer code (`guidance-discovery.ts:75-130`, `guidance-cli.ts:164-169`, `:228-239`). Tests exclude source paths, IDs, and content. |
| Minor — default `--provider all` workflow | **Partially resolved; reopened as m1** | The installed skill now recommends explicit Claude/Codex discovery and explains why `all` fails closed. The user guide and retained experiment README still show `--provider all`; see m1. |
| Reopened p02 M1 — exact metadata-cap boundary | Resolved | The reader now distinguishes a final newline at record 128 from following data (`runtimes.ts:859-861`). Independent generated-runtime proof returned `incomplete:false` for exactly 128 newline-terminated records and `incomplete:true` for 129. Strict fresh-cache discovery still rejects continued prefixes. |

## Findings

### Critical

None.

### Important

- **Continued transcripts still fail in Codex guidance discovery and in preview**
  (`src/transcript/coding-session-handoff/guidance-discovery.ts:245`,
  `src/transcript/coding-session-handoff/discovery.ts:89`,
  `src/transcript/coding-session-handoff/guidance-cli.ts:256`)
  - Issue: `prev1-t12` correctly lets the shared locator use exact metadata and cwd
    from a bounded prefix even when the file/record stream continues. The projection
    then calls the retained executor's `readExactCodexNativeId`, which returns `null`
    solely when that same bounded read reports `incomplete`
    (`discovery.ts:92-97`). A generated-bundle fixture with a 314 KiB, 143-record
    Codex transcript whose exact ID and cwd are in its first record therefore returns
    `discovery-incomplete`, provider `codex`, reason `native-id-missing`; the equivalent
    Claude fixture returns one candidate. Preview has a second break: after its
    guidance-aware lookup succeeds, `rawMatch` calls `discover` with the strict
    `GUIDANCE_DISCOVERY_OPTIONS` (`guidance-cli.ts:261-266`). Previewing the 314 KiB,
    143-record Claude candidate consequently returns `unexpected-failure` before the
    bounded tail preview. The tests separate these seams: the realistic generated
    CLI case covers only Claude `discover` (`guidance-cli.test.ts:541`), while the
    preview/prepare provider-isolation case uses a small transcript
    (`guidance-cli.test.ts:442`). R2 promises continued-prefix attribution for both
    Claude and Codex plus optional bounded preview, so ordinary long sessions still
    do not satisfy the accepted guidance workflow.
  - Fix: Add a guidance-only Codex native-ID extractor that accepts complete,
    internally consistent exact metadata from the bounded prefix despite continuation;
    preserve `readExactCodexNativeId` for the strict executor. Make `rawMatch` use the
    same guidance summarize policy for its selected provider, or carry the already
    attributed raw candidate through without a strict rediscovery. Convert any raw
    match failure to the stable preview envelope. Add generated-bundle cases that
    combine a greater-than-256-KiB, greater-than-128-record transcript with Codex
    discovery and Claude/Codex preview and prepare.
  - Requirement: R1, R2, R8

### Medium

- **The bounded classification cache does not separate summarize and strict policy**
  (`src/transcript/session-observer/lib/locate.ts:466`)
  - Issue: The cache derivation key contains only the byte and record limits, and the
    cache lookup occurs before the strict `read.incomplete` rejection
    (`locate.ts:467-473`, `:496-498`). A continued transcript accepted under
    `unattributablePolicy: summarize` is cached at `locate.ts:543-550`; a later
    strict/default call using the same `ClassificationCache` returns that cached
    result without reapplying strict completeness. Independent generated-runtime
    proof returned one candidate for summarize, then incorrectly returned one for a
    strict call with the same cache; a fresh strict cache rejected with
    `DISCOVERY_TRANSCRIPT_INCOMPLETE`. Current guidance, observer, and retained
    executor compositions create separate caches, limiting immediate reachability,
    but the shared API no longer preserves its advertised strict boundary when its
    request-local cache is reused across policies.
  - Fix: Include `unattributablePolicy` in the bounded derivation namespace, or avoid
    caching a summarize-accepted incomplete result in a namespace readable by strict
    calls. Add a regression that runs summarize then strict with one cache and proves
    the strict call still rejects; retain fresh/default observer and executor tests.
  - Requirement: R2, R8

### Minor

- **Public and retained docs still lead with the absorbed `--provider all` command**
  (`documentation/docs/user-guide/skills/coding-session-handoff.md:44`)
  - Issue: The installed skill correctly changed the default workflow to
    `--provider claude` or `--provider codex`, but the public user guide still runs
    `--provider all` while the same page states that a matching Cursor store fails
    closed (`:16-19`). `tools/coding-session-handoff/README.md:32` repeats the stale
    example. This leaves the prior Minor only partially absorbed and makes the human
    docs disagree with the agent-facing workflow.
  - Suggestion: Change both examples to an explicit supported provider and briefly
    retain the existing explanation that `all` fails closed while Cursor lacks exact
    cwd evidence.
  - Requirement: R7

## Deferred Finding Dispositions

The reopened p02 M1 is resolved at the reader boundary as described above. The other
four inherited Medium findings remain acceptable only while the automated executor is
paused and unreachable:

| Inherited finding | Current disposition | Basis |
| --- | --- | --- |
| p02 M2 — newline-bearing Git evidence schema | Accept deferral | Executor-only schema is unchanged; public guidance emits no executor plan. |
| p03 M1 — empty Claude `--tools` operand round-trip | Accept deferral | Executor argv parsing is unchanged and absent from the public bundle. |
| p03 M2 — authentication-required diagnostic collapse | Accept deferral | Public guidance performs no provider or authentication probe. |
| p03 M3 — raw alias comparison in execute/reconcile | Accept deferral | Public help and exports expose only `discover`, `preview`, and `prepare`; execute/reconcile remain paused. |

Any executor activation invalidates these dispositions and requires a fresh review.
I1 and M1 above must be repaired without making the summarize policy the executor or
observer default.

## Requirements/Design Alignment

**Evidence sources used:** current `spec.md` R1-R8; current `design.md` accepted
revision and historical shared-reader contracts; `plan.md`, including
`prev1-t12`-`prev1-t14`; `implementation.md`; `state.md`; passing lifecycle review
`reviews/archived/final-review-2026-09-13T044900Z.md`; configured-gate review
`reviews/archived/final-review-2026-09-13T051014Z.md`; the exact three-commit range;
all 17 changed paths; generated runtimes; public skill/docs; shared observer/export
consumers; paused executor call sites; and independent focused/full/synthetic checks.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| R1 | partial | Entry-point and explicit-selection coverage is inherited, and provider scoping is implemented. Realistic Codex candidates and realistic previews still fail at downstream strict rereads (I1). No recency selector was added. |
| R2 | partial | Stale cwd summaries, bounded Codex accounting, path-free reasons, zero persistence, and fresh strict defaults are implemented. The end-to-end continued-prefix path is incomplete (I1), and a reused cache can cross the strict policy boundary (M1). |
| R3 | inherited | Destination Git validation is outside the narrowed range and remains covered by prior review and the passing full suite. |
| R4 | inherited | Destination-only shell-safe instructions are unchanged and covered by prior review/full tests. |
| R5 | inherited | Destination-session switching/fallback behavior is unchanged. |
| R6 | implemented | No provider execution, authentication, fork, receipt, reconciliation, retry, or IDE control was added. |
| R7 | partial | Experimental/not-released and paused-executor labels remain accurate; the installed workflow is corrected, but two documentation examples retain the failing `all` default (m1). |
| R8 | partial | Generated parity, dependency-free output, export compatibility, and three skill version sequences are correct. The shared cache namespace violates the strict-consumer preservation contract (M1). |

### Design and Regression Assessment

The accepted design accurately describes the desired guidance-only summarize seam,
strict other consumers, and provider-scoped preview/prepare. Stale-cwd handling,
bounded Codex charging, failure provenance, and the exact-128 parser fix align with
that design. I1 is an implementation gap at downstream helper calls that still apply
the historical strict policy, while M1 is a shared-cache namespace gap. The public
user-guide command is stale artifact alignment rather than a separate runtime defect.

The three commits make no provider or network call, add no runtime dependency, and
remain within the planned modules. `coding-session-handoff` advances `0.1.7` to
`0.1.10` across all three commits; `session-observer` advances `1.0.30` to `1.0.32`
across its two commits; `export-session-transcript` advances `1.0.9` to `1.0.10` with
the shared reader change. Top-level and metadata versions match. Generated source
parity and syntax checks pass.

### Extra Work (not in declared requirements)

None. The shared core/runtime generation and three version sequences are necessary
consequences of the declared fixes.

## Safety, Privacy, and Compatibility

- Guidance discovery keeps `persistence: forbid`, `recency: exact-all`, 50,000-entry,
  512 MiB aggregate, 256 KiB per-entry, and 30-second limits. Codex accounting now
  charges at most the bounded prefix bytes for each file.
- Unattributable summaries contain only provider, stable reason, and count. JSON
  discovery failures expose only stable provider/reason provenance. No private path,
  native ID, transcript content, raw exception text, or credential is retained here.
- Fresh strict/default locator calls and the retained executor still reject incomplete
  prefixes; the cross-policy same-cache exception is M1.
- The 802-test shared lane covers transcript core, session-observer, and
  export-session-transcript compatibility. Generated export and observer modules are
  in sync and parse successfully.
- The public bundle remains dependency-free and guidance-only; it contains no old
  provider probe, behavior verification, execution, receipt, or reconciliation symbol.
- No real transcript store, provider command, authentication probe, installation,
  sync, push, PR mutation, merge, cleanup, publication, or release was used.

## Verification

Independently run at unchanged head
`f5c6a51884202e44dc92236d267a941b2dbc2722`:

- `pnpm exec vitest run tests/coding-session-handoff`: 13 files, 250 tests passed.
- `pnpm exec vitest run tests/transcript-core tests/session-observer tests/export-session-transcript`: 27 files, 802 tests passed.
- `pnpm run test`: 134 files passed and 1 skipped; 1,920 tests passed and 1 skipped.
- `pnpm run type-check`, `pnpm run build:check`, `pnpm run validate`,
  `pnpm run validate:skill-versions --base-ref
  0042b84937076f875380892b87a39120eeefad9e`, and `pnpm run smoke`: passed.
- File-scoped `oxlint` and `oxfmt --check` for every changed authored skill, source,
  and test file: passed.
- User-guide page format check and direct Next.js production build: 37 static pages
  built.
- Narrowed/full-base `git diff --check`, Node syntax checks for all five changed
  generated modules, and `oat --json project validate-plan`: passed.
- Generated-bundle 314 KiB / 143-record fixtures: Claude discovery returned one
  candidate; Codex discovery reproduced `discovery-incomplete` / `native-id-missing`;
  Claude preview reproduced `unexpected-failure`; persistent cache remained absent.
- Generated locator same-cache fixture: summarize returned one candidate, strict with
  the same cache incorrectly returned one, and strict with a fresh cache rejected with
  `DISCOVERY_TRANSCRIPT_INCOMPLETE`.
- Generated core boundary fixture: exactly 128 newline-terminated records returned
  complete; 129 records returned incomplete.
- Public bundle help/symbol checks confirmed the experimental guidance-only surface.
  Root and synced-project baselines were clean before this artifact was written.

The repository formatter excludes `.oat/**`; this artifact was manually formatted and
checked for structure and diff hygiene without changing other files.

## Verification Commands

```bash
git rev-parse f5c6a51884202e44dc92236d267a941b2dbc2722^{commit}
git diff --check b7a8d35f06acb0a850795e54ad40ea729caac8e5..f5c6a51884202e44dc92236d267a941b2dbc2722
git diff --check 0042b84937076f875380892b87a39120eeefad9e..f5c6a51884202e44dc92236d267a941b2dbc2722
pnpm exec vitest run tests/coding-session-handoff
pnpm exec vitest run tests/transcript-core tests/session-observer tests/export-session-transcript
pnpm run test
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run validate:skill-versions --base-ref 0042b84937076f875380892b87a39120eeefad9e
pnpm run smoke
pnpm exec oxlint src/transcript/coding-session-handoff/guidance-cli.ts src/transcript/coding-session-handoff/guidance-discovery.ts src/transcript/core/runtimes.ts src/transcript/session-observer/lib/locate.ts src/transcript/session-observer/lib/types.ts tests/coding-session-handoff/guidance-cli.test.ts tests/coding-session-handoff/guidance-discovery.test.ts tests/session-observer/locate.test.ts tests/transcript-core/runtimes.test.ts
pnpm --dir documentation exec oxfmt --check docs/user-guide/skills/coding-session-handoff.md
pnpm --dir documentation exec next build
oat --json project validate-plan --project-path .oat/projects/synced/coding-session-handoff
```

## Recommended Next Step

Run `oat-project-review-receive` to convert I1, M1, and m1 into bounded repair tasks.
Keep the retained executor paused and unexposed. The operator-authorized extra external
gate must not run until a fresh standard final review passes at the repaired head.
