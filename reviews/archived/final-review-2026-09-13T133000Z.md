---
oat_generated: true
oat_generated_at: 2026-09-13T13:30:00Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: auto
oat_project: .oat/projects/synced/coding-session-handoff
oat_review_base_sha: f5c6a51884202e44dc92236d267a941b2dbc2722
oat_review_head_sha: 10d901e8afb3217db9855fa48c7e83f4f02c8e6c
oat_review_range: f5c6a51884202e44dc92236d267a941b2dbc2722..10d901e8afb3217db9855fa48c7e83f4f02c8e6c
oat_prior_review_artifact: .oat/projects/synced/coding-session-handoff/reviews/archived/final-review-2026-09-13T064500Z.md
oat_prior_review_head_sha: f5c6a51884202e44dc92236d267a941b2dbc2722
oat_review_request_id: handoff-direct-repairs-final-rereview-20260913-01
oat_dispatch_scope: final-direct-repairs
oat_dispatch_action: review
oat_dispatch_role: reviewer
oat_dispatch_producer: unknown
oat_dispatch_provenance: unknown
oat_dispatch_target: oat-reviewer-gpt-5-6-sol-max
oat_model_axis: selected:gpt-5.6-sol
oat_effort_axis: selected:max
oat_dispatch_policy: frontier
oat_dispatch_ceiling: max
oat_dispatch_policy_source: project-state
oat_dispatch_rationale: Consequential final re-review of direct continued-prefix, strict-cache-isolation, and documentation repairs.
---

# Code Review: final

**Reviewed:** 2026-09-13T13:30:00Z
**Scope:** Narrowed re-review of the three direct repair commits, with prior final
coverage inherited through `f5c6a51884202e44dc92236d267a941b2dbc2722` and
integration checked against implementation base
`0042b84937076f875380892b87a39120eeefad9e`
**Files reviewed:** 12 changed paths, plus governing artifacts, generated runtimes,
shared strict consumers, public documentation, and paused-executor boundaries
**Commits:** 3
(`f5c6a51884202e44dc92236d267a941b2dbc2722..10d901e8afb3217db9855fa48c7e83f4f02c8e6c`)
**Reviewed HEAD:** `10d901e8afb3217db9855fa48c7e83f4f02c8e6c`
**Verdict:** PASS
**Reconnaissance:** not-attempted

## Summary

The three direct repairs close every finding from the inherited final review. Continued
Claude and Codex transcripts now complete generated discovery, preview, and prepare;
strict callers remain fail closed even after a summarize call reuses the same cache;
and public examples use an explicit supported provider. The guidance surface remains
bounded, path-free, provider-isolated, zero-persistence, experimental, and separated
from the paused executor.

Findings: 0 critical, 0 important, 0 medium, 0 minor

## Dispatch Provenance

- Request: `handoff-direct-repairs-final-rereview-20260913-01`
- Configured role/target: `oat-reviewer-gpt-5-6-sol-max`
- Configured model/effort: `gpt-5.6-sol`, `max`
- Policy/ceiling: Frontier, `max` from project state
- Dispatch: scope=final-direct-repairs action=review role=reviewer producer=unknown
  provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:max
  dispatch_policy=frontier dispatch_ceiling=max
  target=oat-reviewer-gpt-5-6-sol-max

## Prior Finding Dispositions

| Finding from `final-review-2026-09-13T064500Z.md` | Disposition | Evidence |
| --- | --- | --- |
| I1 — continued-prefix Codex projection and Claude/Codex preview/prepare | **Resolved** | Guidance uses a dedicated Codex prefix extractor that accepts internally consistent ID evidence without treating bounded continuation alone as failure (`guidance-discovery.ts:142-168`), while preview rediscovery uses the same non-Cursor summarize policy and maps discovery failure to `preview-incomplete` (`guidance-cli.ts:256-292`). The generated 314 KiB/143-record fixture independently completed discover, preview, and prepare for both providers. |
| M1 — summarize acceptance can prime a strict shared-cache call | **Resolved** | The bounded derivation namespace now includes the effective `unattributablePolicy` (`locate.ts:456-473`). With one generated `ClassificationCache`, summarize returned the candidate and the following strict call rejected with `DISCOVERY_TRANSCRIPT_INCOMPLETE`; the focused regression passes for Claude and Codex. |
| m1 — public examples lead with `--provider all` | **Resolved** | Both the user guide and retained tool README use `--provider claude`, while explicitly explaining that `all` fails closed until Cursor has independent exact cwd evidence (`documentation/docs/user-guide/skills/coding-session-handoff.md:42-58`; `tools/coding-session-handoff/README.md:29-39`). |

The prior review's already-resolved realistic-store subfindings remain resolved: stale
recorded cwd values are summarized without suppressing attributable candidates;
preview and prepare scope discovery to the provider encoded in the qualified key;
Codex aggregate accounting charges bounded bytes; failure provenance is stable and
path-free; and exactly 128 complete metadata records remain distinguishable from a
continued prefix.

## Findings

### Critical

None.

### Important

None.

### Medium

None.

### Minor

None.

## Deferred Finding Dispositions

The five inherited dispositions remain acceptable and unchanged:

| Inherited finding | Current disposition | Basis |
| --- | --- | --- |
| p02 M1 — exact metadata-cap boundary | Resolved; remains resolved | Generated/shared reader parity and the focused exact-boundary coverage pass. Strict discovery still rejects a continued prefix. |
| p02 M2 — newline-bearing Git evidence schema | Accept deferral | Executor-only schema is unchanged; the public guidance product emits no executor plan. |
| p03 M1 — empty Claude `--tools` operand round-trip | Accept deferral | Executor argument parsing is unchanged and absent from the public bundle. |
| p03 M2 — authentication-required diagnostic collapse | Accept deferral | Public guidance performs no provider or authentication probe. |
| p03 M3 — raw alias comparison in execute/reconcile | Accept deferral | Public help and exports expose only `discover`, `preview`, and `prepare`; execute and reconcile remain paused and unreachable. |

Any executor activation invalidates the four executor-only deferrals and requires a
fresh review. This PASS does not establish live provider behavior or complete the
historical live gates.

## Requirements/Design Alignment

**Evidence sources used:** current `spec.md` R1-R8; current `design.md` accepted
revision and historical strict-consumer contracts; `plan.md`, including
`prev1-t15`-`prev1-t17`; `implementation.md`; `state.md`; `revision-handoff.md`; the
inherited final review `reviews/archived/final-review-2026-09-13T064500Z.md`; the exact
three-commit range; all 12 changed paths; generated guidance, observer, and executor
runtimes; public skill/docs; shared observer and export consumers; and independent
focused, full, and synthetic checks.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| R1 | inherited | The three entry points, explicit selection, and no-recency contract were covered by the prior final review and are unchanged in this range. |
| R2 | implemented | Continued-prefix attribution now reaches Codex projection and both providers' preview/prepare paths. Provider isolation, bounded summaries, zero persistence, and strict observer/executor defaults pass direct regression checks. |
| R3 | inherited | Existing-destination Git validation is unchanged and passed the generated prepare fixture and inherited coverage. |
| R4 | inherited | Destination-only shell-safe instructions are unchanged and remain covered by the full suite. |
| R5 | inherited | Destination-session switching and fallback behavior are unchanged. |
| R6 | implemented | No provider execution, authentication, fork, receipt, reconciliation, retry, store write, or IDE control is reachable from the public bundle. |
| R7 | implemented | Public examples now use an explicit supported provider; experimental/not-released and paused-executor labels remain prominent. |
| R8 | implemented | Cache policy isolation preserves strict shared consumers; generated parity, dependency-free output, skill versions, and observer/export compatibility pass. |

### Design and Regression Assessment

The fixes follow the accepted guidance-only summarize seam without widening the
historical executor policy. `readExactCodexNativeId` remains unchanged and still
rejects incomplete metadata reads (`discovery.ts:89-112`); the new permissive extractor
is confined to guidance projection and preview. The locator cache change separates
effective `fail` and `summarize` derivations rather than changing either policy's
semantics. No recency selection or persistence path was added.

The generated CLI fixture used a disposable HOME and synthetic Git repository. Claude
and Codex each discovered exactly one provider-qualified candidate and completed
preview and prepare from a transcript over both 256 KiB and 128 records. Its later
same-cache strict call failed closed, and no observer state directory was created.
Focused tests also cover unrelated transcript omission, provider-scoped discovery,
Cursor collision refusal, path/content absence, and stable failure envelopes.

`coding-session-handoff` advances `0.1.10` to `0.1.11` for the guidance repair and to
`0.1.12` for the shared locator regeneration. `session-observer` advances `1.0.32` to
`1.0.33`. Top-level and metadata versions match; the documentation-only commit does
not change a canonical skill directory. Generated outputs are byte-for-byte current.

### Extra Work (not in declared requirements)

None. The generated runtime updates and two skill version sequences are required
consequences of the canonical source changes.

## Safety, Privacy, and Compatibility

- Guidance retains `persistence: forbid`, `recency: exact-all`, a 50,000-entry cap,
  512 MiB aggregate cap, 256 KiB per-entry metadata cap, and 30-second scan deadline.
- Unattributable output contains only provider, stable reason code, and count. Failures
  contain stable codes; no transcript path, native ID, content, raw exception, or
  credential is added to failure provenance or durable project state.
- Preview and prepare discover only the provider in the validated qualified key.
- Fresh and same-cache strict discovery fail closed. The retained executor keeps its
  strict ID extractor and remains absent from public exports/help.
- The shared 804-test lane covers transcript core, session-observer, and
  export-session-transcript compatibility. All generated modules parse successfully.
- No real transcript store, provider command, authentication probe, installation,
  global sync, push, PR mutation, merge, cleanup, publication, or release was used.

## Verification

Independently run at unchanged head
`10d901e8afb3217db9855fa48c7e83f4f02c8e6c`:

- `pnpm exec vitest run tests/coding-session-handoff`: 13 files and 251 tests passed.
- `pnpm exec vitest run tests/transcript-core tests/session-observer tests/export-session-transcript`:
  27 files and 804 tests passed.
- `pnpm run test`: 134 files passed and 1 skipped; 1,923 tests passed and 1 skipped.
- `pnpm run type-check`, `pnpm run build:check`, `pnpm run validate`,
  `pnpm run validate:skill-versions --base-ref
  0042b84937076f875380892b87a39120eeefad9e`, and `pnpm run smoke`: passed.
- File-scoped `oxlint` and `oxfmt --check` over all nine changed authored source, test,
  skill, and Markdown paths: passed.
- `pnpm exec next build` from `documentation/`: compiled and generated 37 static pages.
- Narrowed and full-base `git diff --check`; Node syntax checks for the three changed
  generated modules; and `oat --json project validate-plan` from the project directory:
  passed.
- Reviewer-local generated-runtime harness: Claude and Codex 314 KiB/143-record
  discover, preview, and prepare all succeeded; same-cache summarize returned one and
  strict rejected with `DISCOVERY_TRANSCRIPT_INCOMPLETE`; persistence remained absent.
- Public help reported only the experimental read-only surface, and a forbidden-symbol
  search found no execution, behavior-gate, provider-probe, or reconciliation reachability.

The repository formatter excludes `.oat/**`; this artifact was manually formatted and
checked for review structure and diff hygiene without changing another file. Root and
synced-project baselines were clean before it was written.

## Verification Commands

```bash
git rev-parse 10d901e8afb3217db9855fa48c7e83f4f02c8e6c^{commit}
git diff --check f5c6a51884202e44dc92236d267a941b2dbc2722..10d901e8afb3217db9855fa48c7e83f4f02c8e6c
git diff --check 0042b84937076f875380892b87a39120eeefad9e..10d901e8afb3217db9855fa48c7e83f4f02c8e6c
pnpm exec vitest run tests/coding-session-handoff
pnpm exec vitest run tests/transcript-core tests/session-observer tests/export-session-transcript
pnpm run test
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run validate:skill-versions --base-ref 0042b84937076f875380892b87a39120eeefad9e
pnpm run smoke
```

## Recommended Next Step

Run `oat-project-review-receive` for this artifact, then proceed only within the
separately authorized configured-gate and final HiLL boundaries.
