---
oat_generated: true
oat_generated_at: 2026-09-13T03:10:00Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/synced/coding-session-handoff
oat_review_base_sha: 3fdfc2a17b0b94171871a5bf6460beac1a333bea
oat_review_head_sha: 20e86a100832b114a8aa3a20469b849de7ec7f45
oat_review_range: 3fdfc2a17b0b94171871a5bf6460beac1a333bea..20e86a100832b114a8aa3a20469b849de7ec7f45
oat_prior_review_artifact: .oat/projects/synced/coding-session-handoff/reviews/archived/final-review-2026-09-13T023957Z.md
oat_prior_review_head_sha: 3fdfc2a17b0b94171871a5bf6460beac1a333bea
oat_review_request_id: handoff-final-rereview-20260913-01
oat_dispatch_scope: final-rereview-1
oat_dispatch_action: review
oat_dispatch_role: reviewer
oat_dispatch_target: oat-reviewer-gpt-5-6-sol-max
oat_model_axis: selected:gpt-5.6-sol
oat_effort_axis: selected:max
oat_dispatch_policy: frontier
oat_dispatch_ceiling: max
oat_dispatch_rationale: Consequential narrowed final re-review of exact Cursor source association, privacy, generated-source parity, and the paused executor boundary.
---

# Code Review: final

**Reviewed:** 2026-09-13T03:10:00Z
**Scope:** Narrowed final re-review of the accepted Cursor source-association fix;
requirements coverage outside the range is inherited from the prior final review
**Files reviewed:** 11 changed paths, plus the prior final review and governing artifacts
**Commits:** 1
(`3fdfc2a17b0b94171871a5bf6460beac1a333bea..20e86a100832b114a8aa3a20469b849de7ec7f45`)
**Base:** `3fdfc2a17b0b94171871a5bf6460beac1a333bea`
**Reviewed HEAD:** `20e86a100832b114a8aa3a20469b849de7ec7f45`
**Verdict:** PASS
**Reconnaissance:** not-attempted

## Summary

The narrowed fix resolves prior final-review I1. Cursor candidates now carry explicit
cwd-evidence quality, and the public guidance projection rejects caller-derived lossy
or diagnostic source associations before it returns any candidate; the public preview
and prepare paths first repeat that guarded discovery. A filesystem-backed regression
proves that two canonical worktrees with the same Cursor project slug expose neither
candidate paths nor transcript content. No regression was found in bounded discovery,
generated runtimes, versioning, or the retained paused-executor boundary.

Findings: 0 critical, 0 important, 0 medium, 0 minor

## Dispatch Provenance

- Request: `handoff-final-rereview-20260913-01`
- Configured role/target: `oat-reviewer-gpt-5-6-sol-max`
- Configured model/effort: `gpt-5.6-sol`, `max`
- Policy/ceiling: Frontier, `max`
- Dispatch: scope=final-rereview-1 action=review role=reviewer producer=unknown
  provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:max
  dispatch_policy=frontier dispatch_ceiling=max
  target=oat-reviewer-gpt-5-6-sol-max

## Prior Finding Disposition

### I1 — Resolved

The shared candidate type now records a three-state Cursor cwd-evidence quality
(`src/transcript/session-observer/lib/types.ts:100`). The shared locator preserves the
quality on every Cursor candidate (`src/transcript/session-observer/lib/locate.ts:1140`):
direct project-slug lookup is explicitly `caller-derived-lossy`
(`src/transcript/session-observer/lib/locate.ts:1227`), while fallback project entries
are `diagnostic` (`src/transcript/session-observer/lib/locate.ts:1292`). It no longer
represents a requested cwd as independently observed merely because its lossy slug
selects a Cursor directory.

The public guidance layer requires `independent-exact` before canonical cwd matching
or projection (`src/transcript/coding-session-handoff/guidance-discovery.ts:129`). A
weak Cursor result therefore becomes typed `discovery-incomplete`, with no partial
candidate set. Preview first calls that guarded discovery and only then resolves its
selected transcript (`src/transcript/coding-session-handoff/guidance-cli.ts:270`);
prepare has the same guard (`src/transcript/coding-session-handoff/guidance-cli.ts:320`).
The CLI returns a generic path-free failure envelope
(`src/transcript/coding-session-handoff/guidance-cli.ts:217`).

The regression creates two distinct canonical source paths that encode to the same
Cursor slug, exercises both discovery and preview for each path, and asserts that the
source path and private transcript marker are absent from output
(`tests/coding-session-handoff/guidance-cli.test.ts:193`). Unit coverage also rejects
a synthetic caller-derived candidate before projection
(`tests/coding-session-handoff/guidance-discovery.test.ts:150`). The focused suite
passed with 106 tests.

The current Cursor transcript-store path supplies lossy or diagnostic cwd evidence,
so public Cursor discovery fails closed until an independently exact source signal is
available. That is the explicit incomplete-discovery behavior allowed by R2 and by
the prior review's fix guidance; it does not guess a source or permit preview.

## Findings

### Critical

None.

### Important

None.

### Medium

None.

### Minor

None.

## Deferred Finding Dispositions

The narrowed commit does not modify the canonical code behind any of the five
inherited Medium findings. Their accepted dispositions remain valid only for the
current experimental guidance-only product boundary.

| Inherited finding | Disposition after re-review | Evidence and boundary |
| --- | --- | --- |
| p02 M1 — exact metadata-cap boundary | Remains accepted | The bounded prefix reader remains unchanged (`src/transcript/core/runtimes.ts:859`). Guidance still converts incomplete discovery to a path-free error with no partial candidates. |
| p02 M2 — newline-bearing Git evidence schema | Remains accepted | The retained executor parser remains unchanged (`src/transcript/coding-session-handoff/types.ts:395`, `src/transcript/coding-session-handoff/types.ts:666`). Public guidance remains separate and emits no executor plan. |
| p03 M1 — empty Claude `--tools` operand round-trip | Remains accepted | The retained builder/parser behavior remains unchanged (`src/transcript/coding-session-handoff/behavior-contracts.ts:220`, `src/transcript/coding-session-handoff/types.ts:1100`). The public bundle has no executor-plan parser or launch path. |
| p03 M2 — authentication-required diagnostic collapse | Remains accepted | The retained planner still owns provider probes (`src/transcript/coding-session-handoff/cli.ts:532`). Public guidance performs no provider or authentication probe. |
| p03 M3 — raw alias comparison in execute/reconcile | Remains accepted | The retained executor still owns execution and reconciliation (`src/transcript/coding-session-handoff/cli.ts:703`, `src/transcript/coding-session-handoff/cli.ts:751`). Public help and validation expose only `discover`, `preview`, and `prepare`. |

Enabling the retained executor would invalidate these product-boundary dispositions
and require a fresh review before activation. This narrowed fix neither enables nor
otherwise changes that executor.

## Requirements/Design Alignment

**Evidence sources used:** `spec.md` revision R1-R8; `design.md` accepted revision,
shared read-only discovery seam, skill interaction, security, and verification
contracts; `plan.md`, including `prev1-t06` and revision closeout; `implementation.md`;
the prior final review; the authoritative one-commit range; all 11 changed paths;
canonical TypeScript, generated runtimes, focused tests, public skill metadata, and
the retained executor boundary.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| R1 | inherited; fix verified | The prior review's general entry-point coverage is inherited. This pass verifies that a weak Cursor source association cannot become a selectable current candidate. |
| R2 | implemented | Prior I1 is resolved: bounded Cursor discovery now reports the unavailable exact source association explicitly and path-free rather than returning a falsely exact candidate. |
| R3 | inherited | Destination Git validation is outside the narrowed range and remains inherited from the prior final review. |
| R4 | inherited | Instruction construction is outside the narrowed range; the fix does not alter command rendering. |
| R5 | inherited | Destination-session behavior is outside the narrowed range; an unsupported Cursor path still emits no guessed transition. |
| R6 | verified | The public bundle remains guidance-only, and the fix adds no provider execution, authentication, session mutation, reconciliation, or retry route. |
| R7 | inherited; metadata verified | Product-status prose is unchanged; the two changed skills carry synchronized version bumps while the guidance product remains experimental/not released and the executor remains paused. |
| R8 | verified | Canonical TypeScript and all generated outputs are in sync, runtime dependencies are unchanged, focused shared-consumer tests pass, and both changed skills satisfy the version gate. |

### Design and Regression Assessment

The fix stays within the accepted shared-locator and guidance-projection component
split. It preserves observer candidate visibility while adding the evidence-quality
signal needed by the stricter public guidance consumer. Exact-all enumeration's prior
fail-on-incomplete behavior remains intact, Codex and Claude projections are
unchanged, generated outputs derive from canonical TypeScript, and no provider store
write or persistent cache path was added.

### Extra Work (not in declared requirements)

None. Generated runtime changes and the two skill version bumps are required
consequences of the canonical source fix.

## Safety, Privacy, and Public Surface

- Weak Cursor cwd evidence is rejected before candidate output, selection, preview,
  or instruction preparation.
- The collision regression confirms that failure output contains neither canonical
  source paths nor transcript content.
- The public generated bundle still contains no `behavior-verify` or
  `executeHandoffPlan` surface; repository validation and smoke checks passed.
- No live provider command, authentication probe, installation, provider-store read
  outside synthetic tests, session mutation, cleanup, publication, or release action
  was performed by this review.

## Verification

The reviewer independently ran:

- Focused Vitest suite: 3 files, 106 tests passed.
- `pnpm run type-check`: passed.
- `pnpm run build:check`: all generated mappings, including session-observer locate,
  the public guidance bundle, and the retained tool bundle, are in sync.
- `pnpm run validate`: passed.
- `pnpm run validate:skill-versions --base-ref
  0042b84937076f875380892b87a39120eeefad9e`: two changed skills verified.
- `pnpm run smoke`: passed.
- File-scoped `oxlint` and `oxfmt --check` over all changed authored source, tests,
  and skill Markdown: passed.
- Exact narrowed-range `git diff --check` and Node syntax checks for all three changed
  generated modules: passed.

The repository formatter excludes `.oat/**`; this artifact was kept manually
formatted and checked for structure and diff hygiene without rewriting other files.

## Verification Commands

```bash
git rev-parse 3fdfc2a17b0b94171871a5bf6460beac1a333bea^{commit}
git rev-parse 20e86a100832b114a8aa3a20469b849de7ec7f45^{commit}
git diff --check 3fdfc2a17b0b94171871a5bf6460beac1a333bea..20e86a100832b114a8aa3a20469b849de7ec7f45
pnpm exec vitest run tests/session-observer/locate.test.ts tests/coding-session-handoff/guidance-discovery.test.ts tests/coding-session-handoff/guidance-cli.test.ts
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run validate:skill-versions --base-ref 0042b84937076f875380892b87a39120eeefad9e
pnpm run smoke
```

## Recommended Next Step

Run `oat-project-review-receive` to record the passing final re-review at the corrected
head, mark prior I1 resolved, and resurface the five accepted Medium deferrals in the
final receipt. Keep the retained executor paused and unexposed; release, publication,
and provider validation remain separate boundaries.
