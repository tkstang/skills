---
oat_generated: true
oat_generated_at: 2026-09-13T02:39:57Z
oat_review_scope: final
oat_review_type: code
oat_review_invocation: manual
oat_project: .oat/projects/synced/coding-session-handoff
oat_review_base_sha: 0042b84937076f875380892b87a39120eeefad9e
oat_review_head_sha: 3fdfc2a17b0b94171871a5bf6460beac1a333bea
oat_review_range: 0042b84937076f875380892b87a39120eeefad9e..3fdfc2a17b0b94171871a5bf6460beac1a333bea
oat_review_request_id: handoff-final-review-20260913-01
oat_dispatch_target: oat-reviewer-gpt-5-6-sol-max
oat_model_axis: selected:gpt-5.6-sol
oat_effort_axis: selected:max
oat_dispatch_policy: frontier
oat_dispatch_ceiling: max
oat_dispatch_rationale: Consequential final review of exact-source identity, privacy, command safety, generated-source boundaries, and the retained paused executor.
---

# Code Review: final

**Reviewed:** 2026-09-13T02:39:57Z
**Scope:** Complete implementation from the accepted project base through the current
read-only guidance revision
**Files reviewed:** 71 changed paths, plus governing artifacts and the phase-review
chain
**Commits:** 54
(`0042b84937076f875380892b87a39120eeefad9e..3fdfc2a17b0b94171871a5bf6460beac1a333bea`)
**Base:** `0042b84937076f875380892b87a39120eeefad9e`
**Reviewed HEAD:** `3fdfc2a17b0b94171871a5bf6460beac1a333bea`
**Verdict:** CHANGES_REQUESTED
**Reconnaissance:** not-attempted

## Summary

The final range delivers the intended experimental, read-only handoff guidance and
keeps the incomplete automated executor paused and unreachable from the public skill.
One exact-source defect remains: Cursor's non-injective project-directory encoding can
place transcripts from distinct worktrees under the same slug, while the new guidance
path relabels every direct-hit transcript with the requested canonical cwd and permits
its preview. The five inherited Medium findings remain acceptable deferrals for this
product boundary; none enables provider execution or an unsafe fork through the public
skill.

Findings: 0 critical, 1 important, 0 medium, 0 minor

## Dispatch Provenance

- Request: `handoff-final-review-20260913-01`
- Configured role/target: `oat-reviewer-gpt-5-6-sol-max`
- Configured model/effort: `gpt-5.6-sol`, `max`
- Policy/ceiling: Frontier, `max`
- Dispatch: scope=final action=review role=reviewer producer=unknown
  provenance=unknown model_axis=selected:gpt-5.6-sol effort_axis=selected:max
  dispatch_policy=frontier dispatch_ceiling=max
  target=oat-reviewer-gpt-5-6-sol-max

## Findings

### Critical

None.

### Important

- **I1 — Cursor's lossy project slug can cross the exact source-worktree boundary**
  (`src/transcript/coding-session-handoff/guidance-discovery.ts:120`)
  - Issue: Cursor cwd encoding replaces both path separators and dots with `-`, so it
    is not injective: `/repo/a-b/c` and `/repo/a/b-c` both encode as
    `repo-a-b-c` (`src/transcript/core/runtimes.ts:661`). Cursor transcripts contain
    no independently recoverable cwd (`src/transcript/core/runtimes.ts:1326`), but
    direct discovery assigns the requested cwd to every transcript found below that
    slug (`src/transcript/session-observer/lib/locate.ts:1210`,
    `src/transcript/session-observer/lib/locate.ts:1228`). The new guidance wrapper
    then canonicalizes that caller-derived value, presents it as `recordedCwd`, and
    accepts the candidate as an exact source match
    (`src/transcript/coding-session-handoff/guidance-discovery.ts:136`). A
    selected candidate is rediscovered through the same assumption and can be read by
    `preview` (`src/transcript/coding-session-handoff/guidance-cli.ts:240`). The
    `ambiguous` surface and `store-origin-ambiguous` label describe Cursor IDE/CLI
    origin; they do not disclose that source-worktree association is also ambiguous.
    This can show conversation content from a different local worktree under the
    requested source and violates R2's explicit canonical-source boundary. Cursor
    preparation emits no fork command, so this is not a provider-mutation path and is
    graded Important rather than Critical.
  - Fix: Carry Cursor cwd-evidence quality into the guidance projection and require
    independent exact cwd corroboration before returning a public Cursor candidate or
    allowing preview. If no exact evidence exists, fail the Cursor portion as
    `discovery-incomplete` or return a distinct source-association-ambiguous state that
    cannot be selected or previewed. Add a filesystem-backed collision fixture using
    two distinct canonical worktrees with the same encoded slug and prove that no
    transcript is attributed to either source without corroboration. Regenerate the
    public and shared runtimes and apply every required skill version bump.
  - Requirement: R2

### Medium

None.

### Minor

None.

## Deferred Finding Dispositions

| Inherited finding | Final disposition | Current-product rationale |
| --- | --- | --- |
| p02 M1 — exactly 128 newline-terminated records can be treated as incomplete | Remains accepted | The shared exact-all reader still has this boundary behavior (`src/transcript/core/runtimes.ts:859`), so it can affect public discovery. It fails closed: the guidance layer converts the result to path-free `discovery-incomplete`, returns no partial candidate set, and emits no command. This is a narrow availability defect at the configured metadata cap, consistent with R2's explicit-incomplete safety behavior; it should be repaired before claiming broader availability but does not block this experimental guidance product. |
| p02 M2 — newline-bearing Git evidence cannot round-trip through the generic path schema | Remains accepted | The retained executor's generic parsers still reject control-bearing path strings (`src/transcript/coding-session-handoff/types.ts:395`, `src/transcript/coding-session-handoff/types.ts:666`). The public guidance path does not serialize or parse that schema and explicitly rejects LF/CR/NUL before rendering shell guidance (`src/transcript/coding-session-handoff/guidance.ts:74`). Such a path is refused without mutation rather than misquoted. |
| p03 M1 — a ready Claude plan with an intentional empty `--tools` operand cannot round-trip | Remains accepted | The old plan builder still emits the empty operand (`src/transcript/coding-session-handoff/behavior-contracts.ts:234`) while `parseNativeInvocation` applies the nonempty generic string parser to every argument (`src/transcript/coding-session-handoff/types.ts:1100`). The public bundle cannot create or parse executor plans; it builds documented interactive guidance through a separate capability table. This must be fixed before the executor is activated, but it does not affect the shipped guidance surface. |
| p03 M2 — authentication-required collapses into generic probe failure | Remains accepted | The old default planner still performs that collapse (`src/transcript/coding-session-handoff/cli.ts:546`). The public path performs no provider or authentication probe and never renders login guidance from probe results. The defect remains a prerequisite for any future executor activation, not a current public-path failure. |
| p03 M3 — execute/reconcile compares caller aliases rather than canonical roots | Remains accepted | The retained defaults still pass raw caller paths into exact corroboration (`src/transcript/coding-session-handoff/cli.ts:703`, `src/transcript/coding-session-handoff/cli.ts:760`). The public skill exposes neither execute nor reconcile; guidance target validation uses canonical registered worktree roots and the emitted command guards the canonical destination. Fix this before exposing the old executor. |

The five dispositions rely on the documented product boundary: the automated
executor is incomplete, unverified, paused, and absent from public help, exports, and
guidance imports. Enabling that executor would invalidate the p02 M2 and all p03
deferrals immediately and would require a fresh review of p02 M1 in the activated
mutation context.

## Requirements/Design Alignment

**Evidence sources used:** `discovery.md`; `spec.md` including accepted R1-R8 and the
historical contract; `design.md` including the accepted revision and historical
architecture; `plan.md`; `implementation.md`; `state.md`; `revision-handoff.md`;
`summary.md`; `project-log.md`; the p01, p02, p03, p03-t16 through p03-t19, and
p-rev1 review/fix chain; the authoritative 54-commit range; all 71 changed paths;
canonical TypeScript, generated runtimes, tests, public skill/docs, and release
validation surfaces.

### Requirements Coverage

| Requirement | Status | Notes |
| --- | --- | --- |
| R1 | implemented | All three entry points are explicit; current identity is selected only by an exact provider/surface/native-ID match, otherwise the user must select a qualified candidate. No recency selector exists. |
| R2 | partial | Bounded, zero-persistence discovery and sanitized previews are implemented for all providers, and incomplete scans fail closed. I1 shows that Cursor's source-worktree association is not exact when cwd slugs collide. |
| R3 | implemented | Preparation requires distinct, existing, registered worktrees in one canonical Git repository, refuses source dirt, reports destination dirt, and transfers no Git state. |
| R4 | implemented | Codex and Claude instructions use validated IDs, fixed documented argv, shell-word quoting, and a canonical destination cwd guard; output records source selector, provider/surface, expected effect, evidence, and limitations. |
| R5 | implemented | Fresh-destination behavior uses documented switching only where available; otherwise it tells the user to exit and relaunch in the same destination tab. Unsupported Cursor paths emit no guessed command. |
| R6 | implemented | Public help exposes only `discover`, `preview`, and `prepare`. No provider execution, auth, fork, receipt, reconciliation, or retry path is reachable. |
| R7 | implemented | Skill, CLI, documentation, and release surfaces mark guidance experimental/not released and separately mark the retained executor incomplete, unverified, and paused. Historical live failures remain recorded. |
| R8 | implemented | Canonical TypeScript generates distinct public and retained-tool bundles, runtime code remains dependency-free, provider stores are unchanged, portable handoff remains separate, consumers remain covered, and changed skills carry synchronized version metadata. |

### Design and Product-Boundary Alignment

The component split matches the accepted design: guidance capability evidence,
guidance discovery, Git validation, pure instruction construction, and the thin CLI
are separate from the historical executor. Generated outputs derive from canonical
TypeScript and inventory/build checks cover both bundles. Documentation and the public
skill consistently describe destination-tab, user-run behavior and do not claim live
provider proof. I1 is a design-alignment defect at the shared Cursor discovery seam,
where weak store-layout evidence is promoted to exact source identity.

### Extra Work (not in declared requirements)

None. Generated-output mappings, exact skill inventories, release fixtures,
documentation navigation, and version bumps are required consequences of publishing
the new experimental standalone skill while retaining the old tool bundle.

## Safety, Privacy, and Public Surface

- Public preparation performs only bounded local transcript inspection and argv-only
  read-only Git commands; it does not execute provider CLIs or mutate provider stores.
- Preview is bounded and strips tool calls, tool results, command/control content, and
  hidden payload classes; it warns that sanitization cannot remove every human-pasted
  secret. I1 remains material because sanitization does not make another worktree's
  conversation the requested source.
- Codex and Claude native IDs are grammar-validated before command construction;
  shell metacharacters and destination paths are single-word quoted, and LF/CR/NUL
  paths fail before guidance is emitted.
- The public generated bundle has no old `execute`, `behavior-verify`, receipt,
  reconcile, auth-probe, or retry entry point. The retained executor remains under
  `tools/coding-session-handoff/` with paused/incomplete status.
- No live provider command, authentication probe, installation, provider-store read
  outside synthetic tests, session mutation, cleanup, publication, or release action
  was performed by this review.

## Verification

The reviewer resolved both exact SHAs, confirmed the 54-commit/71-path range, reviewed
the post-image and relevant historical findings, and independently ran the full local
verification appropriate to the final range:

- `pnpm run test`: 134 passing files plus one skipped; 1,905 passing tests plus one
  skipped.
- `pnpm run type-check`, `pnpm run build:check`, `pnpm run validate`,
  `pnpm run validate:skill-versions --base-ref
  0042b84937076f875380892b87a39120eeefad9e`, and `pnpm run smoke`: passed.
- Changed authored TypeScript and JavaScript files passed file-scoped `oxlint`;
  changed authored TypeScript, JavaScript, Markdown, and JSON files passed file-scoped
  `oxfmt --check`. This confirms no lint or format error in the exact final range. The
  generic repository-wide lint result cited by implementation contains only inherited
  errors in unchanged files and is outside the repository's incremental changed-file
  lint policy.
- `pnpm --dir documentation run docs:format:check`, exact-range `git diff --check`,
  and Node syntax checks for the public bundle, retained executor bundle, and generated
  session-observer locate module passed.
- The p-rev1 accepted fix was rechecked in current source: unexpected Cursor direct,
  fallback, project-root, iterator, and transcript-stat failures fail the exact-all
  request rather than returning a partial set. I1 is independent of that repair: it
  concerns successful lookup through two canonical paths that encode to the same slug.

The repository formatter intentionally excludes `.oat/**`; this artifact was kept
manually formatted and checked for Markdown structure and diff hygiene without
bypassing that policy.

## Verification Commands

```bash
git rev-parse 0042b84937076f875380892b87a39120eeefad9e^{commit}
git rev-parse 3fdfc2a17b0b94171871a5bf6460beac1a333bea^{commit}
git diff --check 0042b84937076f875380892b87a39120eeefad9e..3fdfc2a17b0b94171871a5bf6460beac1a333bea
pnpm run test
pnpm run type-check
pnpm run build:check
pnpm run validate
pnpm run validate:skill-versions --base-ref 0042b84937076f875380892b87a39120eeefad9e
pnpm run smoke
pnpm --dir documentation run docs:format:check
```

## Recommended Next Step

Run `oat-project-review-receive` to convert I1 into one bounded revision task, repair
Cursor source-association handling and its collision evidence, then run a fresh final
review over the updated head. Keep the retained executor paused and unexposed; its
accepted deferrals require repair before any activation review.
