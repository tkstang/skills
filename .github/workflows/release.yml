name: Release

on:
  push:
    tags:
      - "consensus-v*"
      - "session-v*"

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      GIT_HOOKS: "0"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Capture generated output paths
        run: pnpm tsx scripts/build-generated.ts --list-outputs > "$RUNNER_TEMP/generated-output-paths.txt"
      - run: pnpm run build
      - name: Verify generated outputs are committed
        run: |
          generated_outputs=()
          while IFS= read -r file; do
            generated_outputs+=("$file")
          done < "$RUNNER_TEMP/generated-output-paths.txt"
          git diff --exit-code -- "${generated_outputs[@]}"
      - run: pnpm run type-check
      - run: pnpm run build:check
      - run: pnpm run test
      - run: pnpm run validate
      - run: pnpm run smoke
      - name: Verify selected plugin version
        run: |
          PLUGIN_NAME="${GITHUB_REF_NAME%%-v*}"
          PLUGIN_VERSION="v${GITHUB_REF_NAME#*-v}"
          pnpm tsx scripts/bump-version.ts --check-tag "$PLUGIN_VERSION" --plugin "$PLUGIN_NAME"
